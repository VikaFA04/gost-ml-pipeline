---
phase: 05-rule-profiles-methodical-profile-ingestion
plan: 05
type: execute
wave: 5
depends_on:
  - 05-01
  - 05-02
  - 05-03
  - 05-04
files_modified:
  - tests/fixtures/methodical/normocontrol_berger.pdf
  - .github/workflows/regression-gate.yml
  - Makefile
  - CONTRIBUTING.md
autonomous: false
requirements:
  - REQ-rule-profiles
  - REQ-methodical-profile-extract
user_setup: []

must_haves:
  truths:
    - "`tests/fixtures/methodical/normocontrol_berger.pdf` is committed (binary, ~1.4MB Бергер version) and is NOT gitignored — verified by `git check-ignore` returning empty."
    - "`.github/workflows/regression-gate.yml` runs a 6-file pytest invocation that includes `tests/test_profile_quality_acceptance.py` and `tests/test_methodical_extractor.py`."
    - "`Makefile` regression-gate target runs the same 6-file pytest invocation."
    - "End-to-end CI validation: one PR with clean changes goes GREEN; one designed-failure PR (intentional schema-lint regression) goes RED — both observed in GHA logs and recorded in SUMMARY."
    - "CONTRIBUTING.md updated to mention the methodical fixture + 6-file gate."
    - "CI timeout-minutes is sufficient (10 by default; bumped to 15 only if Бергер extraction time exceeds 60s — verified via local pre-flight)."
  artifacts:
    - path: "tests/fixtures/methodical/normocontrol_berger.pdf"
      provides: "1.4MB Бергер Феврал-2026 normcontrol fixture used by methodical extractor tests in CI."
      contains: "<binary>"
    - path: ".github/workflows/regression-gate.yml"
      provides: "GHA workflow with 6-file pytest invocation (existing 4 + Phase 5 2)."
      contains: "test_profile_quality_acceptance.py"
    - path: "Makefile"
      provides: "Symmetric local 6-file pytest invocation in regression-gate target."
      contains: "test_methodical_extractor.py"
    - path: "CONTRIBUTING.md"
      provides: "Updated developer workflow with methodical-fixture note + 6-file gate."
      contains: "tests/fixtures/methodical"
  key_links:
    - from: ".github/workflows/regression-gate.yml — 'Run regression gate' step"
      to: "tests/test_profile_quality_acceptance.py + tests/test_methodical_extractor.py"
      via: "python -m pytest invocation list"
      pattern: "test_profile_quality_acceptance"
    - from: "Makefile — regression-gate target"
      to: "tests/test_profile_quality_acceptance.py + tests/test_methodical_extractor.py"
      via: "$(PYTHON) -m pytest invocation"
      pattern: "test_methodical_extractor"
---

<objective>
Bring Phase 5's two new test files (`test_profile_quality_acceptance.py` from plan 5-04, `test_methodical_extractor.py` from plan 5-01) under the CI regression gate. The Phase 4 Wave E workflow already runs a 4-file pytest invocation; this plan extends it to 6. The local `Makefile` regression-gate target is updated symmetrically.

A new committed binary fixture `tests/fixtures/methodical/normocontrol_berger.pdf` (1.4 MB, Феврал 2026 Бергер version per D-06) replaces the local 9.4 MB `Нормоконтроль 2026.pdf` for CI use. Per RESEARCH Pattern 5 + Pitfall 6, NO additional workflow-staging step is needed because the fixture lives at its final test-time path.

End-to-end validation mirrors Phase 4 plan 04-05: open one clean PR, observe GREEN; open one designed-failure PR (introduce a bogus required-field in `gost_7_32_2017.json`), observe RED; close without merge.

Purpose: SC-1 + SC-3 + SC-4 enforcement under CI; Phase 5 closes only when the gate is live.
Output: Workflow + Makefile + fixture committed; CI validation PR pair recorded in SUMMARY.

**This plan is `autonomous: false`** because it requires a human checkpoint for:
1. Committing a 1.4 MB binary fixture (verify the file exists at the expected path on disk).
2. Opening the validation PR pair on GitHub (clean PR + designed-failure PR — requires `gh` CLI and a throwaway branch).
3. Reading the GHA logs to confirm GREEN-on-clean / RED-on-broken end-to-end.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/05-rule-profiles-methodical-profile-ingestion/05-CONTEXT.md
@.planning/phases/05-rule-profiles-methodical-profile-ingestion/05-RESEARCH.md
@.planning/phases/05-rule-profiles-methodical-profile-ingestion/05-PATTERNS.md
@.planning/phases/05-rule-profiles-methodical-profile-ingestion/05-VALIDATION.md
@.planning/phases/04-regression-gate/04-05-SUMMARY.md
@.github/workflows/regression-gate.yml
@Makefile

<interfaces>
Current 4-file workflow "Run regression gate" step body (.github/workflows/regression-gate.yml lines 31-35) — extend by appending two more pytest paths:

  python -m pytest -q
    tests/test_negative_corpus_diff_rate.py
    tests/test_positive_docx_regression.py
    tests/test_rules_quality_acceptance.py
    tests/test_format_regression_audit.py

Phase 5 target (6 files):

  python -m pytest -q
    tests/test_negative_corpus_diff_rate.py
    tests/test_positive_docx_regression.py
    tests/test_rules_quality_acceptance.py
    tests/test_format_regression_audit.py
    tests/test_profile_quality_acceptance.py
    tests/test_methodical_extractor.py

Current 4-file Makefile target (Makefile lines 17-21) — extend symmetrically.

Source PDF location on disk:
  /Users/fedorova.van/experiments/gost_formatter/normocontrol/Нормоконтроль Бергер.pdf (1.4 MB)

Target fixture path (NEW per D-06):
  tests/fixtures/methodical/normocontrol_berger.pdf

Phase 4 Wave E lesson: pytest must be invoked as `python -m pytest` (NOT bare `pytest`)
because no pyproject.toml/conftest.py at repo root injects cwd into sys.path.
Verbatim fix from commit 5c6327d. Keep that idiom.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-flight — measure Бергер extraction latency locally</name>

  <read_first>
    - normocontrol/Нормоконтроль Бергер.pdf (file exists at this path; 1.4MB per `ls -la` already in context)
    - .planning/phases/05-rule-profiles-methodical-profile-ingestion/05-RESEARCH.md lines 619-630 (Pitfall 7 — CI runtime budget)
  </read_first>

  <files>
    (none — pre-flight measurement only; outputs recorded in SUMMARY at end of plan)
  </files>

  <action>
Run a one-shot timing probe BEFORE committing any workflow changes. This decides whether the existing `timeout-minutes: 10` is sufficient or must be bumped to 15.

Run this command from the repo root:

    time python3 -c "from pathlib import Path; from src.rules.methodical_extractor import build_methodical_profile; p = Path('normocontrol/Нормоконтроль Бергер.pdf'); profile = build_methodical_profile(input_path=p, base_profile_ids=['gost_7_32_2017']); print('OK', profile.get('profile_id'))"

Record `real` time in the SUMMARY task notes.

**Decision rule (D-10):**
- real time below 30s — keep `timeout-minutes: 10`.
- real time between 30s and 60s — keep `timeout-minutes: 10` (still under 1/10 budget).
- real time above 60s — Task 3 bumps to `timeout-minutes: 15` per Pitfall 7 / D-10.

If the extraction itself fails (e.g., plan 5-01 did not handle some edge case in Бергер PDF Arabic-ligature noise), STOP and return to plan 5-01 for a fix-up — do NOT proceed to fixture commit.
  </action>

  <verify>
    <automated>time python3 -c "from pathlib import Path; from src.rules.methodical_extractor import build_methodical_profile; build_methodical_profile(input_path=Path('normocontrol/Нормоконтроль Бергер.pdf'), base_profile_ids=['gost_7_32_2017']); print('OK')" 2>&1 | tail -5</automated>
  </verify>

  <acceptance_criteria>
    - Command exits 0 and prints `OK`.
    - `real` time observable in the timing output is recorded for downstream decision.
    - If extraction raises any exception, the task fails — return to plan 5-01.
  </acceptance_criteria>

  <done>
    Бергер extraction completes successfully; timing data recorded for Task 3.
  </done>
</task>

<task type="auto">
  <name>Task 2: Copy + commit the Бергер fixture at tests/fixtures/methodical/</name>

  <read_first>
    - .planning/phases/05-rule-profiles-methodical-profile-ingestion/05-RESEARCH.md lines 610-618 (Pitfall 6 — `tests/` is not gitignored; commit via plain `git add`)
    - .planning/phases/05-rule-profiles-methodical-profile-ingestion/05-PATTERNS.md lines 691-705 (committed binary fixture analogs — heading_minimal.docx etc.)
  </read_first>

  <files>tests/fixtures/methodical/normocontrol_berger.pdf</files>

  <action>
1. Create the destination directory:

    mkdir -p tests/fixtures/methodical

2. Copy the Бергер PDF from `normocontrol/` to the canonical ASCII-named target:

    cp "normocontrol/Нормоконтроль Бергер.pdf" tests/fixtures/methodical/normocontrol_berger.pdf

The destination filename is ASCII / underscore-separated per CLAUDE.md «маленькие функции на одну ответственность» — non-ASCII filenames are awkward in CI logs and shell scripts.

3. Verify it is NOT gitignored (Pitfall 6):

    git check-ignore tests/fixtures/methodical/normocontrol_berger.pdf

Expected: empty output, exit code 1 (= NOT ignored).

4. Verify size and content sanity:

    ls -la tests/fixtures/methodical/
    file tests/fixtures/methodical/normocontrol_berger.pdf

Expected: ~1.4MB; `file` output mentions "PDF document".

5. Stage and commit:

    git add tests/fixtures/methodical/normocontrol_berger.pdf
    git commit -m "feat(05-05): commit Бергер methodical fixture (1.4MB, D-06)"

Do NOT use `git add -f` — the file lives under `tests/` which is not gitignored.
  </action>

  <verify>
    <automated>ls -la tests/fixtures/methodical/normocontrol_berger.pdf && git ls-files tests/fixtures/methodical/ | grep normocontrol_berger</automated>
  </verify>

  <acceptance_criteria>
    - `ls -la tests/fixtures/methodical/normocontrol_berger.pdf` shows the file exists with size between 1_000_000 and 2_000_000 bytes.
    - `git ls-files tests/fixtures/methodical/normocontrol_berger.pdf` outputs the file path (= it is tracked).
    - `git check-ignore tests/fixtures/methodical/normocontrol_berger.pdf` exits non-zero with empty stdout (= not ignored).
    - `file tests/fixtures/methodical/normocontrol_berger.pdf` output contains the string `PDF`.
    - `git log -1 --pretty=%s` shows commit subject starting with `feat(05-05):` and mentioning the fixture.
  </acceptance_criteria>

  <done>
    Бергер fixture committed at the canonical path; gitignore check passes.
  </done>
</task>

<task type="auto">
  <name>Task 3: Extend GHA workflow + Makefile to 6-file pytest invocation</name>

  <read_first>
    - .github/workflows/regression-gate.yml (current 35 LOC — extend "Run regression gate" step from 4 files to 6)
    - Makefile (current 22 LOC — extend pytest invocation from 4 files to 6)
    - .planning/phases/05-rule-profiles-methodical-profile-ingestion/05-PATTERNS.md lines 708-765 (verbatim 4-to-6 file diff for both)
    - .planning/phases/05-rule-profiles-methodical-profile-ingestion/05-RESEARCH.md lines 619-630 (Pitfall 7 — timeout-minutes decision based on Task 1 timing)
    - Task 1 SUMMARY notes (Бергер extraction real-time; informs timeout-minutes value)
  </read_first>

  <files>
    .github/workflows/regression-gate.yml,
    Makefile
  </files>

  <action>
1. Edit `.github/workflows/regression-gate.yml`. Replace the "Run regression gate" step body so the pytest invocation lists SIX test files (existing four + Phase 5 two). Preserve the leading `python -m pytest -q \\` form and the 2-space YAML indentation.

The new step body:

      - name: Run regression gate
        run: |
          python -m pytest -q \
            tests/test_negative_corpus_diff_rate.py \
            tests/test_positive_docx_regression.py \
            tests/test_rules_quality_acceptance.py \
            tests/test_format_regression_audit.py \
            tests/test_profile_quality_acceptance.py \
            tests/test_methodical_extractor.py

2. Decide `timeout-minutes` per Pitfall 7:
- If Task 1 real time was under 60s, keep `timeout-minutes: 10` (no change).
- If Task 1 real time was 60s or above, change `timeout-minutes: 10` to `timeout-minutes: 15`.

3. NO fixture-staging step is needed for the methodical fixture. The fixture committed in Task 2 lives at its final test-time path (`tests/fixtures/methodical/normocontrol_berger.pdf`). The existing `Stage corpus subset from fixtures` step copies positive/negative DOCX corpora into `positive_examples/` and `negative_examples/` because those are the canonical input directories for `audit-regression` — that pattern does NOT apply here.

4. Edit `Makefile`. Replace the pytest-invocation block (currently 4 file paths) with a 6-file version. Preserve the tabs and continuation backslashes:

    $(PYTHON) -m pytest -q \
        tests/test_negative_corpus_diff_rate.py \
        tests/test_positive_docx_regression.py \
        tests/test_rules_quality_acceptance.py \
        tests/test_format_regression_audit.py \
        tests/test_profile_quality_acceptance.py \
        tests/test_methodical_extractor.py

5. Verify YAML + Makefile syntax:

    python3 -c "import yaml; yaml.safe_load(open('.github/workflows/regression-gate.yml'))" && echo OK
    make -n regression-gate >/dev/null && echo MAKE-OK

6. Local smoke test of just the Phase 5 subset (skip the corpus tests if positive_examples/negative_examples aren't staged locally):

    python3 -m pytest -q tests/test_profile_quality_acceptance.py tests/test_methodical_extractor.py

Must exit 0. If `positive_examples/` and `negative_examples/` directories exist on disk:

    make regression-gate

Should exit 0.

7. Commit:

    git add .github/workflows/regression-gate.yml Makefile
    git commit -m "feat(05-05): extend regression gate to 6 files (Phase 5 schema + extractor)"
  </action>

  <verify>
    <automated>python3 -c "import yaml; yaml.safe_load(open('.github/workflows/regression-gate.yml'))" && grep -c "test_profile_quality_acceptance.py" .github/workflows/regression-gate.yml Makefile && python3 -m pytest -q tests/test_profile_quality_acceptance.py tests/test_methodical_extractor.py 2>&1 | tail -3</automated>
  </verify>

  <acceptance_criteria>
    - `python3 -c "import yaml; yaml.safe_load(open('.github/workflows/regression-gate.yml'))"` exits 0.
    - `grep -c "test_profile_quality_acceptance.py" .github/workflows/regression-gate.yml` returns 1.
    - `grep -c "test_methodical_extractor.py" .github/workflows/regression-gate.yml` returns 1.
    - `grep -c "test_profile_quality_acceptance.py" Makefile` returns 1.
    - `grep -c "test_methodical_extractor.py" Makefile` returns 1.
    - `make -n regression-gate` exits 0 (dry-run; does not need corpus directories).
    - `python3 -m pytest -q tests/test_profile_quality_acceptance.py tests/test_methodical_extractor.py` exits 0.
    - `timeout-minutes:` line in workflow is either `10` (Task 1 fast) or `15` (Task 1 slow) — choice justified in SUMMARY.
    - Git commit subject matches `^feat\(05-05\):`.
  </acceptance_criteria>

  <done>
    Workflow + Makefile updated; YAML/Make syntactically valid; Phase 5 tests pass locally.
  </done>
</task>

<task type="auto">
  <name>Task 4: Update CONTRIBUTING.md with methodical-fixture note</name>

  <read_first>
    - CONTRIBUTING.md (existing — phase 4 plan 04-04 added the regression-gate Pre-PR section; extend it)
  </read_first>

  <files>CONTRIBUTING.md</files>

  <action>
Append a short section to `CONTRIBUTING.md` documenting the methodical-fixture + 6-file gate. Russian-language section heading and bullets per D-11.

Append (after the existing regression-gate / `make regression-gate` section):

    ## Методичка-фикстура и 6-file regression gate (Phase 5)

    Регресс-гейт включает два новых файла Phase 5:

    - `tests/test_profile_quality_acceptance.py` — двухуровневый schema-lint
      всех профилей в `src/rules/profiles/*.json` (Tier A: все профили; Tier B:
      только `methodical_guidelines`).
    - `tests/test_methodical_extractor.py` — smoke-тесты экстрактора над
      Бергер PDF фикстурой.

    Фикстура коммитится в `tests/fixtures/methodical/normocontrol_berger.pdf`
    (1.4 MB Феврал 2026; ASCII-имя по соображениям CI-логов). Локальные
    разработчики могут также прогонять экстрактор против актуальной
    `normocontrol/Нормоконтроль 2026.pdf` (9.4 MB, gitignored).

    Перед PR: `make regression-gate` (6 файлов) или хотя бы
    `python3 -m pytest -q tests/test_profile_quality_acceptance.py tests/test_methodical_extractor.py`.

Commit:

    git add CONTRIBUTING.md
    git commit -m "docs(05-05): document methodical fixture + 6-file gate"
  </action>

  <verify>
    <automated>grep -c "tests/fixtures/methodical" CONTRIBUTING.md && grep -c "Phase 5" CONTRIBUTING.md</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "tests/fixtures/methodical" CONTRIBUTING.md` returns 1 or more.
    - `grep -c "test_profile_quality_acceptance" CONTRIBUTING.md` returns 1 or more.
    - `grep -c "test_methodical_extractor" CONTRIBUTING.md` returns 1 or more.
    - The new section heading is in Russian per D-11 (contains Cyrillic chars).
    - Git commit subject matches `^docs\(05-05\):`.
  </acceptance_criteria>

  <done>
    Developer-facing docs reflect the new gate.
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 5: End-to-end CI validation via clean PR + designed-failure PR</name>

  <files>
    (no local files modified by this task — opens two PRs on the GitHub remote
    and validates the GHA workflow runs end-to-end; PR URLs recorded in SUMMARY)
  </files>

  <action>
    Run the checkpoint workflow described in `<how-to-verify>` below. The
    executor opens TWO PRs against `main`: one clean PR (expected GREEN) and one
    designed-failure PR (expected RED). The checkpoint cannot be automated
    because GitHub PR review + log inspection requires a human decision. See
    `<how-to-verify>` for the exact `gh` CLI sequence.
  </action>

  <verify>
    <automated>gh pr list --head "$(git branch --show-current)" --json url,statusCheckRollup --jq '.[]' 2>&1 | head -20</automated>
  </verify>

  <done>
    Clean PR run shows GREEN with all 6 pytest files passing; designed-failure
    PR run shows RED with a Tier A schema-lint test failing; URLs of both runs
    pasted into SUMMARY; designed-failure PR closed without merge.
  </done>

  <what-built>
    Phase 5 plans 5-01 through 5-04 + Tasks 1-4 of this plan are committed on a feature branch.
    The Бергер fixture is committed at `tests/fixtures/methodical/normocontrol_berger.pdf`.
    The GHA workflow + Makefile run 6 test files. CONTRIBUTING.md mentions the new gate.
  </what-built>

  <how-to-verify>
    Phase 4 plan 04-05 Wave E established the canonical validation pattern (PR #1 GREEN + PR #2 RED on VikaFA04/gost-ml-pipeline). Mirror that pattern for Phase 5.

    **Clean PR (expect GREEN):**

    1. Confirm the current branch (Phase 5 plans 5-01..5-04 + this plan's Tasks 1-4 committed) is pushed:

           git push -u origin "$(git branch --show-current)"

    2. Open a PR against `main`:

           gh pr create --title "Phase 5: methodical extractor + profile diff + dry-run CLI + CI gate" --body "Phase 5 plans 5-01..5-05. Closes REQ-rule-profiles + REQ-methodical-profile-extract. CI run validates the 6-file gate end-to-end."

    3. Wait for the GHA `regression-gate` workflow run to complete:

           gh pr checks --watch

    4. Confirm GREEN. Record run URL in SUMMARY.

    **Designed-failure PR (expect RED):**

    5. From a NEW throwaway branch off the same feature branch:

           git checkout -b gsd/phase-05-designed-failure-pr
           # Intentionally remove a required top-level key from gost_7_32_2017.json
           # (e.g., delete the "is_default" key) to force Tier A schema lint RED.

    6. Commit:

           git add src/rules/profiles/gost_7_32_2017.json
           git commit -m "test(05-05): designed-failure — drop is_default to trigger Tier A RED"

    7. Push + open PR:

           git push -u origin gsd/phase-05-designed-failure-pr
           gh pr create --title "DESIGNED FAILURE: drop is_default from gost_7_32_2017" --body "Phase 5 plan 5-05 Task 5 — designed-failure validation. Expect Tier A schema lint RED. Will be closed without merge."

    8. Wait for the gate to fail:

           gh pr checks --watch

    9. Confirm the failing job name and the failing test name in the GHA logs include `test_profile_quality_acceptance` / `test_every_profile_has_required_top_level_keys` (or whichever Tier A test fires).

    10. Close the designed-failure PR without merging:

            gh pr close gsd/phase-05-designed-failure-pr

    11. Delete the local branch:

            git checkout -
            git branch -D gsd/phase-05-designed-failure-pr
  </how-to-verify>

  <resume-signal>Type "approved" with run URLs of both PRs (clean GREEN + designed-failure RED) or describe issues observed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Бергер PDF fixture in `tests/fixtures/methodical/` | Committed binary file; trusted source (project owner). |
| GHA workflow input (PR HEAD ref) | Untrusted contributor code crosses into CI runner. |
| CI runner shell → `python -m pytest` | Repo-controlled command; no remote secret usage. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02 | Tampering (inherited) | Fixture path traversal during test setup | accept | Tests use a hardcoded relative path `tests/fixtures/methodical/normocontrol_berger.pdf`; no user-supplied path. Severity: low. |
| T-05-01 | Tampering (inherited) | CI cannot bypass `--reason ≥ 8` guard | accept | Mitigation lives in plan 5-03 dispatcher; CI does not invoke `extract-methodical-profile --apply --force` (only runs the dry-run path via tests). Severity: low. |
| T-05-02 | Tampering (inherited) | Confidence clamping in extractor | accept | Mitigation lives in plan 5-01 `_clamp_confidence`; CI extractor tests assert `0.0 <= c <= 1.0` invariant. Severity: low. |
| T-05-07 | Supply-chain — fixture poisoning | A malicious PR replaces `normocontrol_berger.pdf` with a PDF that exploits pymupdf | accept | pymupdf is sandboxed in the runner; PRs from external contributors are reviewed before merge (existing policy). Severity: low. Detection: `ls -la` size jumps from 1.4MB; reviewer notices. |
| T-05-08 | DoS — CI timeout from slow Бергер extraction | Plan 5-05 Task 1 measures local time; Task 3 bumps timeout only if needed | mitigate | Pre-flight timing in Task 1; conditional `timeout-minutes: 15` bump in Task 3 per Pitfall 7. Severity: low. |
</threat_model>

<verification>
- `git ls-files tests/fixtures/methodical/normocontrol_berger.pdf` returns the file path (tracked).
- `python3 -c "import yaml; yaml.safe_load(open('.github/workflows/regression-gate.yml'))"` exits 0.
- `grep "test_profile_quality_acceptance.py" .github/workflows/regression-gate.yml Makefile | wc -l` returns 2.
- `grep "test_methodical_extractor.py" .github/workflows/regression-gate.yml Makefile | wc -l` returns 2.
- `python3 -m pytest -q tests/test_profile_quality_acceptance.py tests/test_methodical_extractor.py` exits 0 locally.
- Two PRs created on the remote, one GREEN run + one RED run, URLs recorded in SUMMARY.
- Designed-failure PR closed without merge.
</verification>

<success_criteria>
Plan 5-05 closes when:
- Бергер fixture committed at `tests/fixtures/methodical/normocontrol_berger.pdf` (tracked, not gitignored, ~1.4 MB).
- `.github/workflows/regression-gate.yml` + `Makefile` both run a 6-file pytest invocation.
- CONTRIBUTING.md documents the methodical fixture + 6-file gate in Russian per D-11.
- Clean PR run is GREEN; designed-failure PR run is RED; both run URLs recorded.
- All Phase 5 plans (5-01..5-05) closed; ROADMAP Phase 5 SC-1..SC-4 verifiable end-to-end against the gate.
</success_criteria>

<output>
After completion, create `.planning/phases/05-rule-profiles-methodical-profile-ingestion/05-05-SUMMARY.md` containing:
- Task 1 `real` time for the Бергер extraction.
- Task 3 timeout-minutes decision (kept at 10 vs. bumped to 15) with justification.
- Task 2/3/4 commit SHAs.
- Task 5 PR URLs (clean GREEN + designed-failure RED) with the failing test name observed in the RED run.
- A short note confirming Phase 5 SC-1 through SC-4 are now end-to-end verifiable.
- Pointer to the next step: `/gsd-verify-work 5` to close Phase 5 formally.
</output>
