---
phase: 04-regression-gate
plan: 01
type: execute
wave: A
depends_on: []
files_modified:
  - .planning/phases/04-regression-gate/04-WAVE-A-3docx-rootcause.md
  - results/reports/regression_audit_phase4_worst_offenders.csv
  - results/reports/regression_audit_phase4_worst_offenders.json
  - results/reports/3docx_style_diff_HEAD.txt
autonomous: false
requirements:
  - REQ-fix-negative-corpus-no-regression
threat_model:
  - id: T-04-A-00
    note: "Wave A operates only on local files in trusted dev environment — no new attack surface (read-only git bisect + audit CLI runs against local corpus)."

must_haves:
  truths:
    - "3.docx pair drift (0.318 → 0.334) has a documented root cause"
    - "A D-05 branch is chosen on the basis of the root cause (Branch A = bug fix → ceiling 0.318; Branch B = legit Phase 2/3 behaviour change → amend ROADMAP + lock at justified ceiling)"
    - "Worst-offender CSV exists so Wave B can pin the subset deterministically (excluding 58.docx, 59.docx)"
  artifacts:
    - path: ".planning/phases/04-regression-gate/04-WAVE-A-3docx-rootcause.md"
      provides: "Root-cause hypothesis + D-05 branch declaration + locked baseline ceiling value (0.318 OR root-cause-justified value)"
      contains: "Branch A OR Branch B"
    - path: "results/reports/regression_audit_phase4_worst_offenders.csv"
      provides: "Full-corpus regression-audit CSV used to pick worst offenders (Probe 1)"
    - path: "results/reports/regression_audit_phase4_worst_offenders.json"
      provides: "Summary JSON for the same audit run"
  key_links:
    - from: ".planning/phases/04-regression-gate/04-WAVE-A-3docx-rootcause.md"
      to: "Wave B baseline-locking task"
      via: "human reads the artefact, locks ceiling value + (if Branch B) amends ROADMAP/REQUIREMENTS in the same commit before baseline JSON write"
      pattern: "Branch [AB]"
---

<objective>
Wave A: root-cause hunt on the 3.docx pair regression (0.318 → 0.334 per FORMAT_FIX_PLAN Этап 8) BEFORE Wave B locks any baseline ceiling.

Purpose: per D-04 + D-05 + Pitfall 3, Wave B must not silently baseline `0.334` — the ceiling depends on whether the +0.016 drift is a bug (Branch A) or a legitimate Phase 2/3 behaviour change (Branch B). Per CLAUDE.md "При выборе gate-варианта по success criterion ... отдавай предпочтение опции, обусловленной выявлением корневой причины".

Output:
- `04-WAVE-A-3docx-rootcause.md` — written conclusion + D-05 branch declaration.
- `regression_audit_phase4_worst_offenders.csv` + `.json` — full-corpus audit used by Wave B to pin the subset.
- `3docx_style_diff_HEAD.txt` — before/after style-diff dump.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-regression-gate/04-CONTEXT.md
@.planning/phases/04-regression-gate/04-RESEARCH.md
@.planning/phases/04-regression-gate/04-PATTERNS.md
@.planning/FORMAT_FIX_PLAN.md
@CLAUDE.md

<interfaces>
<!-- Audit CLI signature consumed by this wave — verbatim from RESEARCH.md Probe 1 + src/main.py line 350. -->

From src/main.py audit-regression argparse (lines 350-379):
  --positive-dir PATH       (required)
  --negative-dir PATH       (required)
  --workspace-dir PATH      (optional)
  --report-csv PATH         (optional)
  --summary-json PATH       (optional)
  --profile-id ID           (default: gost_7_32_2017)
  --limit N                 (optional)
  --progress                (optional flag)

From RESEARCH.md Probe 3 — suspect commit ranking (highest suspicion first):
  HIGH:  c4d67ac (Phase 2 D-06 numId coerce), 666f888 (Phase 2 D-05 multilevel), 7207cbe (Phase 3 D-05/D-06 heading routing)
  MED:   ac41aaa (Phase 3 +17 heading rules), 2ab21f0 (postprocess D-04), 3c3b179 (ambiguous list)
  LOW-MED: 9202539 (bibliography strip), LOW: 9195efb (Phase 1 guard)

Bisect strategy: start at median candidate 7207cbe. If diff_rate JUMPS at that commit, Branch B (Phase 3 heading routing). If drift predates 7207cbe, retest at c4d67ac (Phase 2 numId).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full-corpus regression audit to enumerate worst offenders</name>
  <files>
    results/reports/regression_audit_phase4_worst_offenders.csv,
    results/reports/regression_audit_phase4_worst_offenders.json
  </files>
  <read_first>
    - .planning/phases/04-regression-gate/04-RESEARCH.md §"Probe 1: Worst-offender enumeration for D-02" (lines 706-729) — exact CLI invocation.
    - .planning/phases/04-regression-gate/04-PATTERNS.md §"src/main.py (MODIFIED — argparse + dispatcher + cmd_audit_regression extension)" — argparse flag set.
    - .planning/phases/04-regression-gate/04-CONTEXT.md D-02 ("Pinned explicit subset … must include 3.docx") + D-11.
    - CLAUDE.md «Ограничивай regression-аудит параметром limit для быстрых прогонов» (но Wave A — это full-corpus enumeration).
  </read_first>
  <action>
    Run the full-corpus regression audit verbatim per RESEARCH.md Probe 1. Do NOT pass `--limit` — Wave A needs all docs to pick worst offenders. Use the existing CLI as-is (this is BEFORE the Wave D extension); no flag changes here.

    Exact invocation (use `.venv/Scripts/python.exe` if on Windows venv per README.md line 37, otherwise `python`):

    ```bash
    python -m src.main audit-regression \
      --positive-dir positive_examples \
      --negative-dir negative_examples \
      --profile-id gost_7_32_2017 \
      --report-csv results/reports/regression_audit_phase4_worst_offenders.csv \
      --summary-json results/reports/regression_audit_phase4_worst_offenders.json \
      --progress
    ```

    After the run completes, inspect the CSV to identify candidates for Wave B's pinned subset per D-02 + RESEARCH.md Probe 1 recommendations:
    - `3.docx` (mandatory — D-02).
    - 1–2 worst offenders by `after_diff_rate` DESC, **EXCLUDING** `58.docx` and `59.docx` (out of scope per Phase 3 D-08 — these are practice reports, not GOST coursework; the existing audit MAY include them but the subset MUST NOT — see `.planning/REQUIREMENTS.md` REQ-fix-docx-generator-custom-styles "deferred to v2").
    - 1 sanity-stable doc (lowest `after_diff_rate`, ideally `≤ 0.1`).

    Record the chosen 4 filenames in a scratchpad inside the task body — they're consumed by the next task. Do NOT yet write any subset list to disk (Wave B writes the baseline JSON).
  </action>
  <verify>
    <automated>
      test -f results/reports/regression_audit_phase4_worst_offenders.csv &amp;&amp; \
      test -f results/reports/regression_audit_phase4_worst_offenders.json &amp;&amp; \
      head -1 results/reports/regression_audit_phase4_worst_offenders.csv | grep -q "after_diff_rate" &amp;&amp; \
      python -c "import pandas as pd; df = pd.read_csv('results/reports/regression_audit_phase4_worst_offenders.csv', encoding='utf-8-sig'); assert '3.docx' in df['negative'].values, '3.docx missing from full-corpus audit'; print('3.docx after_diff_rate =', float(df[df['negative']=='3.docx']['after_diff_rate'].iloc[0]))"
    </automated>
  </verify>
  <acceptance_criteria>
    - `results/reports/regression_audit_phase4_worst_offenders.csv` exists and has the header column `after_diff_rate` (frame from `audits_to_frame`).
    - The CSV contains a row where `negative == "3.docx"`; the printed `after_diff_rate` is the empirical Wave A datapoint (expected ≈ 0.334 per FORMAT_FIX_PLAN Этап 8; if it differs materially, that itself is information for Task 3).
    - `results/reports/regression_audit_phase4_worst_offenders.json` is valid JSON (verified by `python -c "import json; json.load(open(...))"`).
  </acceptance_criteria>
  <done>
    Full-corpus audit CSV + summary JSON exist on disk; `3.docx`'s `after_diff_rate` value is known from the actual run (not from FORMAT_FIX_PLAN folklore); 4-doc subset candidates picked (recorded for the next task to use, but NOT yet written to a baseline JSON).
  </done>
</task>

<task type="auto">
  <name>Task 2: Bisect 3.docx drift against Phase 2 / Phase 3 suspect commits</name>
  <files>
    results/reports/3docx_style_diff_HEAD.txt
  </files>
  <read_first>
    - .planning/phases/04-regression-gate/04-RESEARCH.md §"Probe 3: 3.docx Wave A suspect commits" (lines 742-757) — exact suspect commit ranking and bisect strategy.
    - .planning/phases/04-regression-gate/04-CONTEXT.md D-04 (investigation wave).
    - .planning/FORMAT_FIX_PLAN.md Этап 8 — original 0.318 → 0.334 datapoint.
    - src/evaluation/format_regression_audit.py lines 1-194 (especially `audit_negative_directory`, `audits_to_frame`) — already verified in RESEARCH.md.
  </read_first>
  <action>
    Goal: identify which commit (or commit range) introduced the +0.016 drift on `3.docx` and classify it (bug vs legitimate behaviour change). Per RESEARCH.md Probe 3 the bisect strategy is NOT blind — use the suspicion ranking.

    Step 1 — capture HEAD style-diff dump for the `3.docx` pair. Use the existing CLI with `--limit 1` is NOT enough (lexicographic), so audit the whole negative dir into a temp workspace and grab the per-pair style-diff. Concrete invocation:

    ```bash
    mkdir -p results/reports/3docx_wave_a
    python -m src.main audit-regression \
      --positive-dir positive_examples \
      --negative-dir negative_examples \
      --workspace-dir results/reports/3docx_wave_a \
      --profile-id gost_7_32_2017 \
      --report-csv results/reports/3docx_wave_a/report.csv \
      --summary-json results/reports/3docx_wave_a/summary.json \
      --progress
    ```

    The per-pair workspace contains the formatted DOCX. Dump the after-style-signature comparison to `results/reports/3docx_style_diff_HEAD.txt` using whatever existing project tooling produces a style diff — at minimum, capture `audit_negative_directory`'s per-pair `after_field_mismatches` and `after_diff_rate` values plus the original-vs-formatted summary from the CSV row.

    Step 2 — git bisect by suspicion. Start at median suspect `7207cbe` (Phase 3 D-05/D-06 heading routing). Use `git stash` (or worktree) to avoid corrupting working tree. For each candidate commit:

    ```bash
    git stash push -u -m "wave-a-bisect-stash" || true
    git checkout <commit> -- src/ tests/    # narrow checkout — do NOT touch .planning/
    python -m src.main audit-regression --positive-dir positive_examples --negative-dir negative_examples --profile-id gost_7_32_2017 --limit 4 --report-csv /tmp/3doc_at_<commit>.csv --summary-json /tmp/3doc_at_<commit>.json
    # Inspect the 3.docx row's after_diff_rate; record.
    git checkout HEAD -- src/ tests/         # restore HEAD
    git stash pop || true
    ```

    Note: `--limit 4` is lexicographic and `3.docx` will not be in the first 4 (per Pitfall 1). For the bisect, either run without `--limit` (slow but correct) OR copy `negative_examples/3.docx` to a private tmp dir and run the audit against that single-doc dir. Choose the option that fits the executor's time budget; document the choice in Task 3's artefact.

    Per Probe 3 ranking, probe in this order: `7207cbe` → if drift present, `666f888` → `c4d67ac`. If drift not yet present at `7207cbe`, probe newer commits: `ac41aaa` → `2ab21f0`. Stop when the binary-search interval is one commit wide; that commit is "the cause".

    Step 3 — classify the cause:
    - "Bug" = unsafe autofix, lost text, regression of an explicit Phase 1/2/3 invariant, output that violates the GOST profile.
    - "Legit behaviour change" = bibliography unification under single numId (Phase 2 D-06), heading per-field source routing (Phase 3 D-05/D-06), or any other documented decision that intentionally changes safe-formatter output. Cite the specific D-XX in the conclusion.

    Per CLAUDE.md «При выборе gate-варианта по success criterion …»: if the root cause is a legit decision, the ROADMAP success criterion 2 ("3.docx pair returns to ≤ 0.318") is wrong to enforce as-is — Wave B's Branch B amends it instead.
  </action>
  <verify>
    <automated>
      test -f results/reports/3docx_style_diff_HEAD.txt &amp;&amp; \
      test -s results/reports/3docx_style_diff_HEAD.txt &amp;&amp; \
      grep -q "3.docx" results/reports/3docx_style_diff_HEAD.txt
    </automated>
  </verify>
  <acceptance_criteria>
    - `results/reports/3docx_style_diff_HEAD.txt` exists, is non-empty, and references `3.docx`.
    - Working tree is restored to HEAD after bisect (`git status` shows no unintended `src/` or `tests/` changes from the bisect process).
    - The executor has identified (at minimum) which commit range the drift sits in; recorded for Task 3's artefact.
  </acceptance_criteria>
  <done>
    Style-diff dump on disk; bisect range narrowed to a specific commit (or commit pair); working tree clean.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Write Wave A conclusion artefact and pick D-05 branch</name>
  <files>
    .planning/phases/04-regression-gate/04-WAVE-A-3docx-rootcause.md
  </files>
  <read_first>
    - .planning/phases/04-regression-gate/04-CONTEXT.md D-05 (conditional success bar) + D-04 (investigation wave) — branch definitions.
    - .planning/phases/04-regression-gate/04-RESEARCH.md §"Pitfall 3" (lines 413-421) — explicit-commit + audit-trail requirement; warning sign "no ROADMAP edit in the diff".
    - .planning/phases/04-regression-gate/04-PATTERNS.md §".planning/ROADMAP.md (MODIFIED, conditional Wave A→B)" — Phase 3 D-08 amendment idiom.
    - CLAUDE.md «При выборе gate-варианта по success criterion из ROADMAP/REQUIREMENTS отдавай предпочтение опции, обусловленной выявлением корневой причины».
    - results/reports/regression_audit_phase4_worst_offenders.csv (from Task 1) + results/reports/3docx_style_diff_HEAD.txt (from Task 2).
  </read_first>
  <action>
    Create `.planning/phases/04-regression-gate/04-WAVE-A-3docx-rootcause.md` with EXACTLY these sections (Russian-language headers consistent with the rest of `.planning/`):

    ```markdown
    # Phase 4 — Wave A: 3.docx root-cause investigation

    **Date:** 2026-05-13
    **Author:** <executor>
    **Status:** complete

    ## Empirical datapoints
    - 3.docx after_diff_rate at HEAD: <X> (from regression_audit_phase4_worst_offenders.csv)
    - FORMAT_FIX_PLAN Этап 8 baseline: 0.318
    - FORMAT_FIX_PLAN Этап 8 reported regression: 0.334
    - Drift observed vs FORMAT_FIX_PLAN: <+/-Δ>

    ## Bisect trace
    | Commit | Phase | 3.docx after_diff_rate | Comment |
    |--------|-------|------------------------|---------|
    | HEAD   | -     | <X>                    | observed |
    | <c1>   | <P>   | <X1>                   | <bisect step 1> |
    | …      | …     | …                      | …       |

    ## Root cause
    <One paragraph stating the commit / change identified, with a quoted code or
    spec excerpt that ties the change to the diff_rate movement.>

    ## D-05 branch decision
    **Branch chosen:** A (bug fix) | B (legit behaviour change — ROADMAP amendment)

    **Locked ceiling for `3.docx` in Wave B baseline JSON:**
    - Branch A → `after_diff_rate_ceiling = 0.318`, `field_mismatch_ceiling = <int from audit row>`
    - Branch B → `after_diff_rate_ceiling = <X rounded to 6 decimals>`, `field_mismatch_ceiling = <int from audit row>`

    ## Actions for Wave B
    - [ ] Branch A only: land targeted fix in Wave B Task 1; verify 3.docx returns to ≤ 0.318; THEN write baseline JSON.
    - [ ] Branch B only: amend `.planning/ROADMAP.md` Phase 4 success criterion 2 with citation of <D-XX from Phase 2/3>; amend `.planning/REQUIREMENTS.md` REQ-fix-negative-corpus-no-regression line "3.docx pair returns to ≤ 0.318" → "3.docx pair ≤ <locked>"; commit BEFORE the baseline JSON write (same PR, atomic).
    ```

    Fill in every `<placeholder>`. The "Locked ceiling" lines MUST contain real numbers (not TBD) — Wave B reads this artefact verbatim.

    CHECKPOINT: pause for human review. The user is the final reviewer on which branch to lock — per CLAUDE.md «не выбирай молча» when multiple interpretations exist (a high-suspicion commit may have BOTH bug aspects AND legit behaviour-change aspects). Present the evidence; let the user lock the branch in the artefact.
  </action>
  <what-built>
    Written root-cause investigation artefact at `.planning/phases/04-regression-gate/04-WAVE-A-3docx-rootcause.md` with: empirical 3.docx datapoint, bisect trace table, root cause paragraph, D-05 branch declaration, locked ceiling values for Wave B.
  </what-built>
  <how-to-verify>
    1. Open `.planning/phases/04-regression-gate/04-WAVE-A-3docx-rootcause.md`.
    2. Confirm every section is populated (no `<placeholder>` strings remain).
    3. Confirm the D-05 branch is named (A or B) with rationale tied to the bisect trace.
    4. Confirm the "Locked ceiling" line names a concrete number (e.g. `0.318` for Branch A or e.g. `0.334` for Branch B).
    5. Cross-check the chosen subset candidates from Task 1 (3.docx + 1–2 worst offenders excluding 58/59 + 1 sanity-stable) are also listed in the artefact (Wave B reads them).
    6. If Branch B: confirm the rationale cites a specific Phase 2/3 D-XX decision (e.g. "Phase 2 D-06 numId coercion" or "Phase 3 D-05/D-06 heading routing").
    7. Type "approved" to unblock Wave B, OR describe what's missing.
  </how-to-verify>
  <verify>
    <automated>
      test -f .planning/phases/04-regression-gate/04-WAVE-A-3docx-rootcause.md &amp;&amp; \
      ! grep -q "&lt;placeholder&gt;\|&lt;X&gt;\|TBD" .planning/phases/04-regression-gate/04-WAVE-A-3docx-rootcause.md &amp;&amp; \
      grep -Eq "Branch chosen.*[AB]" .planning/phases/04-regression-gate/04-WAVE-A-3docx-rootcause.md &amp;&amp; \
      grep -Eq "after_diff_rate_ceiling = [0-9]" .planning/phases/04-regression-gate/04-WAVE-A-3docx-rootcause.md &amp;&amp; \
      grep -Eq "field_mismatch_ceiling = [0-9]" .planning/phases/04-regression-gate/04-WAVE-A-3docx-rootcause.md
    </automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/04-regression-gate/04-WAVE-A-3docx-rootcause.md` exists.
    - File contains the literal string `Branch chosen:` followed by either `A` or `B`.
    - File contains a literal `after_diff_rate_ceiling = <numeric>` line (grep-checkable regex `after_diff_rate_ceiling = [0-9]`).
    - File contains a literal `field_mismatch_ceiling = <int>` line.
    - File contains no remaining `<placeholder>`, `<X>`, or `TBD` strings (all placeholders filled).
    - File contains the chosen 4-doc subset list (3.docx + 3 others, excluding 58/59).
    - Human has typed "approved" before Wave B starts.
  </acceptance_criteria>
  <resume-signal>Type "approved" or describe issues</resume-signal>
  <done>
    Wave A artefact is complete, human-approved, contains a concrete D-05 branch + ceiling values + 4-doc subset list. Wave B can start.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev shell → local FS | Wave A runs only against local corpus + local git history; no network, no untrusted input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-A-00 | — | git bisect + audit-regression CLI | accept | No new attack surface. The CLI was already audited in Phase 1/2/3; Wave A only runs it. Bisect operates on already-committed git history. No untrusted input crosses a trust boundary. |
</threat_model>

<verification>
- Task 1 verify command exits 0 (CSV + JSON exist, 3.docx present, after_diff_rate column readable).
- Task 2 verify command exits 0 (style-diff dump exists, references 3.docx, working tree clean).
- Task 3 verify command exits 0 (artefact exists, branch chosen, ceilings concrete, no placeholders).
- After approval, the artefact contains BOTH the empirical 3.docx after_diff_rate (from Task 1) AND a concrete D-05 branch declaration; Wave B's first task can read it without re-running any audit.
</verification>

<success_criteria>
- 3.docx root cause is documented in writing, not in chat history.
- D-05 branch is locked (Branch A or Branch B) before Wave B touches any test or baseline file.
- Worst-offender CSV is on disk; subset candidates are named (so Wave B's baseline JSON `_metadata.subset_filenames` is deterministic).
- ROADMAP success criterion 2 is left UNTOUCHED if Branch A; flagged for amendment-in-Wave-B if Branch B.
</success_criteria>

<output>
After completion, create `.planning/phases/04-regression-gate/04-01-SUMMARY.md` summarising the Wave A findings + the D-05 branch chosen + the locked ceiling values + the 4-doc subset list.
</output>
