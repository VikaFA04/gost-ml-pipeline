---
phase: 06-streamlit-ui-redesign
plan: 02
type: execute
wave: 2
depends_on:
  - 06-00
  - 06-01
files_modified:
  - app.py
autonomous: true
requirements:
  - REQ-ui-main-flow
  - REQ-input-preflight
  - REQ-pipeline-logging
user_setup: []
must_haves:
  truths:
    - "Sidebar renders the new D-01 control panel: profile picker (selectbox with key='profile_selectbox') + «+ Создать профиль из методички» button placeholder + DOCX uploader + «Запустить аудит» primary button."
    - "Old methodical sidebar form (app.py:803-1171 / functions build_methodical_profile_draft, _set_session_methodical_draft, _get_session_methodical_draft, _apply_methodical_form_edits, persist_custom_profile) is removed."
    - "run_processing wires `RunLog` for the 4 pipeline stages and uses `type(exc).__name__` for error_class with FIXED Russian strings for error_message — never `str(exc)`."
    - "Anti-pattern `st.exception(exc)` at app.py:773 is REMOVED and replaced by `st.error('Не удалось обработать документ: ' + type(exc).__name__)`."
    - "preflight_translate_error(exc) is a pure module-level function in app.py mapping FileNotFoundError / ValueError / BadZipFile / NotImplementedError / unknown to fixed Russian strings — no traceback or document text in the returned string."
    - "modal_reason_is_valid(reason) -> bool is a pure module-level one-liner in app.py."
    - "All 5 Wave-0 preflight tests + the 6 modal/contract tests turn GREEN. STATUS_CHIP tests stay RED (delivered in 06-03's render_report integration if needed, but STATUS_CHIP itself is delivered here)."
    - "Russian copy matches 06-UI-SPEC §Copywriting Contract verbatim."
  artifacts:
    - path: "app.py"
      provides: "redesigned sidebar (D-01) + preflight_translate_error + modal_reason_is_valid + STATUS_CHIP constant + RunLog-wired run_processing"
      contains: "preflight_translate_error"
  key_links:
    - from: "app.py run_processing"
      to: "src/inference/run_log.RunLog"
      via: "RunLog(uploaded_file.name) + run_log.record(...)"
      pattern: "RunLog\\(.*\\.name\\)"
    - from: "app.py main() sidebar"
      to: "st.session_state['profile_selectbox']"
      via: "st.selectbox(..., key='profile_selectbox')"
      pattern: "key=.profile_selectbox."
    - from: "app.py preflight"
      to: "src/inference/document_loader.validate_document_input"
      via: "preflight_translate_error catches its exceptions"
      pattern: "preflight_translate_error"
---

<objective>
Redesign the Streamlit sidebar per D-01 (slim config panel: profile picker + modal placeholder + uploader + run button), wire `RunLog` through `run_processing`, add `preflight_translate_error` + `modal_reason_is_valid` + `STATUS_CHIP` to module scope, and REMOVE the `st.exception(exc)` traceback leak at `app.py:773`. Delete the obsolete methodical sidebar form helpers (orphan code per CLAUDE.md «Удаляй orphans»).

Purpose: This wave closes REQ-input-preflight + REQ-pipeline-logging (Russian error surface + PII-clean run-log wiring) and lays the sidebar foundation for 06-03 main-pane + 06-04 modal. Main-pane render (render_report, render_block_section, summary counters) lands in 06-03; the modal lands in 06-04.

Output:
- `app.py` modified: new helpers `preflight_translate_error`, `modal_reason_is_valid`, `STATUS_CHIP` constant added; `run_processing` rewired with `RunLog`; sidebar block in `main()` slimmed to D-01; old methodical form helpers + render_hero deleted.
- `tests/test_preflight.py` 5 tests turn GREEN.
- `tests/test_run_log.py` stays GREEN (no regression).
- `tests/test_render_block_section.py` modal_reason_is_valid tests (5) + upload contract test turn GREEN; STATUS_CHIP tests (3) ALSO turn GREEN because STATUS_CHIP constant is added in this plan.
- `tests/test_app_ui.py` no-traceback assertion test turns GREEN; empty-state substring test turns GREEN as well (the interim main() block emits the empty-state copy). Smoke + import tests stay GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-streamlit-ui-redesign/06-CONTEXT.md
@.planning/phases/06-streamlit-ui-redesign/06-RESEARCH.md
@.planning/phases/06-streamlit-ui-redesign/06-PATTERNS.md
@.planning/phases/06-streamlit-ui-redesign/06-UI-SPEC.md
@.planning/phases/06-streamlit-ui-redesign/06-VALIDATION.md
@.planning/phases/06-streamlit-ui-redesign/06-01-SUMMARY.md
@app.py
@src/inference/run_log.py
@src/inference/document_loader.py
@src/inference/application_service.py

<interfaces>
Existing exports the executor consumes from src/inference/run_log.py (Wave 1):

- class RunLog with __init__(input_filename), .record(stage, status, error_class=None, error_message=None, **extras), .dump_json(path)

From src/inference/document_loader.py (unchanged):

- validate_document_input(path) raises: FileNotFoundError (missing path), ValueError ("Unsupported input format ..."), NotImplementedError (PDF input).

From src/inference/application_service.py (unchanged):

- process_document(...) may internally raise: ValueError("Only DOCX is currently supported..."), ValueError("The input DOCX does not contain any extractable non-empty blocks."), zipfile.BadZipFile via python-docx on malformed DOCX, plus any other Exception from rule engine / model inference.

Existing app.py exports kept verbatim (06-PATTERNS.md §"Imports pattern" + §"Deleted Code"):

- SUPPORTED_UPLOAD_TYPES, SUPPORTED_METHODICAL_UPLOAD_TYPES, CUSTOM_PROFILES_DIR constants (lines 19-21)
- inject_page_styles (26-155), normalize_table_values (208-215), format_profile_option (218-227), build_profile_options (230-236), render_artifact_download_card (458-478)
- run_processing (750-778) — MODIFY in place

Existing app.py functions to DELETE in this plan (06-PATTERNS.md §"Deleted Code" — orphans table):

- render_hero (158-175) — interim empty state replaces it in this plan's main() rewrite
- build_methodical_profile_draft (239-249)
- persist_custom_profile (252-261)
- _set_session_methodical_draft (264-267)
- _get_session_methodical_draft (270-272)
- _apply_methodical_form_edits (275-386)

Existing app.py functions to retain (06-03 and 06-05 handle their removal):

- filter_audit_df (389-425), filter_predictions_df (428-455), render_manual_decision_table (481-523), render_results (526-748), render_metric_card (178-189), render_status_badges (192-205). Keep until later plans remove.

Phase 6 NEW exports to add in this plan:

- STATUS_CHIP: dict[str, tuple[str, str, str]] — 5 status keys
- preflight_translate_error(exc: Exception) -> str
- modal_reason_is_valid(reason: str) -> bool

Russian copy strings (06-UI-SPEC §Copywriting Contract — copy VERBATIM):

- Sidebar header: «Панель управления»
- Profile picker label: «Профиль ГОСТ»
- Modal trigger: «+ Создать профиль из методички»
- DOCX uploader label: «Загрузите DOCX»
- Run button: «Запустить аудит»
- Empty state heading: «Загрузите DOCX-документ, чтобы начать аудит»
- Empty state body: «В левой панели выберите профиль ГОСТ и загрузите файл. После запуска аудита здесь появятся счётчики и блоки.»

Preflight error strings (06-UI-SPEC §Error state copy):

- FileNotFoundError / BadZipFile → «Файл не читается. Проверьте, что это валидный DOCX (.docx, ZIP-архив). Откройте файл в Word и пересохраните, если нужно.»
- ValueError "any extractable non-empty blocks" → «В документе нет извлекаемых непустых блоков. Проверьте, что документ содержит текст.»
- ValueError "Unsupported input format" or "Only DOCX is currently supported" → «Расширение файла `.docx`, но содержимое не соответствует DOCX-формату.»
- NotImplementedError (PDF) → «PDF аудит ещё не поддерживается в этой версии.»
- Unknown → «Не удалось обработать документ. См. журнал запуска.»
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add module-level helpers (STATUS_CHIP, preflight_translate_error, modal_reason_is_valid) and rewire run_processing with RunLog</name>
  <read_first>
    - File being modified: app.py (imports lines 1-25, helpers section 158-523, run_processing 750-778)
    - 06-PATTERNS.md §app.py "Imports pattern" lines 1-17 (current shape)
    - 06-PATTERNS.md §"STATUS_CHIP dict for render_block_section" (exact 5-row constant)
    - 06-PATTERNS.md §"Error Surfacing Pattern (D-04)" (st.exception → st.error rewrite)
    - 06-RESEARCH.md §Code Examples "Run-log integration in run_processing" (full revised skeleton)
    - 06-UI-SPEC.md §"Error state copy" (Russian strings)
    - 06-UI-SPEC.md §"Run-log JSON contract" (error_message MUST be fixed string, NEVER str(exc))
    - src/inference/document_loader.py (exception types raised by validate_document_input)
    - src/inference/application_service.py lines 165-171 (ValueError messages from process_document)
  </read_first>
  <action>
    Edit app.py in-place. Surgical edits only.

    Step A. Add three imports after the existing block ending at line 17:

    1. `from datetime import datetime, timezone`
    2. `from src.inference.run_log import RunLog`
    3. `import zipfile` (for BadZipFile in preflight_translate_error)

    Step B. Add the module-level STATUS_CHIP constant after the existing constants block (after CUSTOM_PROFILES_DIR at line 21):

    Define `STATUS_CHIP` as a dict literal with EXACTLY 5 keys in this order, each mapped to a 3-tuple of (icon, Russian-label, badge-css-class):

    - "no_change" -> ("●", "Без изменений", "badge-ok")
    - "changed" -> ("✏️", "Изменено", "badge-change")
    - "review" -> ("⚠️", "Требует проверки", "badge-warn")
    - "error" -> ("✗", "Ошибка", "badge-error")
    - "blocked_unsafe_autofix" -> ("🛑", "Небезопасное автоисправление заблокировано", "badge-muted")

    Add a 2-line comment above the constant referencing 06-PATTERNS.md §"CRITICAL FINDING: report_df schema" so the next executor knows blocked_unsafe_autofix is a boolean column in report_df, not a status string.

    Step C. Add two pure helper functions immediately after STATUS_CHIP:

    1. `modal_reason_is_valid(reason: str) -> bool` — one-liner: `return len(reason.strip()) >= 8`. Docstring references D-004 / T-05-01.

    2. `preflight_translate_error(exc: Exception) -> str` — branching on `isinstance(exc, ...)`:

       - `(FileNotFoundError, zipfile.BadZipFile)` -> the "Файл не читается..." string from interfaces above.
       - `NotImplementedError` -> "PDF аудит ещё не поддерживается в этой версии."
       - `ValueError` -> inspect `str(exc)`:
         - if `"extractable non-empty blocks"` in msg -> "В документе нет извлекаемых непустых блоков. Проверьте, что документ содержит текст."
         - elif `"Unsupported input format"` in msg or `"Only DOCX is currently supported"` in msg -> "Расширение файла `.docx`, но содержимое не соответствует DOCX-формату."
         - else -> "Не удалось обработать документ. См. журнал запуска."
       - fallback -> "Не удалось обработать документ. См. журнал запуска."

       PII boundary: the function returns ONLY the 5 fixed Russian strings — it must never return `str(exc)` (which may contain document text). String selection via `str(exc)` inspection is safe because the inspected strings are from `application_service.py` / `document_loader.py` and are fixed English source strings, never user content.

    Step D. Replace the body of `run_processing` (lines 750-778) with the new version. Keep the function signature unchanged. The new body must:

    1. Early-return on `uploaded_file is None` (existing st.warning kept) or `selected_model_key == "baseline_unavailable"` (existing st.error kept).
    2. Call `save_uploaded_bytes(uploaded_file.getvalue(), suffix=Path(uploaded_file.name).suffix)`.
    3. Instantiate `run_log = RunLog(uploaded_file.name)`.
    4. `run_log.record("document-read", "ok")` before the try block.
    5. Wrap `process_document(...)` call in try/except.
    6. `except (FileNotFoundError, NotImplementedError, ValueError, zipfile.BadZipFile) as exc:` — call `user_msg = preflight_translate_error(exc)`, then `run_log.record("document-read", "error", error_class=type(exc).__name__, error_message=user_msg)`, then `st.error(user_msg)`. Store run_log + uploaded_name in session_state before returning.
    7. `except Exception as exc:` — `run_log.record("rule-apply", "error", error_class=type(exc).__name__, error_message="Не удалось обработать документ.")` (FIXED Russian string; NEVER `str(exc)`). Then `st.error("Не удалось обработать документ: " + type(exc).__name__)`. Store run_log + uploaded_name in session_state before returning.
    8. On success: `run_log.record("classification", "ok")`, `run_log.record("rule-apply", "ok")`, `run_log.record("save", "ok")`. Store `last_result`, `last_uploaded_name`, `last_run_log` in `st.session_state`.

    REMOVE the previous `except NotImplementedError as exc: st.error(str(exc))` branch (now subsumed by the typed branch above).
    REMOVE the previous `except Exception as exc: st.exception(exc)` branch entirely — this is the D-04 mandatory fix.

    Per CLAUDE.md «минимум кода»: no class wrappers, no decorator helpers. Inline everything. Per CLAUDE.md «не «улучшай» соседний код»: do not refactor unrelated helpers in this task.
  </action>
  <verify>
    <automated>python -c "from app import STATUS_CHIP, modal_reason_is_valid, preflight_translate_error; assert set(STATUS_CHIP) == {'no_change','changed','review','error','blocked_unsafe_autofix'}; assert modal_reason_is_valid('abcdefgh') is True; assert modal_reason_is_valid('abc') is False; assert modal_reason_is_valid('   ') is False; assert 'не читается' in preflight_translate_error(FileNotFoundError()); print('OK')" 2>&1 | tee /tmp/06-02-task1-import.log && python -m pytest tests/test_preflight.py tests/test_run_log.py tests/test_render_block_section.py -x -q 2>&1 | tee /tmp/06-02-task1-pytest.log</automated>
  </verify>
  <acceptance_criteria>
    - grep -n "^STATUS_CHIP" app.py outputs exactly one line.
    - grep -c "badge-ok\|badge-change\|badge-warn\|badge-error\|badge-muted" app.py outputs at least 10 (5 in inject_page_styles CSS plus 5 in STATUS_CHIP).
    - grep -q "Без изменений" app.py and grep -q "Требует проверки" app.py and grep -q "Небезопасное автоисправление заблокировано" app.py.
    - grep -n "^def modal_reason_is_valid" app.py outputs exactly one line.
    - grep -n "^def preflight_translate_error" app.py outputs exactly one line.
    - grep -c "st.exception(exc)" app.py outputs 0 (regression check on D-04 fix).
    - grep -q "RunLog(uploaded_file.name)" app.py.
    - grep -c "run_log.record(" app.py outputs at least 6 (1 initial + 2 error branches + 3 success stages).
    - grep -E "error_message=str\(exc\)" app.py exits non-zero (PII boundary: never str(exc) for error_message).
    - grep -q "from src.inference.run_log import RunLog" app.py.
    - python -c "from app import STATUS_CHIP, modal_reason_is_valid, preflight_translate_error" exits 0.
    - python -m pytest tests/test_preflight.py -x -q reports 5 passed.
    - python -m pytest tests/test_run_log.py -x -q reports 7 passed.
    - python -m pytest tests/test_render_block_section.py -x -q reports 9 passed (5 modal_reason + 1 upload contract + 3 STATUS_CHIP all GREEN).
  </acceptance_criteria>
  <done>Module-level helpers + STATUS_CHIP constant + run_processing rewired with RunLog; st.exception(exc) leak removed; 5 preflight + 7 run-log + 9 render-block-section tests GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Redesign sidebar in main() per D-01; delete obsolete methodical helpers and render_hero</name>
  <read_first>
    - File being modified: app.py (helpers 158-386 and main() 780-1216)
    - 06-PATTERNS.md §"app.py — main() sidebar pattern" (current sidebar lines 800-1194)
    - 06-PATTERNS.md §"Session State Initialization" (keys to retain plus new last_run_log / modal_* keys)
    - 06-PATTERNS.md §"Profile Options Build Pattern" lines 795-796
    - 06-PATTERNS.md §"Deleted Code" orphans table
    - 06-UI-SPEC.md §"Copywriting Contract" (Russian button labels)
    - 06-UI-SPEC.md §"Color — Accent reserved for" (primary button reserved for «Запустить аудит» in sidebar; «+» button is secondary)
    - 06-RESEARCH.md §1 (st.dialog programmatic-close pattern uses key="profile_selectbox" — sidebar MUST use that key so the modal in 06-04 can write to it)
    - 06-CONTEXT.md §D-03 (modal trigger button next to profile dropdown)
  </read_first>
  <action>
    Edit app.py in two phases.

    Step A — Delete obsolete helpers (exact lines from 06-PATTERNS.md §"Deleted Code"):

    1. render_hero (currently 158-175) — entire function definition.
    2. build_methodical_profile_draft (currently 239-249) — entire function.
    3. persist_custom_profile (currently 252-261) — entire function.
    4. _set_session_methodical_draft (currently 264-267) — entire function.
    5. _get_session_methodical_draft (currently 270-272) — entire function.
    6. _apply_methodical_form_edits (currently 275-386) — entire function.

    After deletions, check unused imports with:
      python -c "import ast, sys; tree = ast.parse(open('app.py').read()); refs = {n.id for n in ast.walk(tree) if isinstance(n, ast.Name)}; print('build_methodical_profile' in refs, 'extract_text_from_file' in refs, 'save_methodical_profile' in refs, 'tempfile' in refs, 'json' in refs)"

    For any False values among build_methodical_profile / extract_text_from_file / save_methodical_profile, REMOVE that import name from line 17 of app.py (methodical_extractor import). Plan 06-04 will re-add the needed names when the modal arrives. For tempfile / json False values, REMOVE the top-level import. Per CLAUDE.md «Удаляй orphans».

    Keep ALL other helpers intact: inject_page_styles, render_metric_card, render_status_badges, normalize_table_values, format_profile_option, build_profile_options, filter_audit_df, filter_predictions_df, render_artifact_download_card, render_manual_decision_table, render_results. They are either kept for legacy compat or deleted by 06-03/06-05.

    Step B — Rewrite main() body (replacing the existing sidebar block lines ~800-1194 and the final render_results call at line 1212):

    Top of main():

    1. Call inject_page_styles() (existing — keep).
    2. profile_items = get_profile_options(); if empty -> st.error("Профили ГОСТ не найдены в src/rules/profiles.") and st.stop().
    3. Initialize session_state defaults: "custom_profile_items" = [], "last_run_log" = None, "modal_diff_lines" = None, "modal_draft_profile" = None. Use setdefault.
    4. Build all_profile_items and profile_label_to_path per 06-PATTERNS.md §"Profile Options Build Pattern".
    5. model_options = list_model_options().

    Sidebar block (with st.sidebar:):

    1. st.header("Панель управления")
    2. st.caption("Выберите профиль ГОСТ, загрузите DOCX-документ и запустите аудит.")
    3. selected_profile_label = st.selectbox("Профиль ГОСТ", options=list(profile_label_to_path.keys()), key="profile_selectbox") — the key MUST be "profile_selectbox" so the 06-04 modal can programmatically pre-select after save.
    4. open_modal_clicked = st.button("+ Создать профиль из методички", key="open_methodical_modal", use_container_width=True). On click, emit st.info("Модал создания профиля из методички будет доступен после плана 06-04.") — this is the placeholder; 06-04 replaces the body with the modal call.
    5. model_key = st.selectbox("Модель", options=list(model_options.keys()), format_func=lambda k: model_options.get(k, k), key="model_selectbox").
    6. mode_key = st.radio("Режим", options=["audit","fix"], format_func=lambda k: "Только аудит" if k == "audit" else "Применить безопасные исправления", key="mode_radio").
    7. uploaded_file = st.file_uploader("Загрузите DOCX", type=SUPPORTED_UPLOAD_TYPES, key="docx_uploader").
    8. run_disabled = uploaded_file is None or selected_profile_label is None
    9. run_clicked = st.button("Запустить аудит", type="primary", disabled=run_disabled, use_container_width=True, key="run_audit_button").

    After the sidebar block:

    1. If run_clicked and not run_disabled: selected_profile_path = profile_label_to_path[selected_profile_label]; with st.spinner("Идёт аудит документа..."): call run_processing(...) with kwargs uploaded_file, selected_model_key=model_key, selected_mode=mode_key, selected_profile_path=selected_profile_path.

    2. Read result = st.session_state.get("last_result").

    3. If result is None: emit empty state — st.info("Загрузите DOCX-документ, чтобы начать аудит") then st.caption("В левой панели выберите профиль ГОСТ и загрузите файл. После запуска аудита здесь появятся счётчики и блоки.").

    4. Else (result present): call render_results(result) — interim; 06-03 replaces with render_report.

    Bottom of file: keep the existing if __name__ == "__main__": main() guard.

    Russian copy verbatim from 06-UI-SPEC.md §Copywriting Contract.

    Per CLAUDE.md «минимум кода»: no helper functions for sidebar; everything inline. Per CLAUDE.md «не рефактори то, что работает»: do not touch render_results body, inject_page_styles CSS, render_metric_card.

    Note on model + mode selectors: 06-UI-SPEC.md does not explicitly enumerate these in the sidebar but run_processing needs them. They are kept as plain selects below the profile picker — discretion under 06-CONTEXT.md §"Claude's Discretion" (Russian copy details). Record this choice in 06-02-SUMMARY.md.
  </action>
  <verify>
    <automated>python -c "import app; assert app.SUPPORTED_UPLOAD_TYPES == ['docx']; assert callable(app.main); assert callable(app.modal_reason_is_valid); assert callable(app.preflight_translate_error); assert hasattr(app, 'STATUS_CHIP'); assert not hasattr(app, 'render_hero'); assert not hasattr(app, 'build_methodical_profile_draft'); assert not hasattr(app, '_apply_methodical_form_edits'); print('OK')" 2>&1 | tee /tmp/06-02-task2-import.log && python -m pytest tests/test_app_upload_contract.py tests/test_run_log.py tests/test_preflight.py tests/test_render_block_section.py -x -q 2>&1 | tee /tmp/06-02-task2-pytest.log && python -m pytest tests/test_app_ui.py -x -q 2>&1 | tee /tmp/06-02-task2-apptest.log</automated>
  </verify>
  <acceptance_criteria>
    - grep -E "^def render_hero\b" app.py exits non-zero (function deleted).
    - grep -E "^def build_methodical_profile_draft\b" app.py exits non-zero (deleted).
    - grep -E "^def _apply_methodical_form_edits\b" app.py exits non-zero (deleted).
    - grep -E "^def persist_custom_profile\b" app.py exits non-zero (deleted).
    - grep -E "^def _set_session_methodical_draft\b" app.py exits non-zero (deleted).
    - grep -E "^def _get_session_methodical_draft\b" app.py exits non-zero (deleted).
    - grep -q 'key="profile_selectbox"' app.py (modal-close target key).
    - grep -q 'key="docx_uploader"' app.py.
    - grep -q 'key="run_audit_button"' app.py.
    - grep -q '"Запустить аудит"' app.py and grep -q '"+ Создать профиль из методички"' app.py and grep -q '"Профиль ГОСТ"' app.py and grep -q '"Панель управления"' app.py.
    - grep -q '"Загрузите DOCX-документ, чтобы начать аудит"' app.py (interim empty-state copy).
    - grep -q '"В левой панели выберите профиль ГОСТ"' app.py.
    - grep -c 'type="primary"' app.py outputs 1 (only the run-audit button is primary — accent reserved per UI-SPEC §Color).
    - wc -l app.py outputs a number between 700 and 950 (was 1216; net 250-500 deletion as estimated in 06-RESEARCH.md §2).
    - python -c "import app; ast = __import__('ast'); funcs = [n.name for n in ast.walk(ast.parse(open('app.py').read())) if isinstance(n, ast.FunctionDef)]; print(funcs)" lists modal_reason_is_valid, preflight_translate_error, run_processing, main — but NOT render_hero, build_methodical_profile_draft, _apply_methodical_form_edits.
    - python -m pytest tests/test_app_upload_contract.py -x -q reports 2 passed.
    - python -m pytest tests/test_run_log.py -x -q reports 7 passed.
    - python -m pytest tests/test_preflight.py -x -q reports 5 passed.
    - python -m pytest tests/test_render_block_section.py -x -q reports 9 passed.
    - python -m pytest tests/test_app_ui.py -x -q reports 4 passed (smoke + empty-state substring + no-Traceback all GREEN). If skipped due to system Python no-streamlit, that is acceptable per OQ-3.
  </acceptance_criteria>
  <done>Sidebar redesigned to D-01 shape with correct key="profile_selectbox" anchor for 06-04 modal; 6 obsolete helpers deleted; interim empty state copy emitted; total app.py LoC reduced ~250-500; all Wave-0/Wave-1 tests GREEN; legacy render_results still wired for non-empty result rendering (deferred replacement to 06-03).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Streamlit UI → run-log JSON | Exception messages cross from caught exceptions into RunLog records — must be filtered through preflight_translate_error or fixed Russian string |
| Streamlit UI → end user (rendered) | st.error / st.warning / st.info text rendered to browser — must be Russian, no traceback, no document text |
| User input → file uploader | DOCX bytes from browser uploaded; size and MIME bounded by Streamlit + existing validate_document_input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-6-01 | Information disclosure (HIGH) | run_processing exception path | mitigate | All except-branches use `error_class=type(exc).__name__` (bare class name) and `error_message=<fixed Russian string>` — never `str(exc)`. Acceptance criterion `grep -E "error_message=str\(exc\)" app.py` exits non-zero proves the boundary. See preflight_translate_error implementation which returns ONLY 5 fixed Russian strings. |
| T-6-02 | Information disclosure (MEDIUM) | st.exception(exc) at app.py:773 leaks traceback | mitigate | `st.exception(exc)` is REMOVED and replaced by `st.error("Не удалось обработать документ: " + type(exc).__name__)` per 06-UI-SPEC §"Error state copy" final note. Acceptance criterion `grep -c "st.exception(exc)" app.py` outputs 0. Test `tests/test_app_ui.py::test_app_no_traceback_in_rendered_output` asserts the absence at runtime. |
| T-6-04 | Denial of service (LOW) | st.file_uploader DOCX upload | mitigate | Streamlit's default maxUploadSize=200MB caps the upload; backend validate_document_input + python-docx fail fast on invalid ZIP. Soft size cap (warn at 50MB) is a UI-SPEC discretion item and is NOT implemented in 06-02 — deferred to 06-05 cleanup if needed. Accepted residual: very large valid DOCX may stall the spinner; user gets the run-log download to investigate. |
</threat_model>

<verification>
- All Wave-0 RED tests for preflight (5), run-log (7), render-block-section (9), upload-contract (2), app-ui (4) pass: total 27 tests.
- `git diff --stat app.py` shows substantial deletion (~400-600 lines) and ~150-250 line addition.
- `python -m pytest tests/ -x -q --ignore=tests/test_methodical_profile_editor.py` passes (no regression in pre-existing suite).
- `streamlit run app.py` launches without exception (manual smoke). Empty state is visible. Uploading an empty file shows the Russian preflight error without traceback.
</verification>

<success_criteria>
- 27 of Phase 6 Wave-0 tests pass (5 preflight + 7 run-log + 9 render-block-section + 2 upload-contract + 4 app-ui).
- `app.py` line count between 700-950.
- 6 obsolete helper functions deleted; no orphan references remain (verified by AST scan).
- `st.exception(exc)` leak removed; D-04 PII boundary holds in run-log records.
- Sidebar uses `key="profile_selectbox"` so the 06-04 modal can complete the D-03 step-5 auto-select pattern.
- Russian copy matches 06-UI-SPEC §Copywriting Contract verbatim.
</success_criteria>

<output>
After completion, create `.planning/phases/06-streamlit-ui-redesign/06-02-SUMMARY.md` capturing:
- Final app.py line count (before/after).
- Deleted function inventory (the 6 orphans).
- Imports removed from app.py header (methodical_extractor names + tempfile/json if unused).
- Russian copy strings emitted (with file:line anchors).
- Mode + model picker placement rationale (06-CONTEXT.md §"Claude's Discretion" item — Russian copy details).
- Test counts: 27 GREEN tests this plan enables.
- Note for 06-03 executor: render_results is still wired at the end of main() for the result-present branch; 06-03 replaces that call with render_report + render_block_section + summary counters + run-log download.
</output>
