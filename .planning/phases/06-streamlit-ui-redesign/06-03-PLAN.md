---
phase: 06-streamlit-ui-redesign
plan: 03
type: execute
wave: 3
depends_on:
  - 06-02
files_modified:
  - app.py
autonomous: true
requirements:
  - REQ-ui-main-flow
  - REQ-ui-problem-block-view
  - REQ-pipeline-logging
user_setup: []
must_haves:
  truths:
    - "Main pane renders: report header («Отчёт по документу: {filename}» + «Профиль: {profile_name} ({profile_id})»), 6-cell st.metric strip (total / no_change / changed / review / error / blocked), three grouped sections («Требуют внимания» expanded, «Изменены» collapsed, «Без изменений» collapsed), downloads section."
    - "render_block_section is a module-level function that loops `df.iterrows()` with one `st.expander` per row using the STATUS_CHIP icon+label+badge mapping."
    - "Group-split logic uses report_df['status'].isin(['review','error']) | (report_df['blocked_unsafe_autofix'] == True) for «Требуют внимания»; status == 'changed' & ~blocked for «Изменены»; status == 'no_change' for «Без изменений». Matches the verified report_df schema in 06-PATTERNS.md §CRITICAL FINDING."
    - "Expander body shows: «Оригинальный текст блока» (st.code), «Причина ручной проверки» (st.write explanation), «Нарушенные правила», «Применённые исправления», «Заблокированное автоисправление» (only if blocked_unsafe_autofix == True), «Сообщение об ошибке» (only if status == error)."
    - "Run-log download button «Скачать журнал запуска (JSON)» appears at the bottom of the downloads section, naming the file `{stem}_run_log_{timestamp}.json`."
    - "Legacy render_results function is REMOVED from app.py (main() now calls render_report)."
    - "Legacy render_metric_card, render_status_badges, filter_audit_df, filter_predictions_df, render_manual_decision_table become orphans; delete them (CLAUDE.md «Удаляй orphans»)."
    - "All Wave-0 tests stay GREEN; AppTest smoke test sees the 6 metric cells, 3 group headers, and the run-log download button on the result-present path."
  artifacts:
    - path: "app.py"
      provides: "render_report + render_block_section + render_summary_counters; legacy render_results removed"
      contains: "def render_report\\|def render_block_section\\|def render_summary_counters"
  key_links:
    - from: "app.py render_report"
      to: "ProcessingArtifacts.report_df"
      via: "render_block_section(title, df, expanded_by_default) loops rows"
      pattern: "render_block_section"
    - from: "app.py render_report downloads"
      to: "st.session_state['last_run_log']"
      via: "run_log.dump_json + st.download_button"
      pattern: "run_log.dump_json"
---

<objective>
Replace the legacy 5-tab `render_results` function (app.py:526-748) with a new linear main-pane composition: `render_report` orchestrator + `render_summary_counters` (6-cell st.metric strip) + `render_block_section` (per-row st.expander loop in 3 grouped sections) + downloads section including the run-log JSON download button.

Purpose: This wave closes REQ-ui-main-flow (linear upload → profile → run → summary → table → download) and REQ-ui-problem-block-view (5-status chips, per-block confidence, original text, manual-review reason from explanation). The 5-tab structure is dropped per D-01.

Output:
- `app.py`: 3 new module-level functions (`render_summary_counters`, `render_block_section`, `render_report`) + main() rewired to call render_report instead of render_results.
- Legacy orphans deleted: `render_results` (526-748), `render_metric_card` (178-189), `render_status_badges` (192-205), `filter_audit_df` (389-425), `filter_predictions_df` (428-455), `render_manual_decision_table` (481-523).
- `tests/test_app_ui.py` smoke + empty-state + no-traceback tests stay GREEN; a new contract test asserting the 6-cell metric layout could be added but the existing AppTest smoke is sufficient.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-streamlit-ui-redesign/06-CONTEXT.md
@.planning/phases/06-streamlit-ui-redesign/06-RESEARCH.md
@.planning/phases/06-streamlit-ui-redesign/06-PATTERNS.md
@.planning/phases/06-streamlit-ui-redesign/06-UI-SPEC.md
@.planning/phases/06-streamlit-ui-redesign/06-02-SUMMARY.md
@app.py
@src/inference/application_service.py
@src/inference/run_log.py

<interfaces>
ProcessingArtifacts shape from src/inference/application_service.py (lines 37-54):

- model_type, mode, profile_path, input_path, extracted_csv, predictions_csv, report_csv, report_json, summary_json, summary_txt, output_docx (Path | None), predictions_df, report_df, summary (dict).

report_df column schema (verified from real run, 06-PATTERNS.md §"CRITICAL FINDING"):

- block_id, kind, label, status, action, profile_id, profile_name, confidence_score, low_confidence, manual_review_required, blocked_unsafe_autofix (bool), unsafe_auto_fix_reason, violated_rules, suggested_rule_ids, applied_fixes, suggested_fix, changed_fields, uncertain_fields, reason, explanation, recommendation, text.

status column values in real data: {'review', 'no_change', 'changed'} — NO 'error' or 'blocked_unsafe_autofix' string. blocked_unsafe_autofix is a separate boolean column.

summary dict keys (from application_service._render_summary_text lines 90-106):

- profile_name, blocks_total, no_change, review, changed, error, blocked_unsafe_autofix, output_docx, model_type, mode.

Streamlit primitives used (06-UI-SPEC §"Component library"):

- st.columns, st.metric, st.subheader, st.expander, st.code, st.write, st.markdown, st.download_button, st.caption, st.info.

STATUS_CHIP constant added by 06-02 — reuse for chip rendering.

RunLog from src/inference/run_log.py — already in st.session_state["last_run_log"] after a run. Call run_log.dump_json(path) to materialize.

Existing reusable helpers (KEEP verbatim):

- inject_page_styles (lines 26-155) — the .badge-* CSS classes
- normalize_table_values (lines 208-215)
- render_artifact_download_card (lines 458-478)
- format_profile_option, build_profile_options (218-236)

Russian copy strings (06-UI-SPEC §Copywriting Contract — VERBATIM):

- Report header: «Отчёт по документу: {filename}» followed by «Профиль: {profile_name} ({profile_id})»
- Section: «Требуют внимания»
- Section: «Изменены»
- Section: «Без изменений»
- Downloads section header: «Скачать результаты»
- Run-log download button: «Скачать журнал запуска (JSON)»
- Run-log file name: `{stem}_run_log_{timestamp}.json`
- Empty-attention message: «Документ соответствует профилю — блоков, требующих внимания, нет.»
- Expander body labels: «Оригинальный текст блока», «Причина ручной проверки», «Нарушенные правила», «Применённые исправления», «Заблокированное автоисправление», «Сообщение об ошибке», «Класс ошибки» (caption prefix).
- Summary counter labels: «Всего блоков», «Без изменений», «Изменены», «Требуют проверки», «Ошибки», «Небезопасно (заблокировано)».

Expander header format (06-RESEARCH.md §Pattern 2):

  {icon} {block_id} · {label} · {Russian status label} · уверенность {confidence:.2f}
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add render_summary_counters and render_block_section helpers</name>
  <read_first>
    - File being modified: app.py (insert new helpers above main(); current insertion site after STATUS_CHIP constant)
    - 06-PATTERNS.md §"render_block_section group-split logic" — exact pandas filter expressions using the verified column schema
    - 06-PATTERNS.md §"STATUS_CHIP dict" — already added in 06-02; reuse
    - 06-RESEARCH.md §Pattern 2 — full render_block_section reference implementation (chip rendering, expander body fields)
    - 06-UI-SPEC.md §"Block table widget choice" — per-row st.expander loop with fallback budget for >150 rows
    - 06-UI-SPEC.md §"Summary counters" — st.columns(6) of st.metric strip
    - 06-UI-SPEC.md §"Section headings" — exact Russian section titles and expand/collapse defaults
    - src/inference/application_service.py lines 37-54 — ProcessingArtifacts shape
  </read_first>
  <action>
    Edit app.py — add two new module-level functions after STATUS_CHIP constant and the modal_reason_is_valid / preflight_translate_error helpers (around the location of the deleted render_hero / render_metric_card functions; choose a position before main()).

    Step A — Add `render_summary_counters(summary: dict) -> None`:

    Body:

      cols = st.columns(6)
      metrics = [
          ("Всего блоков",                int(summary.get("blocks_total", 0) or 0)),
          ("Без изменений",               int(summary.get("no_change", 0) or 0)),
          ("Изменены",                    int(summary.get("changed", 0) or 0)),
          ("Требуют проверки",            int(summary.get("review", 0) or 0)),
          ("Ошибки",                      int(summary.get("error", 0) or 0)),
          ("Небезопасно (заблокировано)", int(summary.get("blocked_unsafe_autofix", 0) or 0)),
      ]
      for col, (label, value) in zip(cols, metrics):
          col.metric(label, value)

    Step B — Add `render_block_section(title: str, df: pd.DataFrame, expanded_by_default: bool) -> None`:

    Body:

    1. If df.empty: return immediately. (For «Требуют внимания» the caller may emit a friendly empty-state message — see render_report.)
    2. st.subheader(f"{title} ({len(df)})")
    3. Iterate `for _, row in df.iterrows():`. For each row:
       - status_value = the row's "status" value if its blocked_unsafe_autofix column is False; otherwise the literal "blocked_unsafe_autofix" (because the chip display uses the STATUS_CHIP key, but the row may not have an "error" status — see notes).
       - Determine chip via:
           if bool(row.get("blocked_unsafe_autofix", False)) is True:
               icon, label, _css = STATUS_CHIP["blocked_unsafe_autofix"]
           else:
               key = str(row.get("status", ""))
               icon, label, _css = STATUS_CHIP.get(key, ("?", key or "—", "badge-neutral"))
       - confidence formatting: conf = row.get("confidence_score"); conf_str = f"{float(conf):.2f}" if conf not in (None, "", float("nan")) and not pd.isna(conf) else "—".
       - header = f"{icon} {row.get('block_id', '?')} · {row.get('label', '')} · {label} · уверенность {conf_str}".
       - Open `with st.expander(header, expanded=expanded_by_default):` and emit body fields per 06-RESEARCH.md §Pattern 2:

         (a) If row.get("text"): st.markdown("**Оригинальный текст блока**"); st.code(str(row["text"]), language=None).
         (b) If row.get("explanation"): st.markdown("**Причина ручной проверки**"); st.write(str(row["explanation"])).
         (c) If row.get("violated_rules"): st.markdown("**Нарушенные правила**"); st.write(str(row["violated_rules"])).
         (d) If row.get("applied_fixes"): st.markdown("**Применённые исправления**"); st.write(str(row["applied_fixes"])).
         (e) If bool(row.get("blocked_unsafe_autofix", False)) and row.get("unsafe_auto_fix_reason"): st.markdown("**Заблокированное автоисправление**"); st.write(str(row["unsafe_auto_fix_reason"])).
         (f) If str(row.get("status", "")) == "error": st.markdown("**Сообщение об ошибке**"); st.write(str(row.get("explanation", "Внутренняя ошибка правила. См. журнал запуска."))).

    For the «empty value» guard, use a tolerant check: a value is considered empty when it is None, NaN (pd.isna), an empty string, or the literal string "nan". The normalize_table_values helper already replaces NaN with empty string in upstream usage; rely on `str(value).strip()` non-empty as the gate for emitting each optional section. Implement once via:

      def _has(value) -> bool:
          if value is None:
              return False
          try:
              if pd.isna(value):
                  return False
          except (TypeError, ValueError):
              pass
          s = str(value).strip()
          return bool(s) and s.lower() != "nan"

    Define this `_has` as a nested helper inside render_block_section (or as a module-level private function — executor's choice, but must not be exported).

    Step C — Per CLAUDE.md «минимум кода»: no class wrappers, no Streamlit-elements abstractions. Reuse the existing STATUS_CHIP constant + .badge-* CSS that 06-02 wired.

    Step D — Per CLAUDE.md «не рефактори»: do NOT modify inject_page_styles CSS, do NOT add new badge classes. The 5 .badge-* classes (badge-ok / badge-change / badge-warn / badge-error / badge-muted) cover the 5 STATUS_CHIP entries. The chip icon is the first character of the expander header; the badge CSS class is currently only referenced indirectly via STATUS_CHIP and not used in this Streamlit render (Streamlit's st.expander does not accept HTML classes natively). If the executor wants visual chip styling, st.markdown(unsafe_allow_html=True) is acceptable inside the expander BODY only — discretion item; UI-SPEC §"Block table widget choice" accepts the icon+text chip in the expander label without HTML.
  </action>
  <verify>
    <automated>python -c "import app, pandas as pd; df = pd.DataFrame([{'block_id':1,'label':'body_text','status':'review','confidence_score':0.7,'blocked_unsafe_autofix':False,'text':'sample','explanation':'low conf','violated_rules':'','applied_fixes':''},{'block_id':2,'label':'body_text','status':'changed','confidence_score':0.9,'blocked_unsafe_autofix':False,'text':'sample','explanation':'','violated_rules':'','applied_fixes':'space_after'}]); assert callable(app.render_block_section); assert callable(app.render_summary_counters); print('OK')" 2>&1 | tee /tmp/06-03-task1-import.log && python -m pytest tests/test_render_block_section.py tests/test_run_log.py tests/test_preflight.py -x -q 2>&1 | tee /tmp/06-03-task1-pytest.log</automated>
  </verify>
  <acceptance_criteria>
    - grep -n "^def render_summary_counters" app.py outputs exactly one line.
    - grep -n "^def render_block_section" app.py outputs exactly one line.
    - grep -q '"Всего блоков"' app.py and grep -q '"Небезопасно (заблокировано)"' app.py.
    - grep -q '"Оригинальный текст блока"' app.py and grep -q '"Причина ручной проверки"' app.py and grep -q '"Нарушенные правила"' app.py and grep -q '"Применённые исправления"' app.py and grep -q '"Заблокированное автоисправление"' app.py and grep -q '"Сообщение об ошибке"' app.py.
    - grep -q "уверенность" app.py (confidence_score formatting label in expander header).
    - grep -q "STATUS_CHIP\[" app.py (constant referenced for chip lookup).
    - grep -c "st.expander(" app.py outputs at least 1 (in render_block_section).
    - grep -c "st.metric(" app.py outputs at least 1 (in render_summary_counters).
    - grep -c "st.columns(6)" app.py outputs at least 1.
    - python -c "import app; assert callable(app.render_summary_counters); assert callable(app.render_block_section)" exits 0.
    - python -m pytest tests/test_render_block_section.py -x -q reports 9 passed (no regression).
    - python -m pytest tests/test_run_log.py tests/test_preflight.py -x -q reports 12 passed (no regression).
  </acceptance_criteria>
  <done>render_summary_counters + render_block_section module-level functions exist with the exact Russian labels from 06-UI-SPEC, use STATUS_CHIP for chip mapping, handle blocked_unsafe_autofix boolean correctly, and pass smoke import + no test regressions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add render_report orchestrator with downloads + run-log; wire main() to call render_report; delete render_results and other orphans</name>
  <read_first>
    - File being modified: app.py (main() result-present branch + delete render_results 526-748 and render_metric_card 178-189 and render_status_badges 192-205 and filter_audit_df 389-425 and filter_predictions_df 428-455 and render_manual_decision_table 481-523)
    - 06-RESEARCH.md §Code Examples "Download run-log in render_report" — full pattern for run-log download button
    - 06-UI-SPEC.md §"Download filename scheme" — `{stem}_run_log_{timestamp}.json` under REPORTS_DIR
    - 06-UI-SPEC.md §"Section headings" — section order and Russian labels
    - 06-PATTERNS.md §"render_block_section group-split logic" — pandas group splits using verified report_df schema
    - 06-PATTERNS.md §"Download Button Pattern" — st.download_button skeleton
    - src/inference/application_service.py — REPORTS_DIR constant (Path("results/reports")) and _timestamp() helper (lines 86-87)
  </read_first>
  <action>
    Edit app.py.

    Step A — Add `render_report(result: ProcessingArtifacts) -> None` as a module-level function (place after render_block_section, before main()):

    Body sequence (per 06-UI-SPEC §"Section headings" 1-6 and §"Run-log JSON contract"):

    1. Report header:
       - filename = result.input_path.name
       - profile_name = result.summary.get("profile_name", "—")
       - profile_id = result.summary.get("profile_id") or Path(result.profile_path).stem
       - st.subheader(f"Отчёт по документу: {filename}")
       - st.caption(f"Профиль: {profile_name} ({profile_id})")

    2. Summary counters: render_summary_counters(result.summary).

    3. Group-split the report_df (normalize NaN first via `df = normalize_table_values(result.report_df)`):
       - attention_mask = df["status"].isin(["review", "error"]) | (df["blocked_unsafe_autofix"].astype(bool))
       - df_attention = df[attention_mask]
       - df_changed = df[(df["status"] == "changed") & (~df["blocked_unsafe_autofix"].astype(bool))]
       - df_ok = df[df["status"] == "no_change"]

       Guard against missing "blocked_unsafe_autofix" column: if the column is absent (older corpus snapshot), treat it as all-False — `if "blocked_unsafe_autofix" not in df.columns: df = df.assign(blocked_unsafe_autofix=False)`.

    4. Render groups in order:
       - If df_attention.empty: st.subheader("Требуют внимания (0)"); st.info("Документ соответствует профилю — блоков, требующих внимания, нет.")
       - Else: render_block_section("Требуют внимания", df_attention, expanded_by_default=True)
       - render_block_section("Изменены", df_changed, expanded_by_default=False)
       - render_block_section("Без изменений", df_ok, expanded_by_default=False)

    5. Downloads section: st.subheader("Скачать результаты"). Reuse render_artifact_download_card for: report_csv (label «Отчёт CSV»), summary_json (label «Сводка JSON»), and output_docx if result.output_docx is not None (label «Исправленный DOCX»). The exact card titles + descriptions are executor's discretion (06-CONTEXT.md §"Claude's Discretion") — keep concise Russian.

    6. Run-log download:
       - run_log = st.session_state.get("last_run_log")
       - if run_log is None: skip (early-return / no-op).
       - else:
         - from src.inference.application_service import REPORTS_DIR
         - timestamp = datetime.now().strftime("%Y%m%d_%H%M%S")
         - stem = Path(result.input_path).stem
         - log_path = REPORTS_DIR / f"{stem}_run_log_{timestamp}.json"
         - run_log.dump_json(log_path)
         - st.download_button("Скачать журнал запуска (JSON)", data=log_path.read_bytes(), file_name=log_path.name, mime="application/json", use_container_width=True, key="download_run_log")

    Step B — Rewire main(): replace the previous `render_results(result)` call (at the result-not-None branch added in 06-02) with `render_report(result)`. The empty-state branch (result is None) stays as-is.

    Step C — Delete orphan functions (per CLAUDE.md «Удаляй orphans, появившиеся из-за твоих изменений»):

    1. render_results (526-748) — entire function and its calls.
    2. render_metric_card (178-189) — entire function. No remaining call sites after render_results is deleted.
    3. render_status_badges (192-205) — entire function. No remaining call sites after render_results is deleted.
    4. filter_audit_df (389-425) — entire function. No remaining call sites.
    5. filter_predictions_df (428-455) — entire function. No remaining call sites.
    6. render_manual_decision_table (481-523) — entire function. No remaining call sites.

    After deletions, run AST + grep checks to verify no name references remain to any of the 6 deleted functions. If any reference remains in any retained function (e.g., main() still calling render_results), fix that reference too — the deletion is incomplete otherwise.

    Step D — Import audit: after deletions, the following imports may become unused:

    - `from src.inference.run_log import RunLog` — still used by run_processing; KEEP.
    - `import zipfile` — still used by run_processing + preflight_translate_error; KEEP.
    - any other names imported solely for deleted functions (e.g., if normalize_table_values was only used by render_results, it is now used by render_report — KEEP).

    Run `python -c "import ast; tree = ast.parse(open('app.py').read()); names = {n.id for n in ast.walk(tree) if isinstance(n, ast.Name)}; from itertools import chain; print(sorted(names))"` and visually verify no dangling imports.

    Step E — Per CLAUDE.md «минимум кода»: no extra abstractions over render_block_section / render_summary_counters. render_report is a straight-line orchestrator.

    Step F — Per CLAUDE.md «не рефактори то, что работает»: keep inject_page_styles CSS, render_artifact_download_card, normalize_table_values, format_profile_option, build_profile_options.
  </action>
  <verify>
    <automated>python -c "import app; assert callable(app.render_report); assert not hasattr(app, 'render_results'); assert not hasattr(app, 'render_metric_card'); assert not hasattr(app, 'render_status_badges'); assert not hasattr(app, 'filter_audit_df'); assert not hasattr(app, 'filter_predictions_df'); assert not hasattr(app, 'render_manual_decision_table'); print('OK')" 2>&1 | tee /tmp/06-03-task2-import.log && python -m pytest tests/test_app_upload_contract.py tests/test_run_log.py tests/test_preflight.py tests/test_render_block_section.py tests/test_app_ui.py -x -q 2>&1 | tee /tmp/06-03-task2-pytest.log</automated>
  </verify>
  <acceptance_criteria>
    - grep -n "^def render_report" app.py outputs exactly one line.
    - grep -E "^def render_results\b" app.py exits non-zero (deleted).
    - grep -E "^def render_metric_card\b" app.py exits non-zero (deleted).
    - grep -E "^def render_status_badges\b" app.py exits non-zero (deleted).
    - grep -E "^def filter_audit_df\b" app.py exits non-zero (deleted).
    - grep -E "^def filter_predictions_df\b" app.py exits non-zero (deleted).
    - grep -E "^def render_manual_decision_table\b" app.py exits non-zero (deleted).
    - grep -c "render_report(result)" app.py outputs 1 (main() call site).
    - grep -c "render_results" app.py outputs 0 (no remaining references).
    - grep -q '"Отчёт по документу: "' app.py or grep -q "Отчёт по документу" app.py.
    - grep -q '"Профиль: "' app.py or grep -q "Профиль:" app.py.
    - grep -q '"Скачать результаты"' app.py.
    - grep -q '"Скачать журнал запуска' app.py.
    - grep -q "run_log.dump_json" app.py.
    - grep -q "_run_log_" app.py (file naming scheme {stem}_run_log_{timestamp}.json).
    - grep -q "normalize_table_values" app.py (still used in render_report).
    - wc -l app.py outputs a number between 500 and 750 (was ~800-950 after 06-02; further reduction from the 6 orphan deletions).
    - python -c "import app" exits 0.
    - python -m pytest tests/test_app_upload_contract.py -x -q reports 2 passed.
    - python -m pytest tests/test_run_log.py -x -q reports 7 passed.
    - python -m pytest tests/test_preflight.py -x -q reports 5 passed.
    - python -m pytest tests/test_render_block_section.py -x -q reports 9 passed.
    - python -m pytest tests/test_app_ui.py -x -q reports 4 passed (or skipped per OQ-3).
  </acceptance_criteria>
  <done>render_report orchestrates the new main-pane composition; render_results and 5 other orphan helpers deleted; main() calls render_report on the result-present branch; run-log download button materializes and serves the JSON; all 27 Phase 6 Wave-0 tests + the pre-existing test suite stay GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| report_df → main-pane rendering | DataFrame row text fields cross into Streamlit; rendered with st.code (no HTML interpretation) — safe by primitive choice |
| RunLog → downloadable JSON | run_log.dump_json writes to disk under REPORTS_DIR; the path is composed from `{stem}_run_log_{timestamp}.json` where stem is Path(input_path).stem — basename-safe |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-6-01 | Information disclosure (HIGH continued from 06-01/06-02) | render_report run-log download | mitigate | The downloaded file is the JSON produced by `RunLog.dump_json`. Wave-1 tests assert no document text leaks. This task does not introduce new write paths — it serves the already-PII-clean JSON. Verified by re-running tests/test_run_log.py. |
| T-6-05 | Information disclosure (MEDIUM) | st.code rendering of row.text in expander | accept | Original block text is intentionally surfaced in the expander body per REQ-ui-problem-block-view UI-03 «original block text always inspectable». This is by design — the UI exposes document content TO THE USER who uploaded it (local-only Streamlit, no auth required per D-005 + C-UI-CONTRACT). The text is NEVER routed into the run-log JSON. The boundary is: in-UI display = allowed; on-disk run-log = forbidden. |
| T-6-06 | Tampering (LOW) | run-log file path uses stem from input_path | mitigate | Path(input_path).stem strips any directory traversal attempts in the filename. REPORTS_DIR is fixed at `results/reports/`. The composed path cannot escape the results tree. |
</threat_model>

<verification>
- python -m pytest tests/ -x -q --ignore=tests/test_methodical_profile_editor.py passes (all Wave-0 Phase 6 tests + pre-existing suite GREEN).
- streamlit run app.py launches; uploading a real DOCX produces the new main-pane composition with 6 metric cells, three group sections (attention expanded, others collapsed), downloads section including the run-log JSON button.
- Opening the downloaded run-log JSON in a text editor shows {stage, ts, status, error_class, error_message} keys, no `text`/`paragraph`/`Traceback` substring.
- AppTest smoke test renders 6 st.metric widgets when result is in session_state (manual fixture setup).
</verification>

<success_criteria>
- 3 new module-level functions exist: render_summary_counters, render_block_section, render_report.
- 6 legacy functions deleted: render_results, render_metric_card, render_status_badges, filter_audit_df, filter_predictions_df, render_manual_decision_table.
- main() calls render_report on the result-present branch; empty-state branch unchanged.
- All Phase 6 Wave-0 tests stay GREEN; pre-existing test suite stays GREEN.
- app.py LoC reduced to 500-750.
- D-02 grouped-section UI shipped with «Требуют внимания» first and expanded, «Изменены» and «Без изменений» collapsed.
- Run-log download artifact appears in main-pane downloads section, named `{stem}_run_log_{timestamp}.json`.
</success_criteria>

<output>
After completion, create `.planning/phases/06-streamlit-ui-redesign/06-03-SUMMARY.md` capturing:
- New function inventory with signatures.
- Deleted orphan inventory (6 functions).
- app.py LoC before/after.
- Confirmed test counts: 27 Phase 6 tests + N pre-existing tests all GREEN.
- One-line note for 06-04 executor: methodical modal goes between the open_modal_clicked button click and the rest of the sidebar — st.dialog-decorated function call replaces the st.info placeholder. The selected profile auto-update path uses `st.session_state["profile_selectbox"] = new_profile_id` (already keyed in 06-02 sidebar).
</output>
