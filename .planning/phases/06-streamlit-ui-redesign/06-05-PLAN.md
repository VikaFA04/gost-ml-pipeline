---
phase: 06-streamlit-ui-redesign
plan: 05
type: execute
wave: 5
depends_on:
  - 06-02
  - 06-03
  - 06-04
files_modified:
  - app.py
  - .planning/phases/06-streamlit-ui-redesign/06-DESIGN-REVIEW.md
autonomous: false
requirements:
  - REQ-ui-design-review
  - REQ-ui-main-flow
must_haves:
  truths:
    - "All dead imports + dangling references in app.py are removed. AST scan reports zero unused module-level names."
    - "Russian-copy QA pass: every UI string in app.py matches 06-UI-SPEC §Copywriting Contract verbatim (no rogue English / typo / mismatched punctuation)."
    - "06-DESIGN-REVIEW.md exists and lists the 6 falsifiable PASS criteria from 06-RESEARCH.md §8 + 06-VALIDATION.md §Manual-Only Verifications."
    - "Project owner runs `streamlit run app.py`, walks the linear flow + modal + preflight error path + run-log download, marks each criterion PASS/FAIL with notes; recorded defects fixed before phase close."
  artifacts:
    - path: "app.py"
      provides: "cleaned-up final form: no dead imports, no dead references, Russian copy aligned with UI-SPEC"
    - path: ".planning/phases/06-streamlit-ui-redesign/06-DESIGN-REVIEW.md"
      provides: "Manual design-review checklist with 6 PASS criteria + defect log"
      contains: "Design-Review Sign-Off"
  key_links:
    - from: ".planning/phases/06-streamlit-ui-redesign/06-DESIGN-REVIEW.md"
      to: "06-RESEARCH.md §8 + 06-VALIDATION.md §Manual-Only Verifications"
      via: "verbatim transcription of 6 falsifiable PASS criteria"
      pattern: "Design-Review"
---

<objective>
Final cleanup of `app.py` (dead imports / dangling references / Russian-copy QA) and creation of the human design-review checklist (`06-DESIGN-REVIEW.md`) with falsifiable PASS criteria. Conduct the design-review pass with the project owner; record defects in the checklist; fix blocking defects before phase close.

Purpose: This wave closes REQ-ui-design-review (project owner sign-off with defects fixed) and locks the Phase 6 final shape. The checkpoint task pauses execution for the human design review per 06-CONTEXT.md success criterion 4.

Output:
- `app.py`: cleaned imports + Russian-copy aligned + no dead references.
- `.planning/phases/06-streamlit-ui-redesign/06-DESIGN-REVIEW.md`: the manual review checklist with 6 falsifiable PASS criteria.
- A completed design-review pass with PASS/FAIL marks; any blocking defects fixed via direct app.py edits inside this plan.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-streamlit-ui-redesign/06-CONTEXT.md
@.planning/phases/06-streamlit-ui-redesign/06-RESEARCH.md
@.planning/phases/06-streamlit-ui-redesign/06-PATTERNS.md
@.planning/phases/06-streamlit-ui-redesign/06-UI-SPEC.md
@.planning/phases/06-streamlit-ui-redesign/06-VALIDATION.md
@.planning/phases/06-streamlit-ui-redesign/06-04-SUMMARY.md
@app.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: app.py cleanup — dead imports + Russian copy QA</name>
  <read_first>
    - File being modified: app.py (full file)
    - 06-UI-SPEC.md §"Copywriting Contract" §"Empty state copy" §"Error state copy" — every Russian string in app.py must match a row in this contract verbatim
    - 06-PATTERNS.md §"Deleted Code" — list of functions that should NOT be present (verify they were removed by 06-02 / 06-03)
  </read_first>
  <action>
    Step A — Dead import audit:

    Run `python -c "import ast; tree = ast.parse(open('app.py').read()); imports = []; \
    [imports.append(n.name) for node in ast.walk(tree) if isinstance(node, ast.Import) for n in node.names]; \
    [imports.append((node.module, n.name)) for node in ast.walk(tree) if isinstance(node, ast.ImportFrom) for n in node.names]; \
    names = {n.id for n in ast.walk(tree) if isinstance(n, ast.Name)}; \
    print('imports:', imports); print('referenced names sample:', sorted(names)[:30])"` to enumerate all imports.

    For each imported name, verify with `grep -c "<name>" app.py` that it appears at least twice (once in import, once in use). For any name with count == 1 (only in import), remove from the import block.

    Likely candidates after 06-02/06-03/06-04:

    - `tempfile` (if not used after methodical sidebar form deletion) — verify and remove if unused.
    - `extract_text_from_file` (was imported in original app.py for the methodical sidebar form) — likely unused; 06-04 modal uses build_methodical_profile which internally calls extract_text_from_file, so the modal does NOT need a separate import.
    - `json` — verify usage; render_artifact_download_card may use it via `application_service`; check.

    Step B — Dead-reference scan:

    For each of the deleted functions (render_results, render_hero, render_metric_card, render_status_badges, filter_audit_df, filter_predictions_df, render_manual_decision_table, build_methodical_profile_draft, persist_custom_profile, _set_session_methodical_draft, _get_session_methodical_draft, _apply_methodical_form_edits), run `grep -n "<name>" app.py`. Each command must output zero hits. If any hit appears (e.g., a stale comment referencing a deleted helper), delete the offending line.

    Step C — Russian copy QA:

    Extract all string literals from app.py with `python -c "import ast; t = ast.parse(open('app.py').read()); [print(s.value) for s in ast.walk(t) if isinstance(s, ast.Constant) and isinstance(s.value, str) and len(s.value) > 3 and any(0x0400 <= ord(c) <= 0x04FF for c in s.value)]"`.

    Cross-reference each Cyrillic string against 06-UI-SPEC.md §Copywriting Contract / §Empty state copy / §Error state copy. For any discrepancy:

    - Mismatched punctuation (e.g., trailing dot vs no dot, quotation mark style) — align to UI-SPEC.
    - English fallback inside a Russian string (e.g., "uploaded" instead of "загружен") — fix.
    - Typo or missed translation — fix.

    Common-spec strings to verify (executor must grep each):

    - «Запустить аудит» (run button)
    - «+ Создать профиль из методички» (modal trigger)
    - «Сгенерировать предпросмотр» (preview button)
    - «Применить и сохранить» (apply button)
    - «Перезаписать существующий профиль» (overwrite checkbox)
    - «Причина (минимум 8 символов)» (reason textarea)
    - «Причина должна содержать минимум 8 непробельных символов (D-004: no silent rewrites).»
    - «Отмена» (cancel button)
    - «Загрузите DOCX-документ, чтобы начать аудит» (empty-state heading)
    - «В левой панели выберите профиль ГОСТ и загрузите файл. После запуска аудита здесь появятся счётчики и блоки.» (empty-state body)
    - «Отчёт по документу: {filename}» (report header)
    - «Профиль: {profile_name} ({profile_id})» (profile subline)
    - «Требуют внимания», «Изменены», «Без изменений» (section headers)
    - «Скачать результаты» (downloads header)
    - «Скачать журнал запуска (JSON)» (run-log download)
    - «Документ соответствует профилю — блоков, требующих внимания, нет.» (empty-attention message)
    - «Файл не читается. Проверьте, что это валидный DOCX (.docx, ZIP-архив). Откройте файл в Word и пересохраните, если нужно.» (preflight unreadable)
    - «В документе нет извлекаемых непустых блоков. Проверьте, что документ содержит текст.» (preflight no-blocks)
    - «Расширение файла `.docx`, но содержимое не соответствует DOCX-формату.» (MIME mismatch)
    - «PDF-файл не содержит извлекаемого текста. Скан без OCR не поддерживается.» (modal PDF no-text)
    - «Профиль `{profile_id}` уже существует. Чтобы перезаписать, отметьте чекбокс ниже и заполните поле «Причина» (минимум 8 символов).» (modal collision)
    - All STATUS_CHIP Russian labels (5 strings).
    - All summary counter labels (6 strings).
    - All expander body labels (6 strings: «Оригинальный текст блока», «Причина ручной проверки», «Нарушенные правила», «Применённые исправления», «Заблокированное автоисправление», «Сообщение об ошибке»).

    For each, run `grep -F "<string>" app.py` and verify the exact match. Any mismatch -> fix in app.py.

    Step D — Re-run full test suite to ensure no cleanup regressed tests:

    `python -m pytest tests/ -x -q --ignore=tests/test_methodical_profile_editor.py`

    Step E — Per CLAUDE.md «не «улучшай» соседний код»: do NOT refactor working code. The only edits in this task are:
    - Remove unused imports.
    - Delete dead reference lines (comments / strings that mention deleted helpers).
    - Fix Russian-copy mismatches against UI-SPEC.

    Do NOT rename variables, restructure functions, change CSS, or alter widget keys. Do NOT bump the Streamlit version pin.
  </action>
  <verify>
    <automated>python -c "import ast; t = ast.parse(open('app.py').read()); imp_names = []; [imp_names.append(n.name.split('.')[0]) for node in ast.walk(t) if isinstance(node, ast.Import) for n in node.names]; [imp_names.append(n.asname or n.name) for node in ast.walk(t) if isinstance(node, ast.ImportFrom) for n in node.names]; refs = {n.id for n in ast.walk(t) if isinstance(n, ast.Name)}; unused = [i for i in imp_names if i not in refs]; print('unused imports:', unused); assert not unused, f'Dead imports: {unused}'" 2>&1 | tee /tmp/06-05-task1-imports.log && for fn in render_results render_hero render_metric_card render_status_badges filter_audit_df filter_predictions_df render_manual_decision_table build_methodical_profile_draft persist_custom_profile _set_session_methodical_draft _get_session_methodical_draft _apply_methodical_form_edits; do echo "=== $fn ==="; grep -c "$fn" app.py; done | tee /tmp/06-05-task1-deadrefs.log && python -m pytest tests/ -x -q --ignore=tests/test_methodical_profile_editor.py 2>&1 | tee /tmp/06-05-task1-pytest.log</automated>
  </verify>
  <acceptance_criteria>
    - python AST scan for unused imports outputs `unused imports: []` (empty).
    - For each of the 12 deleted-function names, `grep -c "<name>" app.py` outputs `0`.
    - For each Russian-copy string in the catalog above, `grep -F "<string>" app.py` outputs at least one match.
    - python -m pytest tests/ -x -q --ignore=tests/test_methodical_profile_editor.py exits 0 (all tests GREEN).
    - wc -l app.py outputs a number between 500 and 750 (post-cleanup; reduced from 1216 baseline).
  </acceptance_criteria>
  <done>app.py has no dead imports, no dead references to deleted helpers, and every Russian string matches the UI-SPEC Copywriting Contract verbatim; full test suite passes.</done>
</task>

<task type="auto">
  <name>Task 2: Create 06-DESIGN-REVIEW.md with 6 falsifiable PASS criteria</name>
  <read_first>
    - File being created: .planning/phases/06-streamlit-ui-redesign/06-DESIGN-REVIEW.md (NEW)
    - 06-RESEARCH.md §8 "Design-Review Criteria for REQ-ui-design-review (SC-4)" — 6 PASS criteria, falsifiable
    - 06-VALIDATION.md §"Manual-Only Verifications" — owner runs streamlit + walks linear flow + opens modal + triggers preflight error + downloads run-log
    - 06-UI-SPEC.md §"Checker Sign-Off" — 6 dimension PASS markers for parity
  </read_first>
  <action>
    Create `.planning/phases/06-streamlit-ui-redesign/06-DESIGN-REVIEW.md` with the following structure:

    Top frontmatter:
    
      ---
      phase: 06-streamlit-ui-redesign
      review_type: design-review
      reviewer: project-owner
      created: 2026-05-14
      status: pending
      ---

    Sections (each section uses Markdown headers):

    Section 1 — `# Phase 6 Design Review`

    Body: one-paragraph context referencing 06-CONTEXT.md SC-4 «UI passes a design-review pass by the project owner» and 06-VALIDATION.md §Manual-Only Verifications.

    Section 2 — `## Pre-flight check`

    Bullet list:
    - [ ] `python -m pytest tests/ -x -q --ignore=tests/test_methodical_profile_editor.py` exits 0.
    - [ ] `streamlit run app.py` launches without exception.
    - [ ] Sidebar visible with «Панель управления» header.
    - [ ] Profile dropdown populated.
    - [ ] Run button disabled until DOCX uploaded.

    Section 3 — `## Falsifiable PASS criteria`

    Six numbered criteria (verbatim from 06-RESEARCH.md §8 «Design-Review Criteria for REQ-ui-design-review»):

    1. **Flow completeness.** Tester uploads a real DOCX, selects a profile, clicks «Запустить аудит», and reaches the download section in ≤ 3 sidebar interactions. No dead-ends (empty tab, broken button). — `[ ] PASS  [ ] FAIL`  Notes: ____
    2. **Visual distinction.** «Требуют внимания» section is visually first and expanded by default; «Без изменений» section is collapsed. Status chips for `error` and `no_change` are visually distinguishable at a glance (different background color, different icon). — `[ ] PASS  [ ] FAIL`  Notes: ____
    3. **No traceback exposure.** Running audit on an intentionally malformed DOCX (e.g., empty file or .docx-renamed .txt) shows a Russian error message under the uploader. No Python class names other than the error class name visible. No stack trace visible. — `[ ] PASS  [ ] FAIL`  Notes: ____
    4. **Run-log PII check.** Download «Скачать журнал запуска (JSON)» produces a JSON file. Open it. No paragraph text from the uploaded document appears in any field. Tester must inspect at least the `error_message` fields. — `[ ] PASS  [ ] FAIL`  Notes: ____
    5. **Modal D-004 gate.** In the methodical modal, click «Применить и сохранить» when reason textarea is empty → button is disabled OR shows error message. Enter `"abc"` (3 chars) → still blocked. Enter `"abcdefgh"` (8 chars) → button becomes enabled. — `[ ] PASS  [ ] FAIL`  Notes: ____
    6. **Profile auto-select.** After completing the methodical modal apply flow, close modal, verify the sidebar profile picker shows the new profile selected. — `[ ] PASS  [ ] FAIL`  Notes: ____

    Section 4 — `## Visual / Russian copy spot check`

    Spot-check 8 strings (one per row, with a checkbox):

    - [ ] Sidebar run button reads «Запустить аудит» (not «Запустить анализ документа» or similar).
    - [ ] Modal trigger button reads «+ Создать профиль из методички».
    - [ ] Report header reads «Отчёт по документу: {filename}».
    - [ ] Section header «Требуют внимания» visible at top of main pane (after a run).
    - [ ] Download button «Скачать журнал запуска (JSON)» visible.
    - [ ] Empty-state heading «Загрузите DOCX-документ, чтобы начать аудит» visible before any run.
    - [ ] No tab strip («Обзор / Предсказания / Аудит / Форматирование / Артефакты») present anywhere.
    - [ ] No `st.exception` traceback visible on any error path.

    Section 5 — `## Defect log`

    Markdown table with columns: `Severity (CRITICAL/HIGH/MEDIUM/LOW)`, `Criterion ref (1-6 or spot-check item)`, `Description`, `Status (open / fixed)`, `Fix commit (sha)`. Add a single empty row as a template for the owner to fill.

    Per 06-CONTEXT.md SC-4: «defects fixed before close». CRITICAL and HIGH defects are blocking; MEDIUM and LOW may close as known-issue v2 entries (with explicit user sign-off).

    Section 6 — `## Design-Review Sign-Off`

    - Reviewer: ____
    - Date: ____
    - Final status: `[ ] APPROVED  [ ] APPROVED-WITH-FOLLOWUPS  [ ] REJECTED`
    - Approval signature line.

    Per CLAUDE.md «минимум»: the file is a checklist, not an essay. Bullet/table content, no narrative paragraphs beyond Section 1.
  </action>
  <verify>
    <automated>test -f .planning/phases/06-streamlit-ui-redesign/06-DESIGN-REVIEW.md && grep -c "^## " .planning/phases/06-streamlit-ui-redesign/06-DESIGN-REVIEW.md && grep -c "PASS\\|FAIL" .planning/phases/06-streamlit-ui-redesign/06-DESIGN-REVIEW.md && grep -q "Запустить аудит" .planning/phases/06-streamlit-ui-redesign/06-DESIGN-REVIEW.md && grep -q "abcdefgh" .planning/phases/06-streamlit-ui-redesign/06-DESIGN-REVIEW.md</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/06-streamlit-ui-redesign/06-DESIGN-REVIEW.md` exists.
    - `grep -c "^## " 06-DESIGN-REVIEW.md` outputs at least 5 (5 H2 sections + the H1).
    - `grep -c "PASS" 06-DESIGN-REVIEW.md` outputs at least 6 (one per falsifiable criterion).
    - `grep -c "FAIL" 06-DESIGN-REVIEW.md` outputs at least 6.
    - `grep -q "Запустить аудит" 06-DESIGN-REVIEW.md` (spot-check item present).
    - `grep -q "abcdefgh" 06-DESIGN-REVIEW.md` (Modal D-004 gate test data present).
    - `grep -q "Скачать журнал запуска" 06-DESIGN-REVIEW.md`.
    - `grep -q "phase: 06-streamlit-ui-redesign" 06-DESIGN-REVIEW.md` (frontmatter).
  </acceptance_criteria>
  <done>06-DESIGN-REVIEW.md exists with frontmatter, 5 H2 sections, 6 falsifiable PASS criteria with PASS/FAIL checkboxes, 8 spot-check items, and a defect log table template.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Project-owner design review — walk through 6 PASS criteria and sign off</name>
  <what-built>
    The Phase 6 UI redesign is complete after 06-00..06-04:
    - Sidebar with profile picker, modal trigger, DOCX uploader, run button (D-01).
    - Main pane with report header, 6-cell st.metric strip, 3 grouped sections («Требуют внимания» / «Изменены» / «Без изменений»), downloads section + run-log JSON button (D-02).
    - methodical-extraction `st.dialog` modal with preview / apply / force-reason gate (D-03 + D-004 mirror).
    - Russian preflight error surface, no traceback leak in UI, PII-clean run-log JSON (D-04).
    - 27+ automated tests GREEN; pre-existing test suite GREEN.

    `app.py` cleaned up (no dead imports, no dead references, Russian copy aligned).
    `06-DESIGN-REVIEW.md` checklist ready for review.
  </what-built>
  <how-to-verify>
    Step 1 — Run the pre-flight checklist in `.planning/phases/06-streamlit-ui-redesign/06-DESIGN-REVIEW.md` §"Pre-flight check":
      - `python -m pytest tests/ -x -q --ignore=tests/test_methodical_profile_editor.py` exits 0.
      - `streamlit run app.py` launches.
      - Sidebar shows «Панель управления».
      - Profile dropdown populated.
      - Run button disabled until DOCX uploaded.

    Step 2 — For each of the 6 falsifiable PASS criteria in §"Falsifiable PASS criteria", perform the action and check the PASS / FAIL box:

      1. Flow completeness — upload a real DOCX (e.g., from positive_examples/), select a profile, click «Запустить аудит», confirm download section reachable in ≤ 3 sidebar interactions.
      2. Visual distinction — confirm «Требуют внимания» is first and expanded, «Без изменений» collapsed; status chip icons differ between `no_change` (●) and `error` (✗); colors differ visibly.
      3. No traceback — try uploading a renamed-text-file as `.docx` or an empty `.docx`; confirm Russian error under uploader, no Python stack trace anywhere in the UI.
      4. Run-log PII — click «Скачать журнал запуска (JSON)»; open the JSON; check at least 2 `error_message` fields and confirm no paragraph text appears.
      5. Modal D-004 — click «+ Создать профиль из методички»; upload a small PDF; pick a base profile; click «Сгенерировать предпросмотр»; confirm diff renders; if the profile_id collides, confirm overwrite checkbox + reason gate behaves correctly with «abc» (blocked) → «abcdefgh» (enabled).
      6. Profile auto-select — complete the modal apply flow on a non-colliding profile_id; confirm the sidebar profile picker shows the new profile pre-selected after the modal closes.

    Step 3 — Spot-check the 8 Russian copy / structural items in §"Visual / Russian copy spot check".

    Step 4 — Fill in §"Defect log" for any CRITICAL/HIGH defects; if any are CRITICAL/HIGH, fix them in `app.py` and re-test before signing off. MEDIUM/LOW defects may be marked as v2-known-issues with explicit acknowledgement.

    Step 5 — Fill in §"Design-Review Sign-Off" with reviewer name, date, final status. Final status must be `APPROVED` or `APPROVED-WITH-FOLLOWUPS` to proceed to phase verification. `REJECTED` requires returning to the appropriate plan (06-02/03/04) and re-executing.
  </how-to-verify>
  <resume-signal>
    Type "approved" (with all 6 PASS criteria + 8 spot-check items checked PASS and no CRITICAL/HIGH defects open in the defect log) to mark REQ-ui-design-review complete and proceed to phase verification.

    Type "approved-with-followups: <description>" if MEDIUM/LOW defects are accepted as v2 follow-ups; record them in PROJECT.md after phase close.

    Type "defect: <severity> <criterion-or-spot> <description>" to record a blocking defect; this requires returning to the relevant plan (06-02 / 06-03 / 06-04) for repair before re-running this checkpoint.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Design-review checklist → project owner | Checklist guides human verification; falsifiable PASS criteria prevent subjective sign-off |
| Defect log → app.py | Blocking defects fixed via direct edits inside this plan (no new wave needed) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-6-09 | Repudiation (LOW) | Design-review sign-off | mitigate | The 06-DESIGN-REVIEW.md file records reviewer name, date, status, and the defect log inside the planning artifact tree (committed to git). Sign-off is traceable. |
| T-6-10 | Information disclosure (LOW) | Manual review may upload a real corpus DOCX | accept | The review is performed locally on the developer's machine; no upload to external services. The run-log download is the only artifact materialized; its PII boundary is enforced by RunLog (T-6-01). |
</threat_model>

<verification>
- Task 1: AST scan reports zero unused imports; all 12 deleted-function names absent from app.py; every Russian string in the catalog is present; full test suite GREEN.
- Task 2: 06-DESIGN-REVIEW.md exists with 5 H2 sections, 6 PASS/FAIL pairs, frontmatter.
- Task 3: human design-review pass — all 6 PASS criteria + 8 spot-check items marked PASS; defect log shows no CRITICAL/HIGH open; sign-off field filled with reviewer name + date + status APPROVED or APPROVED-WITH-FOLLOWUPS.
</verification>

<success_criteria>
- app.py is in final shape: no dead imports, no orphan references, Russian copy aligned with UI-SPEC.
- 06-DESIGN-REVIEW.md committed with all 6 falsifiable PASS criteria + spot-check items + defect log + sign-off section.
- Project owner has walked the linear flow + modal + preflight error + run-log download and marked sign-off APPROVED or APPROVED-WITH-FOLLOWUPS.
- All blocking (CRITICAL/HIGH) defects fixed inside this plan with re-test confirmation.
- All Phase 6 Wave-0 tests + pre-existing test suite remain GREEN at the moment of sign-off.
</success_criteria>

<output>
After completion, create `.planning/phases/06-streamlit-ui-redesign/06-05-SUMMARY.md` capturing:
- Final app.py line count (target 500-750).
- List of imports removed in cleanup.
- List of Russian-copy mismatches fixed in cleanup (if any).
- Final 06-DESIGN-REVIEW.md sign-off status (APPROVED / APPROVED-WITH-FOLLOWUPS / REJECTED).
- Defect log summary: CRITICAL=0, HIGH=0, MEDIUM=X (deferred to v2), LOW=Y.
- Final test count green: 27+ Phase 6 tests + N pre-existing.
- Reviewer name + sign-off date.
</output>
