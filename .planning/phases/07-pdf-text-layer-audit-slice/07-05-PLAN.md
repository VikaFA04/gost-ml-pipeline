---
phase: 07
plan: 05
type: tdd
wave: 4
depends_on: [07-04]
gap_closure: true
closes_gaps: [G-07-02, G-07-03]
files_modified:
  - tests/test_app_ui.py
  - tests/inference/test_pdf_loader.py
  - app.py
  - src/inference/pdf_loader.py
autonomous: true
requirements: [REQ-pdf-text-only]
tags: [pdf, audit, gap-closure, tdd, copy, wording]

must_haves:
  truths:
    - "tests/test_app_ui.py::test_app_empty_state_visible_without_docx now asserts substring «Загрузите документ (DOCX или PDF)» (or accepts both «DOCX» and «PDF» substrings in the same haystack) — was previously asserting «Загрузите DOCX-документ»"
    - "A new RED-then-GREEN test in tests/inference/test_pdf_loader.py asserts that extract_pdf_blocks returns body-text rows whose explanation contains «требует ручной проверки» (the reviewer-facing rephrase, locked-substring «PDF блок» remains)"
    - "app.py main-pane empty-state alert now reads «Загрузите документ (DOCX или PDF), чтобы начать аудит» (was «Загрузите DOCX-документ, чтобы начать аудит»)"
    - "src/inference/pdf_loader.py extract_pdf_blocks emits the reviewer-facing reason «PDF блок — текстовый слой, требует ручной проверки» (locked substring «PDF блок» preserved; image-only-page sentinel «PDF page — без извлекаемого текста» UNCHANGED)"
    - "All Plan 07-01 RED tests remain GREEN (locked-substring «PDF блок» still present per Test 4 truth; the image-only-page sentinel test still passes verbatim)"
    - "Phase 6 contract preserved: render_block_section / STATUS_CHIP / modal_reason_is_valid tests unchanged"
  artifacts:
    - path: tests/test_app_ui.py
      provides: "Updated assertion on empty-state copy (DOCX + PDF mentioned)"
      contains: "Загрузите документ"
    - path: tests/inference/test_pdf_loader.py
      provides: "New test: extract_pdf_blocks text-block explanation contains «требует ручной проверки»"
      contains: "требует ручной проверки"
    - path: app.py
      provides: "Empty-state st.info copy updated to mention both DOCX and PDF"
      contains: "Загрузите документ (DOCX или PDF), чтобы начать аудит"
    - path: src/inference/pdf_loader.py
      provides: "Reviewer-facing reason wording for text-bearing PDF blocks"
      contains: "PDF блок — текстовый слой, требует ручной проверки"
  key_links:
    - from: tests/test_app_ui.py::test_app_empty_state_visible_without_docx
      to: app.py main-pane empty-state st.info call
      via: "AppTest harvests st.info text; assertion on Russian substring"
      pattern: "Загрузите документ"
    - from: tests/inference/test_pdf_loader.py
      to: src/inference/pdf_loader.py:extract_pdf_blocks
      via: "fixture text_pdf -> rows -> row['explanation'] contains «требует ручной проверки»"
      pattern: "требует ручной проверки"
    - from: app.py
      to: REQ-pdf-text-only + Phase 7 D-04 §3 (uploader-extension copy alignment)
      via: "empty-state copy mirrors sidebar uploader label («Загрузите документ (DOCX или PDF)»)"
      pattern: "Загрузите документ \\(DOCX или PDF\\)"
---

<objective>
Close G-07-02 (cosmetic) and G-07-03 (cosmetic) — both are small copy/wording tweaks identified during Phase 7 UAT:

- **G-07-02:** Main-pane empty-state alert in `app.py` still reads «Загрузите DOCX-документ, чтобы начать аудит» (Phase 6 leftover). Now inconsistent with the sidebar uploader label «Загрузите документ (DOCX или PDF)» landed by Plan 07-03.
- **G-07-03:** Per-block PDF audit reason in `src/inference/pdf_loader.py:extract_pdf_blocks` reads «PDF блок — классификация недоступна (SVM требует DOCX-формата)» — framed from the model's POV. Prefer wording centred on the human reviewer's action.

Both fixes are TDD-disciplined per CLAUDE.md «Багфикс начинается с падающего теста» — even cosmetic copy changes get a test landing before the production-side change so future drift fails CI.

Bundled into a single plan because both are mechanical copy edits with grep-shaped regression tests; separating them would violate CLAUDE.md «минимум кода». No file overlap inside the same wave with another plan (07-04 already merged before this wave starts; this plan touches `app.py` for an unrelated copy site).

Output: 2 modified test files + 2 modified production files; total ≤ 10 new/changed lines.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-pdf-text-layer-audit-slice/07-CONTEXT.md
@.planning/phases/07-pdf-text-layer-audit-slice/07-RESEARCH.md
@.planning/phases/07-pdf-text-layer-audit-slice/07-PATTERNS.md
@.planning/phases/07-pdf-text-layer-audit-slice/07-UI-SPEC.md
@.planning/phases/07-pdf-text-layer-audit-slice/07-UAT.md
@.planning/phases/07-pdf-text-layer-audit-slice/07-01-SUMMARY.md
@.planning/phases/07-pdf-text-layer-audit-slice/07-02-SUMMARY.md
@./CLAUDE.md

@app.py
@src/inference/pdf_loader.py
@tests/test_app_ui.py
@tests/inference/test_pdf_loader.py
@tests/inference/conftest.py

<interfaces>
<!-- Two-state contract for the gap-fix executor. -->

# G-07-02: app.py main-pane empty-state alert
#
# STATE A — main HEAD (gsd/phase-04-regression-gate at 53246b5):
#   app.py:699:        st.info("Загрузите DOCX-документ, чтобы начать аудит")
#   surrounding st.caption("В левой панели...") at lines 700-703 UNCHANGED.
#
# STATE B — worktree-agent-ae7c41d1 (post Plan 07-03 merge):
#   app.py:715:        st.info("Загрузите DOCX-документ, чтобы начать аудит")
#   surrounding st.caption(...) at lines 716-719 UNCHANGED.
#
# Both states carry the IDENTICAL «Загрузите DOCX-документ, чтобы начать аудит»
# string (Phase 7 did NOT update this; that's the gap). The fix is a one-line
# string replacement; line numbers differ but the literal source line to match
# is byte-identical:
#
#   st.info("Загрузите DOCX-документ, чтобы начать аудит")
#
# Executor uses `grep -n "Загрузите DOCX-документ, чтобы начать аудит" app.py`
# to locate the exact line on disk (one match expected); no need to rely on
# line numbers.
#
# The `st.warning("Сначала загрузите DOCX-документ.")` at line 407 (STATE A) /
# line 422 (STATE B) is a DIFFERENT string (trailing period, no «чтобы начать
# аудит») and is OUT OF SCOPE for this gap — it is a transient action prompt
# (after «Запустить аудит» pressed with no upload), not the persistent empty-
# state hint. The gap definition explicitly targets the main-pane empty-state
# alert.

# G-07-03: src/inference/pdf_loader.py text-block explanation wording
#
# Current (both STATE A and STATE B — pdf_loader.py was landed by Plan 07-02
# and is identical on both main HEAD and worktree-agent-ae7c41d1):
#   src/inference/pdf_loader.py:90:                        "explanation": "PDF блок — классификация недоступна (SVM требует DOCX-формата)",
#
# The image-only-page sentinel at lines 69-78 carries the DIFFERENT locked
# string «PDF page — без извлекаемого текста» which is NOT in scope for this
# gap — leave unchanged. The 07-RESEARCH.md / 07-01-PLAN.md / Plan 07-01 test
# `test_extract_pdf_blocks_image_only_page_sentinel` asserts that sentinel
# string verbatim.
#
# Locked substring «PDF блок» MUST remain in the new wording (Plan 07-01
# §truth + Test 4 UAT note). Reviewer-facing rephrase from 07-UAT G-07-03:
# «PDF блок — текстовый слой, требует ручной проверки».

# Test file state on disk (both STATE A and STATE B carry these — they were
# landed by Plan 07-01):
#
# tests/inference/test_pdf_loader.py lines 76-83 — test_extract_pdf_blocks_image_only_page_sentinel
#   asserts «PDF page — без извлекаемого текста» (image-only sentinel — UNCHANGED here)
# tests/inference/test_pdf_loader.py lines 56-67 — test_extract_pdf_blocks_schema
#   asserts row["status"] == "review" + applied_fixes == "" + kind == "paragraph";
#   does NOT assert any explanation substring. We must add a NEW test that
#   asserts the reviewer-facing «требует ручной проверки» substring on text
#   blocks (NOT the image-only-page sentinel — those have a different explanation).
#
# tests/test_app_ui.py lines 32-48 — test_app_empty_state_visible_without_docx
#   asserts `"Загрузите DOCX-документ" in haystack` — this assertion MUST be
#   updated to assert the new copy. Since the new copy is «Загрузите документ
#   (DOCX или PDF), чтобы начать аудит», the natural substring assertion is
#   «Загрузите документ» (matches both old «Загрузите DOCX-документ» AND new
#   «Загрузите документ (DOCX или PDF)» — but the OLD copy is what the gap
#   says is broken, so we must assert specifically for the PDF-mention; use
#   two substring assertions: «Загрузите документ (DOCX или PDF)»).
</interfaces>
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: RED — update existing test_app_empty_state_visible_without_docx assertion + add new test_extract_pdf_blocks_text_block_reviewer_wording</name>
  <files>tests/test_app_ui.py, tests/inference/test_pdf_loader.py</files>
  <read_first>
    - tests/test_app_ui.py lines 32-48 (current test_app_empty_state_visible_without_docx — asserts old copy «Загрузите DOCX-документ»; this must be flipped to the new copy)
    - tests/inference/test_pdf_loader.py lines 56-67 (existing test_extract_pdf_blocks_schema — DOES NOT assert explanation wording; we add a NEW test next to it)
    - tests/inference/test_pdf_loader.py lines 76-83 (existing test_extract_pdf_blocks_image_only_page_sentinel — asserts the «PDF page — без извлекаемого текста» sentinel verbatim; OUT OF SCOPE for this plan, do NOT modify)
    - tests/inference/conftest.py (text_pdf + scanned_pdf fitz fixtures already defined; we reuse `text_pdf` for the new test)
    - .planning/phases/07-pdf-text-layer-audit-slice/07-UAT.md §G-07-02 + §G-07-03 (verbatim gap definitions)
    - ./CLAUDE.md §Тестирование «Багфикс: начинается с падающего теста, воспроизводящего баг»
  </read_first>
  <behavior>
    - **G-07-02 test update (tests/test_app_ui.py):** The existing `test_app_empty_state_visible_without_docx` asserts substring «Загрузите DOCX-документ». Flip the assertion to require BOTH substrings «Загрузите документ» AND «(DOCX или PDF)» in the rendered haystack. The docstring is updated to reference Phase 7 D-04 §3 + G-07-02 (uploader copy alignment). Function name is PRESERVED — the regression-guard role of the test continues; only the contract content evolves (matches the Plan 07-01 «test_app_upload_contract_unchanged» precedent where the name stayed but the assertion content changed).

    - **G-07-03 test addition (tests/inference/test_pdf_loader.py):** Add a new test `test_extract_pdf_blocks_text_block_reviewer_wording` that:
      - Uses the existing `text_pdf` fixture (from `tests/inference/conftest.py`).
      - Calls `extract_pdf_blocks(text_pdf)` and inspects the FIRST row (which is a text block per the conftest fixture — single-page PDF with «Тестовый текст параграфа» inserted).
      - Asserts row["status"] == "review" (sanity guard).
      - Asserts «PDF блок» in row["explanation"] (locked substring per Plan 07-01 §truth — preserves the existing invariant).
      - Asserts «требует ручной проверки» in row["explanation"] (NEW reviewer-facing substring — RED until Task 2 lands the wording change).
      - Does NOT assert the old wording «SVM требует DOCX-формата» (would be brittle — the new wording drops that explanation, which is the point of the gap fix).

    - Both tests stay otherwise unchanged. Phase 6 tests, the image-only-page sentinel test, the other 12 Plan 07-01 tests — all UNTOUCHED.
  </behavior>
  <action>
    Per CLAUDE.md «Багфикс: начинается с падающего теста». Two surgical edits — one per test file.

    **Edit 1 — `tests/test_app_ui.py` lines 32-48 (replace test body + docstring):**

    Locate the existing `test_app_empty_state_visible_without_docx` function. Replace the function body (docstring + assertion) WITHOUT changing the name, the parameter list, or the `at = app_test.run(timeout=30)` + `rendered = []` + `for collection in (at.markdown, ...)` harvesting pattern. Replace ONLY the docstring and the final assertion.

    Current:
    ```python
    def test_app_empty_state_visible_without_docx(app_test) -> None:
        """Phase 6 empty-state heading from UI-SPEC §Empty state copy must be rendered.

        RED today — current `render_hero` text is different. Drives Waves 2/3.
        Match substring `Загрузите DOCX-документ` (without trailing punctuation).
        """
        at = app_test.run(timeout=30)
        rendered = []
        for collection in (at.markdown, at.title, at.header, at.subheader, at.warning, at.info):
            for element in collection:
                value = getattr(element, "value", None)
                if value is not None:
                    rendered.append(str(value))
        haystack = "\n".join(rendered)
        assert "Загрузите DOCX-документ" in haystack, (
            "Phase 6 empty-state heading missing from rendered output"
        )
    ```

    Replace with:
    ```python
    def test_app_empty_state_visible_without_docx(app_test) -> None:
        """Phase 7 D-04 §3 / G-07-02 empty-state copy must mention DOCX + PDF.

        Original Phase 6 copy was «Загрузите DOCX-документ»; Phase 7 expanded
        SUPPORTED_UPLOAD_TYPES to ['docx', 'pdf'] and Plan 07-03 updated the
        sidebar uploader label to «Загрузите документ (DOCX или PDF)». G-07-02
        closes the drift between sidebar uploader copy and main-pane empty
        state — the empty-state alert now mirrors the uploader: «Загрузите
        документ (DOCX или PDF), чтобы начать аудит».
        """
        at = app_test.run(timeout=30)
        rendered = []
        for collection in (at.markdown, at.title, at.header, at.subheader, at.warning, at.info):
            for element in collection:
                value = getattr(element, "value", None)
                if value is not None:
                    rendered.append(str(value))
        haystack = "\n".join(rendered)
        assert "Загрузите документ" in haystack, (
            "Phase 7 G-07-02 empty-state copy missing «Загрузите документ» substring"
        )
        assert "(DOCX или PDF)" in haystack, (
            "Phase 7 G-07-02 empty-state copy missing «(DOCX или PDF)» substring"
        )
    ```

    Notes — design choices that follow CLAUDE.md:
    - Function name PRESERVED (matches the Plan 07-01 «test_app_upload_contract_unchanged» pattern — keep regression-guard name, evolve contract content).
    - Two separate substring assertions (not one combined string assertion) — per CLAUDE.md «маленькие функции на одну ответственность, разделить compound assertions»; each assertion has its own diagnostic message. This catches partial-fix regressions (e.g. someone changes copy to «Загрузите DOCX или PDF документ» — first assertion passes, second fails).
    - Substring «Загрузите документ» (not «Загрузите документ (DOCX или PDF), чтобы начать аудит» full-form) — allows minor punctuation drift without making the test brittle while still failing on the OLD copy «Загрузите DOCX-документ» (which lacks the space between «документ» and the next token because «DOCX-документ» is hyphenated).
    - The harvesting boilerplate (lines 38-44 in the original) is byte-identical — we do NOT touch it.

    **Edit 2 — `tests/inference/test_pdf_loader.py` (INSERT new test after the existing test_extract_pdf_blocks_image_only_page_sentinel):**

    Locate the existing `test_extract_pdf_blocks_image_only_page_sentinel` function at lines 76-83. INSERT this new test immediately AFTER its closing line (blank line separation preserved):

    ```python


    def test_extract_pdf_blocks_text_block_reviewer_wording(text_pdf: Path) -> None:
        """G-07-03: text-block explanation must be framed for the human reviewer.

        Locked substring «PDF блок» (Plan 07-01 §truth) is preserved. The full
        wording shifts from model-POV «классификация недоступна (SVM требует
        DOCX-формата)» to reviewer-action «требует ручной проверки» per
        07-UAT.md G-07-03.
        """
        from src.inference.pdf_loader import extract_pdf_blocks

        rows = extract_pdf_blocks(text_pdf)
        assert len(rows) >= 1
        first = rows[0]
        assert first["status"] == "review"
        # Locked substring — preserves Plan 07-01 §truth / UAT Test 4 invariant.
        assert "PDF блок" in first["explanation"]
        # New reviewer-facing substring — the G-07-03 fix.
        assert "требует ручной проверки" in first["explanation"]
    ```

    Notes:
    - Deferred import of `extract_pdf_blocks` inside the test body — matches the deferred-import style established by Plan 07-01 (07-01 §"Deferred imports inside test bodies").
    - Uses the existing `text_pdf` fixture from `tests/inference/conftest.py` — no new fixture introduced, per CLAUDE.md «минимум кода».
    - Inspects `rows[0]` (first row) — the conftest text_pdf fixture inserts «Тестовый текст параграфа» on page 1, so rows[0] is a text block (not an image-only sentinel). The `assert len(rows) >= 1` is a sanity gate; the `assert first["status"] == "review"` confirms we are on a text-block row (image-only sentinels also have status="review", which is why the «PDF блок» substring assertion is the disambiguator — image-only sentinels carry «PDF page — без извлекаемого текста», NOT «PDF блок»).
    - Two locked-substring assertions (per CLAUDE.md compound-assertion split): «PDF блок» (old + new — invariant) + «требует ручной проверки» (new only — the fix).

    DO NOT:
    - Modify any other test in `tests/test_app_ui.py` (imports + smoke + traceback test all stay byte-identical).
    - Modify any of the 12 other tests in `tests/inference/test_pdf_loader.py`.
    - Modify `tests/inference/conftest.py`.
    - Touch production code in this task.

    Per CLAUDE.md «верификация готово»: after both file edits, run pytest in current session before marking task done.
  </action>
  <verify>
    <automated>python -m pytest tests/test_app_ui.py::test_app_empty_state_visible_without_docx tests/inference/test_pdf_loader.py::test_extract_pdf_blocks_text_block_reviewer_wording --collect-only 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Загрузите DOCX-документ" tests/test_app_ui.py` returns 0 (old assertion gone)
    - `grep -c "Загрузите документ" tests/test_app_ui.py` returns ≥1 (new assertion)
    - `grep -c "(DOCX или PDF)" tests/test_app_ui.py` returns 1 (new assertion)
    - `grep "G-07-02" tests/test_app_ui.py` returns ≥1 (docstring reference)
    - `grep -c "test_extract_pdf_blocks_text_block_reviewer_wording" tests/inference/test_pdf_loader.py` returns 1 (new test added once)
    - `grep "требует ручной проверки" tests/inference/test_pdf_loader.py` returns 1
    - `grep "G-07-03" tests/inference/test_pdf_loader.py` returns ≥1 (docstring reference)
    - The image-only-page sentinel test `test_extract_pdf_blocks_image_only_page_sentinel` is UNTOUCHED: `grep "PDF page — без извлекаемого текста" tests/inference/test_pdf_loader.py` returns 1 (the existing assertion)
    - On Streamlit-enabled venv: `python -m pytest tests/test_app_ui.py::test_app_empty_state_visible_without_docx -x` exits NON-ZERO (RED) — assertion fails because app.py still emits the OLD copy «Загрузите DOCX-документ»
    - `python -m pytest tests/inference/test_pdf_loader.py::test_extract_pdf_blocks_text_block_reviewer_wording -x` exits NON-ZERO (RED) — assertion fails because pdf_loader.py still emits the OLD wording «классификация недоступна (SVM требует DOCX-формата)»
    - No production code touched (`git diff app.py src/` is empty after this task)
    - No other test file touched (`git diff tests/ | grep "^diff --git" | wc -l` returns exactly 2)
  </acceptance_criteria>
  <done>
    Both RED tests in place. Task 2 will land the production-side copy/wording changes that flip them GREEN.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — update app.py empty-state copy + src/inference/pdf_loader.py text-block reason wording</name>
  <files>app.py, src/inference/pdf_loader.py</files>
  <read_first>
    - app.py — locate the line `st.info("Загрузите DOCX-документ, чтобы начать аудит")` via `grep -n "Загрузите DOCX-документ, чтобы начать аудит" app.py` (one match expected, line number differs between STATE A and STATE B)
    - app.py — confirm the `st.warning("Сначала загрузите DOCX-документ.")` is a DIFFERENT line (trailing period, no «чтобы начать аудит») and is OUT OF SCOPE
    - src/inference/pdf_loader.py:90 — locate the literal string `"PDF блок — классификация недоступна (SVM требует DOCX-формата)"`
    - src/inference/pdf_loader.py:76 — confirm the OTHER occurrence of «PDF» in the file is the image-only-page sentinel «PDF page — без извлекаемого текста» which MUST stay unchanged
    - tests/test_app_ui.py + tests/inference/test_pdf_loader.py (from Task 1 — the RED tests this edit must make GREEN)
    - ./CLAUDE.md §«не рефактори то, что работает» (no other edits to app.py or pdf_loader.py)
  </read_first>
  <action>
    Two surgical string replacements. Per CLAUDE.md «минимум кода»: 0 new lines, 2 replaced lines, 0 deleted lines.

    **Edit 1 — `app.py` empty-state copy (G-07-02):**

    Use grep to confirm exactly one match before editing:
    ```bash
    grep -n "Загрузите DOCX-документ, чтобы начать аудит" app.py
    ```

    Replace the literal line (whatever its line number on disk):

    ```python
            st.info("Загрузите DOCX-документ, чтобы начать аудит")
    ```

    With:

    ```python
            st.info("Загрузите документ (DOCX или PDF), чтобы начать аудит")
    ```

    Indentation is PRESERVED (the existing 8-space indent inside `if result is None:`). Only the string contents change. No new import, no new function, no surrounding line touched.

    DO NOT touch:
    - The `st.warning("Сначала загрузите DOCX-документ.")` line in `run_processing` — that is a different transient action prompt (trailing period, no «чтобы начать аудит» suffix); G-07-02 explicitly scopes the persistent empty-state alert, not the action prompt.
    - The `st.caption("В левой панели выберите профиль ГОСТ и загрузите файл. ...")` lines immediately below — the caption already reads «загрузите файл» without DOCX-specificity, no change needed (Phase 6 copy is incidentally Phase-7-friendly here).

    **Edit 2 — `src/inference/pdf_loader.py` text-block reason wording (G-07-03):**

    In `extract_pdf_blocks`, locate the literal explanation string in the text-block branch (line 90 on both STATE A and STATE B — the file was landed by Plan 07-02 and is identical on main HEAD and worktree-agent-ae7c41d1):

    ```python
                        "explanation": "PDF блок — классификация недоступна (SVM требует DOCX-формата)",
    ```

    Replace with:

    ```python
                        "explanation": "PDF блок — текстовый слой, требует ручной проверки",
    ```

    Indentation is PRESERVED (the existing 24-space indent inside the nested dict). Only the string contents change.

    DO NOT touch:
    - The image-only-page sentinel at line 76: `"explanation": "PDF page — без извлекаемого текста"` — that string is asserted verbatim by `test_extract_pdf_blocks_image_only_page_sentinel` and is OUT OF SCOPE for G-07-03.
    - The module docstring (lines 1-16).
    - `_ARABIC_RE` / `_TEXT_CAP` constants.
    - `PdfNoTextLayer` exception class.
    - `check_text_layer` function.
    - The fitz `try/finally` wrapper or any block-iteration logic.

    Per CLAUDE.md «не рефактори то, что работает»: this task touches ONLY two string literals. No function signature changes, no new constants, no shared-string-extraction refactor.

    Per CLAUDE.md «верификация готово»: after both file edits, run pytest in current session.
  </action>
  <verify>
    <automated>python -m pytest tests/test_app_ui.py::test_app_empty_state_visible_without_docx tests/inference/test_pdf_loader.py::test_extract_pdf_blocks_text_block_reviewer_wording tests/inference/test_pdf_loader.py::test_extract_pdf_blocks_image_only_page_sentinel tests/inference/test_pdf_loader.py::test_extract_pdf_blocks_schema -x 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Загрузите DOCX-документ, чтобы начать аудит" app.py` returns 0 (old empty-state copy gone)
    - `grep -c "Загрузите документ (DOCX или PDF), чтобы начать аудит" app.py` returns 1 (new empty-state copy present, exactly once)
    - The OTHER «Загрузите DOCX-документ» occurrence (the transient action prompt with trailing period) is UNTOUCHED: `grep -c "Сначала загрузите DOCX-документ\." app.py` returns 1
    - `grep -c "классификация недоступна (SVM требует DOCX-формата)" src/inference/pdf_loader.py` returns 0 (old reason wording gone)
    - `grep -c "PDF блок — текстовый слой, требует ручной проверки" src/inference/pdf_loader.py` returns 1 (new reason wording present, exactly once)
    - `grep -c "PDF блок" src/inference/pdf_loader.py` returns 1 (locked substring still present — Plan 07-01 §truth invariant preserved)
    - `grep -c "PDF page — без извлекаемого текста" src/inference/pdf_loader.py` returns 1 (image-only-page sentinel UNCHANGED — out of scope for G-07-03)
    - On Streamlit-enabled venv: `python -m pytest tests/test_app_ui.py::test_app_empty_state_visible_without_docx tests/inference/test_pdf_loader.py::test_extract_pdf_blocks_text_block_reviewer_wording -v` exits 0 — both RED tests now GREEN
    - Plan 07-01 tests still pass: `python -m pytest tests/inference/test_pdf_loader.py -v` reports 13 of 13 PASS (if Streamlit-enabled and Plan 07-02/07-03 GREEN on the working tree) or 7 of 7 PASS for the pdf_loader-only subset on system Python
    - Phase 7 plans 07-01..07-04 tests unchanged: `python -m pytest tests/test_preflight.py tests/test_app_upload_contract.py tests/test_render_block_section.py tests/test_render_report_pdf.py tests/test_run_processing_pdf_bypass.py` exit code is 0 on Streamlit-enabled venv (clean-skip on system Python is acceptable)
    - Phase 1-6 regression suite unchanged: `python -m pytest tests/ --ignore=tests/inference --ignore=tests/test_preflight.py --ignore=tests/test_app_upload_contract.py --ignore=tests/test_render_block_section.py --ignore=tests/test_render_report_pdf.py --ignore=tests/test_run_processing_pdf_bypass.py --ignore=tests/test_app_ui.py` exit code unchanged from pre-plan baseline
    - `wc -l app.py` change: 0 (string-only edit, no new lines)
    - `wc -l src/inference/pdf_loader.py` change: 0 (string-only edit, no new lines)
    - `git diff app.py src/inference/pdf_loader.py | grep "^+" | grep -v "^+++"` reports exactly 2 added lines (one per file) and exactly 2 removed lines (one per file) — pure 1-for-1 line replacement
  </acceptance_criteria>
  <done>
    Empty-state copy mirrors sidebar uploader label; text-block reason wording is reviewer-facing; locked «PDF блок» substring + image-only-page sentinel both preserved; both Task 1 RED tests are now GREEN. G-07-02 + G-07-03 closed at the code level.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| app.py empty-state st.info text → DOM | Hardcoded Russian string, no user input interpolation, no `unsafe_allow_html` — safe |
| pdf_loader.py text-block explanation → audit CSV → DOM | Existing Phase 7 surface; explanation is a hardcoded string, no user input interpolation |
| Test fixtures → fitz | tmp_path-only (reused from Plan 07-01 conftest); no new fixture, no new attack surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-05-01 | Tampering | Locked Russian substring «PDF блок» drift | mitigate | New test `test_extract_pdf_blocks_text_block_reviewer_wording` asserts the locked substring «PDF блок» AND the new substring «требует ручной проверки»; CI fails on any character change to either. The existing image-only-page sentinel test continues to assert «PDF page — без извлекаемого текста» verbatim — that string is now LITERALLY pinned in two different test paths (the new text-block test asserts NOT-equality by virtue of asserting a different substring, the existing sentinel test asserts equality). |
| T-07-05-02 | Tampering | Empty-state copy drift between sidebar uploader and main pane | mitigate | Updated `test_app_empty_state_visible_without_docx` asserts both «Загрузите документ» AND «(DOCX или PDF)» — any future copy drift that drops PDF mention (e.g. revert to «Загрузите DOCX-документ») fails CI. Sidebar uploader label «Загрузите документ (DOCX или PDF)» is asserted by Plan 07-03 grep audits, not by this plan — the two surfaces stay aligned via test parallelism, not shared constants (which would be overkill for two strings; per CLAUDE.md «минимум кода»). |
| T-07-05-03 | Information Disclosure | New text-block explanation surfaces in the audit CSV | accept | The new wording «PDF блок — текстовый слой, требует ручной проверки» contains no path, no exception name, no document text. Same PII profile as the OLD wording «классификация недоступна (SVM требует DOCX-формата)» (also no PII). D-04 boundary unchanged. |
| T-07-05-04 | Spoofing | Attacker uploads `.pdf` file hoping the empty-state copy change reveals new attack surface | accept | The empty-state copy is rendered when `st.session_state["last_result"] is None` — no user input flows through the rendering. Pure presentation change. No new code path, no new endpoint. |

No new trust boundaries introduced. Both edits are cosmetic copy changes with regression-test pinning.
</threat_model>

<verification>
1. Targeted GREEN of the two RED tests on Streamlit-enabled venv:
   `python -m pytest tests/test_app_ui.py::test_app_empty_state_visible_without_docx tests/inference/test_pdf_loader.py::test_extract_pdf_blocks_text_block_reviewer_wording -v`
   → Both PASS.

2. Phase 7 Plans 07-01..07-04 regression — all locked substrings still asserted, no test regressed:
   `python -m pytest tests/inference/test_pdf_loader.py tests/test_preflight.py tests/test_app_upload_contract.py tests/test_render_block_section.py tests/test_render_report_pdf.py tests/test_app_ui.py tests/test_run_processing_pdf_bypass.py -v`
   → All PASS / clean-skip.

3. Phase 1-6 regression suite unchanged:
   `python -m pytest tests/ --ignore=tests/inference --ignore=tests/test_preflight.py --ignore=tests/test_app_upload_contract.py --ignore=tests/test_render_block_section.py --ignore=tests/test_render_report_pdf.py --ignore=tests/test_run_processing_pdf_bypass.py --ignore=tests/test_app_ui.py -x`
   → All GREEN.

4. Static grep audit for the four locked strings (the contract this plan establishes + the invariants it preserves):
   - `grep -c "Загрузите документ (DOCX или PDF), чтобы начать аудит" app.py` returns 1 (new G-07-02 copy)
   - `grep -c "Загрузите DOCX-документ, чтобы начать аудит" app.py` returns 0 (old G-07-02 copy gone)
   - `grep -c "PDF блок — текстовый слой, требует ручной проверки" src/inference/pdf_loader.py` returns 1 (new G-07-03 wording)
   - `grep -c "классификация недоступна (SVM требует DOCX-формата)" src/inference/pdf_loader.py` returns 0 (old G-07-03 wording gone)
   - `grep -c "PDF блок" src/inference/pdf_loader.py` returns 1 (locked substring invariant preserved)
   - `grep -c "PDF page — без извлекаемого текста" src/inference/pdf_loader.py` returns 1 (image-only-page sentinel UNCHANGED)

5. Live UAT smoke (post-fix manual sanity, optional but recommended on Streamlit-enabled venv): run `streamlit run app.py` with no upload. The main pane shows the new empty-state copy «Загрузите документ (DOCX или PDF), чтобы начать аудит». Upload Berger PDF, run audit, expand any block — «Причина ручной проверки» now reads «PDF блок — текстовый слой, требует ручной проверки» instead of the model-POV phrasing. Reproduces the UAT scenarios that originally surfaced G-07-02 and G-07-03.

6. Per CLAUDE.md «верификация готово»: pytest must run in the current session before SUMMARY is written.
</verification>

<success_criteria>
- [ ] `tests/test_app_ui.py::test_app_empty_state_visible_without_docx` asserts «Загрузите документ» AND «(DOCX или PDF)» (was «Загрузите DOCX-документ»)
- [ ] `tests/inference/test_pdf_loader.py::test_extract_pdf_blocks_text_block_reviewer_wording` exists and asserts «PDF блок» AND «требует ручной проверки»
- [ ] `app.py` main-pane empty-state alert reads «Загрузите документ (DOCX или PDF), чтобы начать аудит» (exactly one occurrence)
- [ ] `app.py` does NOT touch the transient action-prompt warning «Сначала загрузите DOCX-документ.» (still present, unchanged)
- [ ] `src/inference/pdf_loader.py` text-block branch emits «PDF блок — текстовый слой, требует ручной проверки» (exactly one occurrence)
- [ ] `src/inference/pdf_loader.py` image-only-page sentinel «PDF page — без извлекаемого текста» UNCHANGED (still asserted by Plan 07-01 test)
- [ ] Plan 07-01 locked substring «PDF блок» still appears in pdf_loader.py (invariant preserved)
- [ ] Both new RED tests now GREEN on Streamlit-enabled venv
- [ ] Phase 6 contract preserved (STATUS_CHIP / modal_reason_is_valid / render_block_section tests unchanged)
- [ ] Phase 7 Plans 07-01..07-04 tests unchanged
- [ ] Phase 1-6 regression suite unchanged
- [ ] No file outside `files_modified` is touched (`git diff --name-only` matches the frontmatter list)
- [ ] No new dependencies, no new fixtures, no new helpers — pure string replacement + 1 new test
- [ ] `wc -l` for app.py and src/inference/pdf_loader.py unchanged (both edits are 1-for-1 line replacements)
- [ ] G-07-02 and G-07-03 status moves from `failed` to `resolved` in the next UAT run (out of scope for THIS plan — closes the gaps at the code level only)
</success_criteria>

<output>
After completion, create `.planning/phases/07-pdf-text-layer-audit-slice/07-05-SUMMARY.md` per `$HOME/.claude/get-shit-done/templates/summary.md`. Record:
- Test result deltas: BEFORE Task 2 (`python -m pytest tests/test_app_ui.py::test_app_empty_state_visible_without_docx tests/inference/test_pdf_loader.py::test_extract_pdf_blocks_text_block_reviewer_wording 2>&1 | tail -1`) vs AFTER Task 2 (same command)
- Exact `git diff app.py src/inference/pdf_loader.py` (paste the full diff — should be small: 2 lines changed total across both production files)
- Confirmation that the locked «PDF блок» + «PDF page — без извлекаемого текста» substrings are both still present (paste the grep counts)
- Confirmation that no Phase 1-6 or Phase 7 Plans 01-04 test regressed (paste the full-suite exit summary)
- Note for orchestrator: G-07-02 and G-07-03 closed at the code level; UAT re-run is needed to flip the gap statuses from `failed` to `resolved` in 07-UAT.md
</output>
</content>
</invoke>