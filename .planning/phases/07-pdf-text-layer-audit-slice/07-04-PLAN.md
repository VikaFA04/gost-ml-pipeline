---
phase: 07
plan: 04
type: tdd
wave: 3
depends_on: [07-03]
gap_closure: true
closes_gaps: [G-07-01]
files_modified:
  - tests/test_run_processing_pdf_bypass.py
  - app.py
  - CLAUDE.md
autonomous: true
requirements: [REQ-pdf-text-only]
tags: [pdf, audit, gap-closure, tdd, red-green, baseline-bypass]

must_haves:
  truths:
    - "Failing test exists in tests/test_run_processing_pdf_bypass.py asserting that run_processing does NOT short-circuit for PDF input when selected_model_key == 'baseline_unavailable' (the test monkeypatches app.process_document to capture invocation; before the fix the test FAILS because process_document is never called)"
    - "After the GREEN edit, app.run_processing bypasses the baseline_unavailable guard when Path(uploaded_file.name).suffix.lower() == '.pdf' — PDF inputs proceed to process_document, which dispatches to the PDF branch per Plan 07-02"
    - "DOCX inputs still receive the baseline_unavailable st.error short-circuit (Phase 6 contract preserved — no regression)"
    - "CLAUDE.md §'Принципы исполнения' carries one new императивное rule about checking pre-pipeline UI gates when supported input formats expand; the rule is appended ONCE (no duplicate), grep on file confirms exactly one matching line"
    - "Phase 1-6 regression suite remains GREEN; Phase 7 Plans 07-01..07-03 tests remain GREEN (test_app_upload_contract, test_render_report_pdf, etc.)"
  artifacts:
    - path: tests/test_run_processing_pdf_bypass.py
      provides: "RED-then-GREEN regression test asserting PDF bypasses baseline_unavailable guard"
      min_lines: 40
      contains: "baseline_unavailable"
    - path: app.py
      provides: "PDF-aware short-circuit on baseline_unavailable guard inside run_processing"
      contains: 'Path(uploaded_file.name).suffix.lower() == ".pdf"'
    - path: CLAUDE.md
      provides: "New rule about cross-checking pre-pipeline gates on supported-format expansion"
      contains: "расширении набора поддерживаемых входных форматов"
  key_links:
    - from: tests/test_run_processing_pdf_bypass.py
      to: app.run_processing + app.process_document
      via: "monkeypatch on app.process_document captures invocation; assertion that capture list is non-empty for PDF input"
      pattern: "monkeypatch\\.setattr.*process_document|app\\.process_document"
    - from: app.run_processing
      to: Path(uploaded_file.name).suffix.lower()
      via: "early-bypass branch evaluates uploaded file suffix BEFORE baseline_unavailable st.error short-circuit"
      pattern: "suffix\\.lower\\(\\)\\s*==\\s*\"\\.pdf\""
    - from: CLAUDE.md §'Принципы исполнения'
      to: "rule append per Self-improvement protocol"
      via: "single императивное предложение appended to existing bullet list"
      pattern: "расширении набора поддерживаемых входных форматов"
---

<objective>
Close G-07-01: PDF audit flow must run without a baseline .joblib artifact (PDF path bypasses SVM per Plan 07-02 §truths).

Purpose: The `app.py:run_processing` `baseline_unavailable` guard fires BEFORE `process_document` is invoked, so the PDF branch (which does not need the SVM) never gets a chance to run when the workspace lacks a baseline model. UAT was unblocked manually by copying a legacy .joblib; the underlying gate is the actual defect. This plan adds a RED regression test, then patches the guard to skip when the uploaded file's suffix is `.pdf`, then appends one императивное self-improvement rule to CLAUDE.md documenting the root-cause class.

TDD-disciplined per CLAUDE.md «Багфикс начинается с падающего теста».

Output: 1 new test file + 1 modified `app.py` + 1 appended-line `CLAUDE.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-pdf-text-layer-audit-slice/07-CONTEXT.md
@.planning/phases/07-pdf-text-layer-audit-slice/07-RESEARCH.md
@.planning/phases/07-pdf-text-layer-audit-slice/07-PATTERNS.md
@.planning/phases/07-pdf-text-layer-audit-slice/07-UI-SPEC.md
@.planning/phases/07-pdf-text-layer-audit-slice/07-UAT.md
@.planning/phases/07-pdf-text-layer-audit-slice/07-01-SUMMARY.md
@.planning/phases/07-pdf-text-layer-audit-slice/07-02-SUMMARY.md
@./CLAUDE.md

@app.py
@tests/test_preflight.py
@tests/test_app_upload_contract.py

<interfaces>
<!-- Frozen contracts this plan consumes (from Plans 07-01..07-03). -->

# app.run_processing (post Plan 07-03 — executor must verify state on disk before editing)
#
# The gap-fix executor will branch from MAIN HEAD `gsd/phase-04-regression-gate`
# which currently DOES NOT YET contain Plan 07-03's commits (07-03 lives on
# worktree-agent-ae7c41d1 and is awaiting checkpoint resolution + final merge).
#
# Two states the executor MUST distinguish before editing:
#
# STATE A — main HEAD (gsd/phase-04-regression-gate at commit 53246b5, what
# the gap-fix executor will see when branching for THIS plan):
#
#   app.py around lines 399-413:
#     def run_processing(uploaded_file, selected_model_key: str, ...) -> None:
#         """..."""
#         if uploaded_file is None:
#             st.warning("Сначала загрузите DOCX-документ.")
#             return
#
#         if selected_model_key == "baseline_unavailable":
#             st.error("Baseline-модель недоступна: в workspace нет сохраненного .joblib-артефакта.")
#             return
#
#         input_path = save_uploaded_bytes(uploaded_file.getvalue(), suffix=Path(uploaded_file.name).suffix)
#         ...
#     The except tuple at line 425 is still (FileNotFoundError, NotImplementedError, ValueError, zipfile.BadZipFile).
#     SUPPORTED_UPLOAD_TYPES at line 24 is still ["docx"].
#     No `from src.inference.pdf_loader import PdfNoTextLayer` import.
#
# STATE B — worktree-agent-ae7c41d1 (what the working tree looks like AFTER
# Plan 07-03 finishes merging):
#
#   app.py around lines 414-427 (note +15 line offset because of the
#   PdfNoTextLayer import + sidebar caption + audit-only badge edits from 07-03):
#     def run_processing(uploaded_file, selected_model_key: str, ...) -> None:
#         """..."""
#         if uploaded_file is None:
#             st.warning("Сначала загрузите DOCX-документ.")
#             return
#
#         if selected_model_key == "baseline_unavailable":
#             st.error("Baseline-модель недоступна: в workspace нет сохраненного .joblib-артефакта.")
#             return
#
#         input_path = save_uploaded_bytes(uploaded_file.getvalue(), suffix=Path(uploaded_file.name).suffix)
#         ...
#     The except tuple at line 440 is (FileNotFoundError, PdfNoTextLayer, ValueError, zipfile.BadZipFile).
#     SUPPORTED_UPLOAD_TYPES at line 25 is ["docx", "pdf"].
#     `from src.inference.pdf_loader import PdfNoTextLayer` is imported at line 19.
#
# The `if selected_model_key == "baseline_unavailable":` GUARD BODY itself is
# byte-identical between STATE A and STATE B — only line numbers differ. The
# gap-fix edit applies identically to both states; the orchestrator will
# resolve any merge conflict when finalising the 07-03 worktree merge.

# app.process_document (provided by Plans 07-01..07-02, frozen — DO NOT modify)
#
# Branches on Path(input_path).suffix.lower():
#   - ".pdf" -> _process_pdf -> PdfNoTextLayer (rejected) or ProcessingArtifacts(input_extension=".pdf", output_docx=None)
#   - ".docx" -> existing DOCX path with SVM + rule engine
# When called from the test in this plan, process_document is MONKEYPATCHED — the
# real implementation is NOT invoked. We only assert that the guard does not
# short-circuit before the call site.

# Locked test invariants this plan introduces
#
# - Test file name: tests/test_run_processing_pdf_bypass.py (per gap_closure
#   bundling guidance in the planning context).
# - Test must use pytest.importorskip("streamlit") since it imports `app`.
# - Test must monkeypatch app.process_document so the real PDF pipeline does
#   NOT run during unit-testing — we only verify the GUARD behaviour.
# - Test must use a fake uploaded_file object (a tiny class with .name, .getvalue() — see app.py:414 usage).
</interfaces>
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: RED — write tests/test_run_processing_pdf_bypass.py asserting PDF bypasses baseline_unavailable guard</name>
  <files>tests/test_run_processing_pdf_bypass.py</files>
  <read_first>
    - app.py lines 399-440 (STATE A, main HEAD; if the working tree already has STATE B per 07-03 merge, lines 414-455) — locate `def run_processing` and the `if selected_model_key == "baseline_unavailable":` short-circuit
    - tests/test_preflight.py (existing Phase 6/7 Streamlit-gated test file — pytest.importorskip pattern + monkeypatch style)
    - tests/test_app_upload_contract.py (existing — bare `import app` pattern, post-07-01)
    - .planning/phases/07-pdf-text-layer-audit-slice/07-UAT.md §G-07-01 (gap definition — missing items list)
    - ./CLAUDE.md §Тестирование «Багфикс: начинается с падающего теста, воспроизводящего баг»
  </read_first>
  <behavior>
    - Test 1 (test_run_processing_pdf_input_bypasses_baseline_unavailable_guard):
      Construct a fake uploaded_file with `.name = "doc.pdf"` and `.getvalue() -> b""`.
      Monkeypatch `app.process_document` with a recorder that appends to a list and returns a stub ProcessingArtifacts-like object (use MagicMock or a SimpleNamespace; the function does not need to return anything observable beyond the «was called» fact — run_processing stores the result in st.session_state but the test asserts the recorder is non-empty).
      Monkeypatch `app.save_uploaded_bytes` with a no-op returning a tmp_path Path so the test does not write real bytes.
      Monkeypatch `app.RunLog` with a stub that has no-op .record / .dump_json so no real RunLog file is created.
      Call `app.run_processing(uploaded_file=fake, selected_model_key="baseline_unavailable", selected_mode="audit", selected_profile_path=str(tmp_path / "p.json"))`.
      Assert that the recorder list has exactly 1 entry (process_document WAS called) — BEFORE the fix this is 0 (RED), AFTER the fix this is 1 (GREEN).

    - Test 2 (test_run_processing_docx_input_still_short_circuits_on_baseline_unavailable):
      Same setup but `.name = "doc.docx"`. Call run_processing. Assert the recorder list has 0 entries (DOCX still short-circuits — Phase 6 contract preserved).
      This is a regression guard ensuring the fix does not accidentally bypass the gate for DOCX.

    - Both tests are gated by `pytest.importorskip("streamlit")` at module top.
  </behavior>
  <action>
    Per CLAUDE.md «Багфикс: начинается с падающего теста, воспроизводящего баг». This task lands the test BEFORE the production code change in Task 2.

    Per CLAUDE.md «минимум кода»: 2 tests in one file, ~60-80 lines total. No helper classes beyond the SimpleNamespace upload stub. No parametrisation. Each test is straight-line.

    Create `tests/test_run_processing_pdf_bypass.py` with this exact body:

    ```python
    """Regression test for G-07-01: PDF input must not be short-circuited by the
    baseline_unavailable guard in app.run_processing.

    The PDF branch of process_document bypasses the SVM (Plan 07-02 §truths), so
    the absence of a baseline .joblib is irrelevant for PDF input. Phase 6
    introduced the guard for DOCX; Phase 7 must teach it to skip PDFs.

    Test uses pytest.importorskip("streamlit") + monkeypatch on app.process_document
    so no real pipeline runs — we only assert the GUARD behaviour.
    """

    from __future__ import annotations

    from pathlib import Path
    from types import SimpleNamespace

    import pytest

    pytest.importorskip("streamlit")

    import app  # noqa: E402  — gated by importorskip above


    def _make_uploaded(name: str) -> SimpleNamespace:
        """Build a minimal uploaded-file stub matching the duck-type that
        app.run_processing reads (Path(uploaded_file.name).suffix + .getvalue())."""
        return SimpleNamespace(name=name, getvalue=lambda: b"")


    def _patch_pipeline(monkeypatch, tmp_path: Path) -> list:
        """Replace process_document + save_uploaded_bytes + RunLog with recorders
        so run_processing executes without touching disk or invoking the real
        SVM/PDF pipeline. Returns the recorder list — non-empty iff
        process_document was called."""
        process_calls: list = []

        def _record(*args, **kwargs):
            process_calls.append((args, kwargs))
            return SimpleNamespace(
                model_type="stub",
                mode="audit",
                profile_path=tmp_path / "p.json",
                input_path=tmp_path / "x",
                input_extension=".pdf",
                extracted_csv=tmp_path / "e.csv",
                predictions_csv=tmp_path / "e.csv",
                report_csv=tmp_path / "r.csv",
                report_json=tmp_path / "r.json",
                summary_json=tmp_path / "s.json",
                summary_txt=tmp_path / "s.txt",
                output_docx=None,
                predictions_df=None,
                report_df=None,
                summary={},
            )

        monkeypatch.setattr(app, "process_document", _record, raising=True)
        monkeypatch.setattr(
            app,
            "save_uploaded_bytes",
            lambda data, suffix: tmp_path / f"upload{suffix}",
            raising=True,
        )

        class _StubRunLog:
            def __init__(self, *args, **kwargs) -> None: ...
            def record(self, *args, **kwargs) -> None: ...
            def dump_json(self, *args, **kwargs) -> None: ...

        monkeypatch.setattr(app, "RunLog", _StubRunLog, raising=True)
        return process_calls


    def test_run_processing_pdf_input_bypasses_baseline_unavailable_guard(
        monkeypatch, tmp_path: Path
    ) -> None:
        """G-07-01: PDF inputs must reach process_document even when
        selected_model_key == 'baseline_unavailable' (PDF path bypasses SVM)."""
        process_calls = _patch_pipeline(monkeypatch, tmp_path)

        app.run_processing(
            uploaded_file=_make_uploaded("sample.pdf"),
            selected_model_key="baseline_unavailable",
            selected_mode="audit",
            selected_profile_path=str(tmp_path / "p.json"),
        )

        assert len(process_calls) == 1, (
            "PDF input must NOT be short-circuited by the baseline_unavailable "
            "guard; expected process_document to be called once, got "
            f"{len(process_calls)} call(s)."
        )


    def test_run_processing_docx_input_still_short_circuits_on_baseline_unavailable(
        monkeypatch, tmp_path: Path
    ) -> None:
        """DOCX regression guard: the fix must NOT widen the bypass to DOCX.
        Phase 6 contract — DOCX without a baseline produces st.error and returns."""
        process_calls = _patch_pipeline(monkeypatch, tmp_path)

        app.run_processing(
            uploaded_file=_make_uploaded("sample.docx"),
            selected_model_key="baseline_unavailable",
            selected_mode="audit",
            selected_profile_path=str(tmp_path / "p.json"),
        )

        assert process_calls == [], (
            "DOCX input MUST still short-circuit on baseline_unavailable; "
            f"got {len(process_calls)} unexpected call(s) to process_document."
        )
    ```

    Notes — these are deliberate design choices following CLAUDE.md:
    - `pytest.importorskip("streamlit")` mirrors the Phase 6 gate used in tests/test_preflight.py and tests/test_render_block_section.py — collection succeeds on system Python without Streamlit; the file skips cleanly.
    - SimpleNamespace upload stub avoids a custom class; mirrors the duck-typing pattern the existing test_app_ui.py uses.
    - The recorder pattern is identical to Plan 07-01 Task 4's `_capture_streamlit_calls` style — no new abstraction introduced.
    - We do NOT call `st.session_state` directly — the run_processing implementation handles all session-state writes via the stubbed RunLog/save_uploaded_bytes. Streamlit's session_state is auto-initialised on `import app`.
    - DOCX-regression test is the «test for the regression I might cause» per CLAUDE.md «обработаны happy/unhappy/error path и крайние случаи».
  </action>
  <verify>
    <automated>python -m pytest tests/test_run_processing_pdf_bypass.py --collect-only 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/test_run_processing_pdf_bypass.py` exists
    - `python -m pytest tests/test_run_processing_pdf_bypass.py --collect-only` exits 0 and reports exactly 2 collected tests (or 2 skipped if Streamlit absent — skip is acceptable since the file will run on the Streamlit-enabled venv)
    - On a Streamlit-enabled interpreter: `python -m pytest tests/test_run_processing_pdf_bypass.py -x` exits NON-ZERO (RED) — `test_run_processing_pdf_input_bypasses_baseline_unavailable_guard` fails with `assert 0 == 1` (process_document was NOT called because baseline_unavailable short-circuits)
    - On a Streamlit-enabled interpreter: `test_run_processing_docx_input_still_short_circuits_on_baseline_unavailable` PASSES (DOCX correctly short-circuits — the pre-existing behaviour we will preserve)
    - `grep "pytest.importorskip" tests/test_run_processing_pdf_bypass.py` matches exactly 1 line
    - `grep "baseline_unavailable" tests/test_run_processing_pdf_bypass.py` matches ≥3 lines (docstring + 2 test names + assertion messages)
    - `grep "monkeypatch.setattr(app, \"process_document\"" tests/test_run_processing_pdf_bypass.py` matches exactly 1 line (inside _patch_pipeline)
    - No production code touched (`git diff app.py src/` is empty after this task)
  </acceptance_criteria>
  <done>
    RED test in place; on a Streamlit-enabled venv pytest exits non-zero on the PDF-bypass assertion. DOCX regression-guard test is GREEN. Task 2 will flip the PDF test to GREEN by adding the suffix-check bypass to app.run_processing.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — add PDF-aware bypass on baseline_unavailable guard in app.run_processing</name>
  <files>app.py</files>
  <read_first>
    - app.py — locate `def run_processing` (search for the literal `def run_processing(uploaded_file`). The function body shape is byte-identical between STATE A (main HEAD) and STATE B (post-07-03 worktree); only line numbers differ
    - Verify which state is on disk by running `grep -n "from src.inference.pdf_loader import PdfNoTextLayer" app.py` — if it returns 1 line you are on STATE B, if 0 lines you are on STATE A. Both are valid bases for this edit
    - tests/test_run_processing_pdf_bypass.py (from Task 1 — the test this edit must make GREEN)
    - .planning/phases/07-pdf-text-layer-audit-slice/07-UAT.md §G-07-01 «missing» list (the literal fix specification: `Path(uploaded_file.name).suffix.lower() == ".pdf"`)
    - ./CLAUDE.md §«не рефактори то, что работает» (no other edits to app.py)
  </read_first>
  <action>
    ONE surgical edit to `app.py`. Per CLAUDE.md «маленькие функции на одну ответственность»: this task touches only the `baseline_unavailable` guard inside `run_processing`. Per CLAUDE.md «минимум кода»: ≤4 lines added, 0 removed.

    Locate the existing guard inside `run_processing`. The guard body is byte-identical between STATE A and STATE B; the literal lines to find via grep are:

    ```python
        if selected_model_key == "baseline_unavailable":
            st.error("Baseline-модель недоступна: в workspace нет сохраненного .joblib-артефакта.")
            return
    ```

    Replace those 3 lines with the following 4 lines (one extra line: the PDF-suffix check):

    ```python
        if selected_model_key == "baseline_unavailable" and Path(uploaded_file.name).suffix.lower() != ".pdf":
            # G-07-01: PDF input bypasses the SVM (Plan 07-02 D-02), so a missing
            # baseline .joblib must not block PDF audits. DOCX still short-circuits.
            st.error("Baseline-модель недоступна: в workspace нет сохраненного .joblib-артефакта.")
            return
    ```

    Notes — these are deliberate choices following CLAUDE.md «следуй существующему стилю» and «минимум кода»:

    - The condition is folded into the existing `if` with `and` rather than introducing a new outer guard — preserves the function shape, matches Phase 6 style elsewhere in the file (e.g. line 406 single-line `if uploaded_file is None` guard).
    - `Path` is already imported at the top of app.py (line 5 in STATE A, line 5 in STATE B — confirm via `grep -n "from pathlib import Path" app.py`) — no new import.
    - The Russian `st.error` message is UNCHANGED (DOCX users still see the same message — Phase 6 contract preserved).
    - The 2-line inline comment documents the gap ID + rationale so future readers can trace it via `git blame`. Per CLAUDE.md «Каждая изменённая строка должна напрямую отслеживаться до запроса пользователя».
    - The `.suffix.lower()` cast matches the same idiom used in `save_uploaded_bytes(... suffix=Path(uploaded_file.name).suffix)` at line 414/429 of run_processing — case-insensitive on the suffix is necessary because Streamlit's uploader does not normalise the extension casing client-side.

    DO NOT touch:
    - The `st.warning("Сначала загрузите DOCX-документ.")` branch above (that empty-state copy is G-07-02's scope — Plan 07-05 will update it).
    - The `except` tuple inside the try-block (Plan 07-03 already wired PdfNoTextLayer on STATE B; on STATE A the orchestrator's final 07-03 merge handles it).
    - The PdfNoTextLayer import / SUPPORTED_UPLOAD_TYPES list / sidebar uploader copy / render_report badge (all Plan 07-03 surface).
    - Any other call site of `run_processing` or `process_document`.

    Per CLAUDE.md «верификация готово»: after the edit, run pytest in the current session BEFORE marking the task done.
  </action>
  <verify>
    <automated>python -m pytest tests/test_run_processing_pdf_bypass.py -x 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'selected_model_key == "baseline_unavailable" and Path(uploaded_file.name).suffix.lower() != ".pdf"' app.py` matches exactly 1 line
    - `grep -n 'selected_model_key == "baseline_unavailable":$' app.py` returns no matches (the standalone single-condition guard is gone — replaced by the compound condition)
    - `grep -c "G-07-01" app.py` returns 1 (the inline rationale comment)
    - On Streamlit-enabled venv: `python -m pytest tests/test_run_processing_pdf_bypass.py -v` exits 0 — BOTH tests GREEN (PDF bypass + DOCX regression guard)
    - Phase 7 plan-07-01 tests still pass: `python -m pytest tests/inference/test_pdf_loader.py::test_check_text_layer_berger_accepted tests/inference/test_pdf_loader.py::test_check_text_layer_scanned_rejected tests/inference/test_pdf_loader.py::test_extract_pdf_blocks_schema -v` exits 0
    - Phase 6 contract preserved — `python -m pytest tests/test_app_upload_contract.py tests/test_render_block_section.py tests/test_preflight.py -v` exits 0 on Streamlit-enabled venv (skip cleanly on system Python is acceptable)
    - Phase 1-6 regression suite unchanged: `python -m pytest tests/ --ignore=tests/inference --ignore=tests/test_preflight.py --ignore=tests/test_app_upload_contract.py --ignore=tests/test_render_block_section.py --ignore=tests/test_render_report_pdf.py --ignore=tests/test_run_processing_pdf_bypass.py --ignore=tests/test_app_ui.py` exit code unchanged from pre-plan baseline
    - `wc -l app.py` increase is exactly +1 line (4 new lines replace 3 old lines)
    - No other site of app.py touched: `git diff app.py | grep "^+" | grep -v "^+++"` reports ≤5 added lines and 3 removed lines
  </acceptance_criteria>
  <done>
    PDF input no longer short-circuits on baseline_unavailable; DOCX behaviour preserved; G-07-01 closed at the code level. Both regression tests GREEN.
  </done>
</task>

<task type="auto">
  <name>Task 3: Append self-improvement rule to CLAUDE.md §Принципы исполнения (no duplicate)</name>
  <files>CLAUDE.md</files>
  <read_first>
    - ./CLAUDE.md §Self-improvement protocol («Перед добавлением правила выполни поиск по этому файлу. Если уже есть правило, покрывающее этот случай, не дублируй, а уточни существующее.» + «Формат правила: одно императивное предложение, без обоснования и без примеров»)
    - ./CLAUDE.md §Принципы исполнения (full bullet list — confirm no existing rule already covers «расширение supported input formats» + «pre-pipeline UI gate»)
    - The directive in the planning context: «Self-improvement rule to register at the bottom of CLAUDE.md»
  </read_first>
  <action>
    Per CLAUDE.md §Self-improvement protocol:

    1. Search the file for a rule already covering this root-cause class. Run:
       ```bash
       grep -n "supported.*формат\|поддерживаемых.*формат\|pre-pipeline\|baseline_unavailable\|UI-гейт" CLAUDE.md
       ```
       Expected: zero matches inside §Принципы исполнения. If a similar rule exists, UPDATE it instead of adding a new one (per protocol). Based on the read above, no such rule exists.

    2. Append ONE императивное предложение to the existing bullet list in §Принципы исполнения. Insertion site: at the END of the bullet list, AFTER the last existing bullet «- Не добавляй в commit messages трейлеры Co-Authored-By, signed-off-by, Generated-by или любые маркеры авторства AI/инструмента.» and BEFORE the `---` section separator that closes §Принципы исполнения.

    3. The rule to append (exact text — single line, one императивное предложение, no обоснование, no example, per protocol format):

       ```
       - При расширении набора поддерживаемых входных форматов проверь все pre-pipeline UI-гейты, опирающиеся на наличие артефактов модели или специфический suffix, и убедись, что они не блокируют новые форматы, чьи pipeline-ветки не зависят от соответствующего артефакта.
       ```

    Notes — exact protocol compliance:
    - The leading `- ` matches the existing bullet style in §Принципы исполнения.
    - Single императивное предложение, no rationale, no example (per protocol: «без обоснования и без примеров, если случай не двусмысленный»).
    - Wording is one full sentence; «pre-pipeline UI-гейты» / «опирающиеся на наличие артефактов модели или специфический suffix» / «не зависят от соответствующего артефакта» — captures the root-cause class (the baseline_unavailable guard) without describing the specific symptom (per CLAUDE.md «После фикса бага записывай правило про класс корневой причины, а не про конкретный симптом»).
    - No emoji, no markdown bold, no nested list.

    DO NOT:
    - Touch any other section of CLAUDE.md (Self-improvement protocol, Тестирование, Code Review, Доступные MCP-серверы).
    - Modify any other bullet in §Принципы исполнения.
    - Reorder existing bullets.
    - Add an explanatory paragraph below the new bullet.
    - Bump the token-budget comment («2500 токенов») — that is an existing protocol field and our +1 bullet does not breach it (current file is well under the cap).
  </action>
  <verify>
    <automated>grep -c "расширении набора поддерживаемых входных форматов" CLAUDE.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "При расширении набора поддерживаемых входных форматов" CLAUDE.md` returns exactly 1 (no duplicate)
    - `grep -c "pre-pipeline UI-гейты" CLAUDE.md` returns exactly 1
    - The new bullet appears INSIDE §Принципы исполнения, BEFORE the `---` separator that closes the section: `awk '/## Принципы исполнения/,/^---$/' CLAUDE.md | grep -c "расширении набора поддерживаемых"` returns 1
    - The new bullet is the LAST bullet of the section (so the file's structure stays predictable): the line immediately AFTER «Не добавляй в commit messages трейлеры…» starts with the new rule
    - No other section was modified: `git diff CLAUDE.md | grep "^[+-]" | grep -v "^+++\|^---" | wc -l` ≤ 2 (one added line, one trailing blank-line shuffle at most)
    - `wc -l CLAUDE.md` increased by exactly 1 line vs the pre-plan baseline
    - Total file token count remains ≤ 2500 tokens (per protocol budget); rough word-count proxy: `wc -w CLAUDE.md` < 2000
  </acceptance_criteria>
  <done>
    One императивное rule appended to §Принципы исполнения; no duplicate; no other section touched; protocol budget honoured.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Streamlit upload widget → run_processing | `uploaded_file.name` is user-controlled but used ONLY for `Path(...).suffix.lower()` extraction; no traversal possible since `.name` is the filename component only |
| run_processing guard logic → process_document | Guard decision based on `selected_model_key` (internal enum) + uploaded suffix (sanitised via Path.suffix); no untrusted data flows past the guard into a security-sensitive context |
| Test monkeypatch on process_document → test isolation | Tests do not invoke the real pipeline; no fitz, no SVM, no disk writes outside tmp_path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-04-01 | Spoofing | Attacker uploads a `.pdf`-named file that is NOT a real PDF, hoping to bypass the guard | accept | Bypass only skips the `baseline_unavailable` short-circuit; the file still flows into `process_document → validate_document_input` which routes on `SUPPORTED_EXTENSIONS` (already includes `.pdf` since Phase 6) and then `_process_pdf → fitz.open()` which validates the PDF magic bytes. A `.pdf`-named non-PDF raises a fitz parse error caught by the generic `except Exception` branch at app.py:438 (STATE A) / app.py:453 (STATE B), surfaced as «Не удалось обработать документ.» per existing Phase 6 contract. No new attack surface — the spoofed file lands in the same parser sink whether the baseline guard bypasses or not. |
| T-07-04-02 | Tampering | Test monkeypatch leaks into other tests in the same pytest session | mitigate | pytest's `monkeypatch` fixture is function-scoped — all setattr calls are auto-reverted after each test function. The recorder list is local to each test (no module-level state). Per CLAUDE.md «тесты убирают за собой». |
| T-07-04-03 | Information Disclosure | New `.suffix.lower()` call leaks original filename casing to logs | accept | `Path(uploaded_file.name).suffix.lower()` returns ONLY the lowercased extension (`.pdf` or `.docx`) — no full filename, no path. RunLog already records the full filename per Phase 6 contract; this gap fix does not widen that surface. PII boundary (D-04) preserved. |
| T-07-04-04 | Denial of Service | Attacker uploads a `.pdf`-named file repeatedly to exhaust the new code path | accept | Same DoS profile as the existing PDF path (T-07-W1-02 from Plan 07-02 threat model) — bounded by Streamlit's 200 MB upload cap. The baseline-bypass adds no new processing step; it just removes an early return. |
| T-07-04-05 | Elevation of Privilege | Bypass of baseline_unavailable could be exploited to invoke a PDF code path that has elevated privileges | accept | `_process_pdf` runs in the same process / same user context as the existing DOCX path; no privilege boundary crossed. The function is local-only (REQ-pdf-text-only + C-NFR-PRIVACY). |

No new threats introduced beyond the existing Phase 7 surface. The change is a guard relaxation, not a new code path.
</threat_model>

<verification>
1. Targeted GREEN of the gap-closure tests on Streamlit-enabled venv:
   `python -m pytest tests/test_run_processing_pdf_bypass.py -v`
   → 2 of 2 PASS (test_run_processing_pdf_input_bypasses_baseline_unavailable_guard + test_run_processing_docx_input_still_short_circuits_on_baseline_unavailable).

2. Phase 7 regression — Plans 07-01..07-03 tests unchanged:
   `python -m pytest tests/inference/ tests/test_preflight.py tests/test_app_upload_contract.py tests/test_render_block_section.py tests/test_render_report_pdf.py -v`
   → Same pass/skip count as pre-Phase-7-04 baseline.

3. Phase 1-6 regression suite unchanged:
   `python -m pytest tests/ --ignore=tests/inference --ignore=tests/test_preflight.py --ignore=tests/test_app_upload_contract.py --ignore=tests/test_render_block_section.py --ignore=tests/test_render_report_pdf.py --ignore=tests/test_run_processing_pdf_bypass.py --ignore=tests/test_app_ui.py -x`
   → All GREEN (or same skipped count as before).

4. Static grep audit for the fix and the rule:
   - `grep -c "Path(uploaded_file.name).suffix.lower() != \".pdf\"" app.py` returns 1
   - `grep -c "G-07-01" app.py` returns 1
   - `grep -c "При расширении набора поддерживаемых входных форматов" CLAUDE.md` returns 1

5. Live PDF + missing-baseline smoke (post-fix manual sanity, optional but recommended on Streamlit-enabled venv): rename or move the workspace `results/models/*.joblib` so `list_model_options` reports baseline_unavailable; run `streamlit run app.py`; upload Berger PDF; click «Запустить аудит». The pipeline must execute and the report must render — no «Baseline-модель недоступна» error. This reproduces the UAT scenario that originally surfaced G-07-01.

6. Per CLAUDE.md «верификация готово»: pytest must run in the current session before SUMMARY is written.
</verification>

<success_criteria>
- [ ] `tests/test_run_processing_pdf_bypass.py` exists with 2 tests (PDF bypass + DOCX regression guard)
- [ ] Test is gated by `pytest.importorskip("streamlit")`
- [ ] On Streamlit-enabled venv: 2 of 2 tests PASS after Task 2
- [ ] On Streamlit-enabled venv: 1 of 2 tests FAILS before Task 2 (PDF bypass test RED at the assertion `len(process_calls) == 1`)
- [ ] `app.py:run_processing` `baseline_unavailable` guard is widened to skip when `Path(uploaded_file.name).suffix.lower() == ".pdf"`
- [ ] Russian `st.error` text for DOCX path UNCHANGED (no Phase 6 copy regression)
- [ ] Phase 7 Plans 07-01..07-03 tests unchanged (all still PASS / skip-clean)
- [ ] Phase 1-6 regression suite unchanged
- [ ] CLAUDE.md §Принципы исполнения has exactly 1 new bullet covering the root-cause class
- [ ] CLAUDE.md token budget (≤2500) preserved
- [ ] No file outside `files_modified` is touched (`git diff --name-only` matches the frontmatter list)
- [ ] G-07-01 status moves from `failed` to `resolved` in the next UAT run (out of scope for THIS plan — closes the gap at the code level only)
</success_criteria>

<output>
After completion, create `.planning/phases/07-pdf-text-layer-audit-slice/07-04-SUMMARY.md` per `$HOME/.claude/get-shit-done/templates/summary.md`. Record:
- Test result deltas: BEFORE Task 2 (`python -m pytest tests/test_run_processing_pdf_bypass.py 2>&1 | tail -1`) vs AFTER Task 2 (same command)
- Exact diff of the `app.py` edit (`git diff app.py | head -20`)
- The exact CLAUDE.md new-bullet line (one line, copy-pasted verbatim)
- Confirmation that no Phase 1-6 or Phase 7 Plans 01-03 test regressed
- Note for orchestrator: G-07-01 closed at the code level; UAT re-run is needed to flip the gap status from `failed` to `resolved` in 07-UAT.md
</output>
</content>
</invoke>