---
phase: 07
plan: 03
type: tdd
wave: 2
depends_on: [07-02]
files_modified:
  - app.py
  - README.md
autonomous: false
requirements: [REQ-pdf-text-only]
tags: [pdf, audit, green, streamlit, ui, docs]

must_haves:
  truths:
    - "Streamlit uploader accepts .pdf alongside .docx (SUPPORTED_UPLOAD_TYPES == ['docx', 'pdf'])"
    - "Uploader label reads «Загрузите документ (DOCX или PDF)»; sidebar caption «PDF: только аудит, без OCR» appears under the uploader"
    - "preflight_translate_error returns the locked Russian string «PDF без извлекаемого текстового слоя — OCR не поддерживается.» for PdfNoTextLayer"
    - "preflight_translate_error no longer carries a NotImplementedError branch (dead code removed per Pitfall 3)"
    - "run_processing's except tuple includes PdfNoTextLayer; NotImplementedError removed from the tuple"
    - "render_report shows the audit-only badge «PDF — режим аудита, без исправлений» when result.input_extension == '.pdf'"
    - "render_report hides the corrected-DOCX download card when result.input_extension == '.pdf'"
    - "README.md §Limits paragraph contains «audit-only», «text layer», «no OCR» (or «OCR is not supported»), and «no corrected PDF» (or equivalent)"
    - "All Plan 07-01 RED tests are GREEN (the full 9 in tests/inference/test_pdf_loader.py + 1 new preflight test + 2 upload-contract assertions)"
  artifacts:
    - path: app.py
      provides: "PDF uploader support + sidebar caption + audit-only badge + DOCX-download gate + PdfNoTextLayer preflight branch"
      contains: "PdfNoTextLayer"
    - path: README.md
      provides: "§Limits section stating text-layer + audit-only + no-OCR + no-corrected-PDF (English per 07-UI-SPEC)"
      contains: "## Limits"
  key_links:
    - from: app.py:preflight_translate_error
      to: src.inference.pdf_loader.PdfNoTextLayer
      via: "isinstance(exc, PdfNoTextLayer) -> locked Russian string"
      pattern: "isinstance\\(exc, PdfNoTextLayer\\)"
    - from: app.py:render_report
      to: result.input_extension
      via: "if result.input_extension == '.pdf' guards audit-only badge + DOCX download visibility"
      pattern: 'input_extension.*==\\s*"\\.pdf"|input_extension\\s*!=\\s*"\\.pdf"'
    - from: app.py:run_processing except tuple
      to: PdfNoTextLayer
      via: "except (FileNotFoundError, PdfNoTextLayer, ValueError, zipfile.BadZipFile)"
      pattern: "PdfNoTextLayer.*ValueError"
    - from: README.md
      to: SC-4 (text-layer + read-only + no-OCR boundary)
      via: "## Limits section text"
      pattern: "audit-only|text layer|no OCR|OCR is not supported"
---

<objective>
Wave 2 GREEN for Phase 7 UI + docs (REQ-pdf-text-only). Closes all four Phase 7 success criteria.

Purpose: Land the four `app.py` UI sites (SUPPORTED_UPLOAD_TYPES, preflight branch, render_report badge + DOCX gate, sidebar uploader label + caption) and the `README.md` §Limits paragraph. After this plan: every Plan 07-01 RED test is GREEN; manual UAT (run `streamlit run app.py`, upload Berger PDF, verify badge + hidden DOCX download) passes.

Output: Two modified files (`app.py` + `README.md`).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/07-pdf-text-layer-audit-slice/07-CONTEXT.md
@.planning/phases/07-pdf-text-layer-audit-slice/07-RESEARCH.md
@.planning/phases/07-pdf-text-layer-audit-slice/07-PATTERNS.md
@.planning/phases/07-pdf-text-layer-audit-slice/07-UI-SPEC.md
@./CLAUDE.md

@app.py
@README.md
@src/inference/pdf_loader.py
@src/inference/application_service.py

<interfaces>
<!-- Frozen contracts from Plan 07-02 that this plan consumes. -->

# src/inference/pdf_loader.py (from Plan 07-02)

```python
class PdfNoTextLayer(Exception):
    """Raised when a PDF has fewer than 50% pages with extractable text."""

def check_text_layer(path: str | Path) -> float: ...
def extract_pdf_blocks(path: str | Path) -> list[dict]: ...
```

# src/inference/application_service.ProcessingArtifacts (post-Plan 07-02)

```python
@dataclass(slots=True)
class ProcessingArtifacts:
    model_type: str
    mode: str
    profile_path: Path
    input_path: Path
    input_extension: str          # ".docx" or ".pdf" — gates UI in render_report
    extracted_csv: Path
    predictions_csv: Path
    report_csv: Path
    report_json: Path
    summary_json: Path
    summary_txt: Path
    output_docx: Path | None      # None for PDF input (D-PDF-SCOPE)
    predictions_df: pd.DataFrame
    report_df: pd.DataFrame
    summary: dict[str, Any]
```

# Locked Russian strings (07-UI-SPEC + 07-CONTEXT)

- D-03 preflight rejection: "PDF без извлекаемого текстового слоя — OCR не поддерживается."
  Substrings asserted by tests: "PDF без извлекаемого текстового слоя" AND "OCR не поддерживается"
- D-04 §2 audit-only badge: "PDF — режим аудита, без исправлений"
- D-04 §3 sidebar caption: "PDF: только аудит, без OCR"
- Uploader label (Phase 7): "Загрузите документ (DOCX или PDF)"

# Existing CSS class to reuse (no new class introduced)

- `.badge.badge-warn` from inject_page_styles (Phase 6) — matches review status (yellow/amber)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: app.py — extend SUPPORTED_UPLOAD_TYPES, swap preflight branch (NotImplementedError → PdfNoTextLayer), update run_processing except tuple, change uploader label + add caption</name>
  <files>app.py</files>
  <read_first>
    - app.py (full file — sites at lines 24, 51-71, 425, 670-674)
    - tests/test_preflight.py (post Plan 07-01 — confirms locked Russian substrings)
    - tests/test_app_upload_contract.py (post Plan 07-01 — confirms ["docx", "pdf"] assertion)
    - .planning/phases/07-pdf-text-layer-audit-slice/07-UI-SPEC.md §"Copywriting Contract" + §"Download visibility" + §"Error state copy"
    - .planning/phases/07-pdf-text-layer-audit-slice/07-PATTERNS.md §"app.py (modify — 4 discrete sites)"
    - .planning/phases/07-pdf-text-layer-audit-slice/07-RESEARCH.md §"app.py Extension Sites" line table
  </read_first>
  <action>
    Five surgical edits to `app.py`. Per CLAUDE.md «не рефактори то, что работает»: do NOT touch any code outside the lines listed. Do NOT renumber, do NOT reorder imports beyond what is required, do NOT modernise other Phase 6 sites.

    **Edit 1 — add PdfNoTextLayer import (after line 22, before line 24):**

    Replace the import block at the top of the file. Current shape:

    ```python
    from src.inference.application_service import (
        REPORTS_DIR,
        ProcessingArtifacts,
        get_profile_options,
        list_model_options,
        process_document,
        save_uploaded_bytes,
    )
    from src.inference.run_log import RunLog
    from src.rules.methodical_extractor import build_methodical_profile, save_methodical_profile
    from src.rules.profile_diff import compute_profile_diff
    from src.rules.profile_loader import PROFILES_DIR, list_available_profiles, load_profile
    ```

    ADD after `from src.inference.run_log import RunLog` and before the rules.methodical_extractor import:

    ```python
    from src.inference.pdf_loader import PdfNoTextLayer
    ```

    Per CLAUDE.md «следуй существующему стилю»: this matches the per-module top-level absolute-import convention already used in the file.

    **Edit 2 — flip SUPPORTED_UPLOAD_TYPES (line 24):**

    Change:
    ```python
    SUPPORTED_UPLOAD_TYPES = ["docx"]
    ```
    To:
    ```python
    SUPPORTED_UPLOAD_TYPES = ["docx", "pdf"]
    ```

    **Edit 3 — swap preflight branch (lines 62-63), preserving the 5-branch enum shape:**

    Locate the current `preflight_translate_error` body (lines 51-71). Replace lines 62-63:

    ```python
        if isinstance(exc, NotImplementedError):
            return "PDF аудит ещё не поддерживается в этой версии."
    ```

    With:

    ```python
        if isinstance(exc, PdfNoTextLayer):
            # 07-CONTEXT.md D-03 — locked Russian message; substrings asserted by
            # tests/test_preflight.py::test_preflight_translate_pdf_no_text_layer.
            return "PDF без извлекаемого текстового слоя — OCR не поддерживается."
    ```

    Per Pitfall 3 (07-RESEARCH.md) and CLAUDE.md «Удаляй orphans»: the `NotImplementedError` branch is GONE entirely — Phase 7 makes that exception unreachable from `validate_document_input`. The 5-branch enum becomes a 5-branch enum with the «PDF аудит ещё не поддерживается» row swapped for the «PDF без извлекаемого текстового слоя» row.

    **Edit 4 — update run_processing except tuple (line 425):**

    Change:
    ```python
        except (FileNotFoundError, NotImplementedError, ValueError, zipfile.BadZipFile) as exc:
    ```
    To:
    ```python
        except (FileNotFoundError, PdfNoTextLayer, ValueError, zipfile.BadZipFile) as exc:
    ```

    Rationale: `process_document` no longer raises `NotImplementedError` (Plan 07-02 removed it from `validate_document_input`); it now raises `PdfNoTextLayer` in the PDF branch via `_process_pdf`. The catch-all `except Exception as exc` at line 438 stays as the fallback for unexpected exceptions (e.g. fitz parse errors on corrupt PDFs — T-07-W1-01).

    **Edit 5 — sidebar uploader label change + caption (lines 670-674):**

    Locate the existing uploader call (lines 670-674):

    ```python
    uploaded_file = st.file_uploader(
        "Загрузите DOCX",
        type=SUPPORTED_UPLOAD_TYPES,
        key="docx_uploader",
    )
    ```

    Replace with:

    ```python
    uploaded_file = st.file_uploader(
        "Загрузите документ (DOCX или PDF)",
        type=SUPPORTED_UPLOAD_TYPES,
        key="docx_uploader",
    )
    st.caption("PDF: только аудит, без OCR")
    ```

    The session-state key `docx_uploader` is PRESERVED (per 07-UI-SPEC «не рефактори то, что работает»; renaming would break Phase 6 session continuity). Only the label string changes; everything else under the `with st.sidebar:` block is untouched.

    Per CLAUDE.md «маленькие функции на одну ответственность»: this task touches only the upload + preflight surface; the badge + download-gate edits are Task 2 to keep diff scope clear.
  </action>
  <verify>
    <automated>python -m pytest tests/test_preflight.py tests/test_app_upload_contract.py tests/test_render_block_section.py -x 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep "from src.inference.pdf_loader import PdfNoTextLayer" app.py` matches exactly 1 line
    - `grep 'SUPPORTED_UPLOAD_TYPES = \["docx", "pdf"\]' app.py` matches exactly 1 line
    - `grep 'SUPPORTED_UPLOAD_TYPES = \["docx"\]$' app.py` returns no matches
    - `grep "isinstance(exc, NotImplementedError)" app.py` returns no matches (dead branch removed)
    - `grep "PDF аудит ещё не поддерживается в этой версии" app.py` returns no matches (dead copy removed)
    - `grep "isinstance(exc, PdfNoTextLayer)" app.py` matches exactly 1 line
    - `grep "PDF без извлекаемого текстового слоя — OCR не поддерживается" app.py` matches exactly 1 line
    - `grep "except (FileNotFoundError, PdfNoTextLayer, ValueError, zipfile.BadZipFile)" app.py` matches exactly 1 line
    - `grep "except (FileNotFoundError, NotImplementedError" app.py` returns no matches
    - `grep "Загрузите документ (DOCX или PDF)" app.py` matches exactly 1 line
    - `grep "Загрузите DOCX" app.py` returns no matches (old label gone — note that the warning message «Сначала загрузите DOCX-документ.» at line 407 is DIFFERENT text and stays unchanged; the grep regex must NOT match it — it does not, because line 407 has trailing «-документ.»)
    - `grep 'st.caption("PDF: только аудит, без OCR")' app.py` matches exactly 1 line
    - tests in `tests/test_preflight.py` and `tests/test_app_upload_contract.py` all GREEN on a Streamlit-enabled venv: `python -m pytest tests/test_preflight.py tests/test_app_upload_contract.py -x` exits 0; the affected `test_app_upload_contract_unchanged` in `tests/test_render_block_section.py` is also GREEN
  </acceptance_criteria>
  <done>
    Uploader accepts PDF; preflight returns locked Russian string for PdfNoTextLayer; except tuple corrected; dead NotImplementedError branch removed; sidebar caption renders. Tests for upload contract + preflight all GREEN.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: app.py:render_report — insert audit-only badge after header + gate corrected-DOCX download card on input_extension != '.pdf'</name>
  <files>app.py</files>
  <read_first>
    - app.py:303-381 (render_report function — focus on lines 319-320 for badge insertion, lines 360-367 for DOCX gate)
    - app.py:76-180 (inject_page_styles — confirm `.badge.badge-warn` class exists in the inline CSS; if not, find it elsewhere in the file)
    - .planning/phases/07-pdf-text-layer-audit-slice/07-UI-SPEC.md §"Spacing Scale" placement contract + §"Color status-chip contract" + §"Download visibility"
    - .planning/phases/07-pdf-text-layer-audit-slice/07-PATTERNS.md §"app.py (modify — 4 discrete sites)" §Site 3
    - tests/inference/test_pdf_loader.py (Plan 07-01 — confirm Plan 07-01 README test runs only against README.md, not against app.py)
  </read_first>
  <action>
    Two edits to `render_report` (app.py:303-381). Both gated on `result.input_extension == ".pdf"` — the field added in Plan 07-02.

    **Edit 1 — audit-only badge insertion (after line 320):**

    Current sequence (lines 319-322):

    ```python
    st.subheader(f"Отчёт по документу: {filename}")
    st.caption(f"Профиль: {profile_name} ({profile_id})")

    # 2. Summary counters.
    render_summary_counters(result.summary)
    ```

    Insert the badge between `st.caption(f"Профиль: ...")` and the `# 2. Summary counters.` comment. Replace lines 319-322 with:

    ```python
    st.subheader(f"Отчёт по документу: {filename}")
    st.caption(f"Профиль: {profile_name} ({profile_id})")
    if result.input_extension == ".pdf":
        # 07-CONTEXT.md D-04 §2 + 07-UI-SPEC §"Copywriting Contract" — locked badge text.
        # Reuses existing .badge.badge-warn class (no new CSS — CLAUDE.md «минимум кода»).
        st.markdown(
            '<span class="badge badge-warn">PDF — режим аудита, без исправлений</span>',
            unsafe_allow_html=True,
        )

    # 2. Summary counters.
    render_summary_counters(result.summary)
    ```

    The badge is rendered ONLY for PDF input; DOCX input is unaffected (Phase 6 contract preserved). The class `.badge.badge-warn` is the existing review-status chip class from `inject_page_styles` — no new CSS class introduced (per 07-UI-SPEC «no new chip color, no palette extension»).

    **Edit 2 — gate corrected-DOCX download card (lines 360-367):**

    Current:
    ```python
    if result.output_docx is not None and result.output_docx.exists():
        render_artifact_download_card(
            title="Исправленный DOCX",
            description="Редактируемый DOCX, созданный после безопасных исправлений.",
            path=result.output_docx,
            mime="application/vnd.openxmlformats-officedocument.wordprocessingml.document",
            key="download_output_docx",
        )
    ```

    Replace with:
    ```python
    if (
        result.input_extension != ".pdf"
        and result.output_docx is not None
        and result.output_docx.exists()
    ):
        render_artifact_download_card(
            title="Исправленный DOCX",
            description="Редактируемый DOCX, созданный после безопасных исправлений.",
            path=result.output_docx,
            mime="application/vnd.openxmlformats-officedocument.wordprocessingml.document",
            key="download_output_docx",
        )
    ```

    Per 07-UI-SPEC §"Download visibility": `result.output_docx` is `None` for PDF input (Plan 07-02 `_process_pdf` enforces this at the service layer), so the existing `is not None` check would already hide it — but the UI-SPEC mandates an explicit UI-side guard as «defense in depth» / documentation-of-intent. The triple guard makes the contract self-documenting at the call site.

    Per CLAUDE.md «не рефактори то, что работает»: do NOT extract a helper, do NOT reorder the section. The `render_artifact_download_card(...)` call itself is byte-for-byte identical to the current code; ONLY the surrounding `if` is widened.

    Per CLAUDE.md «маленькие функции на одну ответственность»: two surgical edits to one function in one task. No touching of `render_summary_counters`, `render_block_section`, `normalize_table_values`, or the run-log download section at the end.

    Sanity: verify the existing `.badge` / `.badge-warn` CSS class definitions exist in `inject_page_styles` (Phase 6 contract; should be near line 100-180). If for any reason they do NOT exist (which would contradict the read of UI-SPEC), STOP and raise a checkpoint — the badge would render unstyled.
  </action>
  <verify>
    <automated>python -m pytest tests/inference/test_pdf_loader.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep 'PDF — режим аудита, без исправлений' app.py` matches exactly 1 line (locked badge text)
    - `grep 'class="badge badge-warn"' app.py` matches ≥1 line (badge uses existing class — adjust grep if it appears in STATUS_CHIP rendering too; ≥1 is the floor)
    - `grep 'result.input_extension == ".pdf"' app.py` matches exactly 1 line (badge gate)
    - `grep 'result.input_extension != ".pdf"' app.py` matches exactly 1 line (DOCX download gate)
    - render_report does not introduce a new CSS class — `grep "badge-" app.py | grep -v "badge-ok\|badge-change\|badge-warn\|badge-error\|badge-muted"` returns no matches
    - `python -m pytest tests/inference/test_pdf_loader.py` reports 8 of 9 tests GREEN (README test still RED — Task 3)
    - `python -c "import app; print(app.SUPPORTED_UPLOAD_TYPES)"` prints `['docx', 'pdf']` (sanity — app module still imports cleanly)
  </acceptance_criteria>
  <done>
    render_report renders audit-only badge for PDF; corrected-DOCX download hidden for PDF; no new CSS class; no Phase 6 regression in the function shape.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: README.md — add §Limits section with all four locked substrings (audit-only, text layer, no OCR, no corrected PDF)</name>
  <files>README.md</files>
  <read_first>
    - README.md (full file — confirm existing section headings and where §Limits should land; current `## Ограничения MVP` at line 189 is Russian — Phase 7 adds a SEPARATE English-language §Limits per 07-UI-SPEC Claude's-discretion lock)
    - .planning/phases/07-pdf-text-layer-audit-slice/07-UI-SPEC.md §"README §Limits update" (paragraph contract — English, must include the 4 substrings)
    - tests/inference/test_pdf_loader.py::test_readme_limits_keywords (asserts the 4 substrings)
  </read_first>
  <action>
    Add an English-language `## Limits` section to `README.md` per 07-UI-SPEC §"README §Limits update". The exact placement: AFTER the existing `## Ограничения MVP` section (line 189) — at the end of the file. This keeps the Russian developer-facing limits (which predate Phase 7) intact and adds the Phase 7 PDF audit-only contract as a separate, English-language section addressed to CI/contributor audience (per 07-UI-SPEC rationale «CONTRIBUTING.md is already English»).

    Append the following at the bottom of `README.md` (after the last existing line):

    ```markdown

    ## Limits

    PDF input is supported in audit-only mode: the document must contain an extractable text layer (≥ 50% of pages must return non-empty text via `fitz.Page.get_text()`). Scanned PDFs and page-image PDFs are rejected at preflight — OCR is not supported. No corrected PDF is produced; `applied_fixes` is always empty for PDF blocks. DOCX input remains the only path that produces a corrected document.
    ```

    Substring contract (locked, asserted by `tests/inference/test_pdf_loader.py::test_readme_limits_keywords`):
    - `audit-only` — present verbatim ✓ (first sentence)
    - `text layer` — present verbatim ✓ (first sentence)
    - `OCR is not supported` (or `no OCR`) — present verbatim ✓ (second sentence: «OCR is not supported»)
    - `no corrected PDF` (or `no corrected ... PDF`) — present verbatim ✓ (third sentence: «No corrected PDF is produced»)

    Per CLAUDE.md «минимум кода» (applies to docs too): one paragraph, four sentences, ~70 words. No bullet list, no code block, no version-history table.

    Per CLAUDE.md «следуй существующему стилю»: README uses `## H2` per section heading and bare paragraphs — the new §Limits matches that.

    Do NOT modify, translate, or merge the existing Russian `## Ограничения MVP` section — it remains the in-app Russian limits surface; the new English `## Limits` is the Phase-7 contract per 07-UI-SPEC.
  </action>
  <verify>
    <automated>python -m pytest tests/inference/test_pdf_loader.py::test_readme_limits_keywords -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep "^## Limits$" README.md` matches exactly 1 line
    - `grep "^## Ограничения MVP$" README.md` matches exactly 1 line (existing Russian section preserved)
    - `grep "audit-only" README.md` matches exactly 1 line
    - `grep "text layer" README.md` matches exactly 1 line
    - `grep "OCR is not supported" README.md` matches exactly 1 line
    - `grep "no corrected PDF" README.md` OR `grep "No corrected PDF" README.md` matches exactly 1 line
    - `python -m pytest tests/inference/test_pdf_loader.py::test_readme_limits_keywords -x` exits 0 (the last RED test from Plan 07-01 is now GREEN)
    - `python -m pytest tests/inference/test_pdf_loader.py` reports 9 of 9 tests GREEN
    - `wc -l README.md` increase ≤ 6 (one blank + heading + one paragraph + blank — total ~4-5 new lines)
  </acceptance_criteria>
  <done>
    README §Limits paragraph in place; final Plan 07-01 RED test (README keywords) is GREEN. Phase 7 SC-4 satisfied: «README + UI copy state the text-layer + read-only + no-OCR limits».
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Manual UAT — run streamlit, upload Berger PDF, verify badge + caption + hidden DOCX download + locked Russian error path for scanned PDF</name>
  <files>—</files>
  <read_first>
    - .planning/phases/07-pdf-text-layer-audit-slice/07-VALIDATION.md §"Manual-Only Verifications"
    - .planning/phases/07-pdf-text-layer-audit-slice/07-UI-SPEC.md §"Checker Sign-Off"
  </read_first>
  <action>
    All four `app.py` Phase 7 UI edits + README §Limits paragraph. Phase 7 SC-1..SC-4 closed at the code level. This checkpoint is the human visual confirmation that ROADMAP SC-3 («UI uploader supports `.pdf` alongside `.docx` and clearly labels PDF as audit-only») renders correctly.

    Surfaces to verify in this checkpoint:
    1. Sidebar uploader label «Загрузите документ (DOCX или PDF)»
    2. Sidebar caption «PDF: только аудит, без OCR» under uploader
    3. PDF upload acceptance — Berger PDF processes without exception
    4. Audit-only badge «PDF — режим аудита, без исправлений» appears in the report header
    5. Corrected-DOCX download card does NOT render for PDF input
    6. Report CSV download + Summary JSON download + Run-log JSON download DO render for PDF input (per SC-2)
    7. Counters show all blocks routed to «Требуют проверки» (per D-02 «every PDF block is review»)
    8. Scanned-PDF rejection path: synthesise a scanned PDF (use the same `scanned_pdf` fixture recipe via a Python REPL) → upload → `st.error` shows the locked Russian string «PDF без извлекаемого текстового слоя — OCR не поддерживается.»
  </action>
  <verify>
    1. Activate the Streamlit-enabled venv (`source .venv/bin/activate` or equivalent — the venv used in Phase 6 verifier).
    2. Run `streamlit run app.py` and open the browser tab.
    3. **Sidebar check:** confirm the uploader label reads «Загрузите документ (DOCX или PDF)» (not the old «Загрузите DOCX»). Below the uploader, confirm the caption «PDF: только аудит, без OCR» appears in muted text.
    4. **PDF upload happy path:** upload `tests/fixtures/methodical/normocontrol_berger.pdf`. Click «Запустить аудит».
    5. After processing, confirm:
       - Report header: «Отчёт по документу: normocontrol_berger.pdf» followed by the profile caption.
       - Immediately below: a yellow/amber badge reading «PDF — режим аудита, без исправлений» (CSS class `.badge.badge-warn`).
       - Summary counters: «Всего блоков: <N>», «Требуют проверки: <N>», all other counters 0.
       - Block list expands «Требуют внимания» section with one expander per PDF block; each expander reveals «Оригинальный текст блока» (PDF text) and «Причина ручной проверки» containing the substring «PDF блок».
       - Downloads section: «Отчёт CSV» button visible; «Сводка JSON» button visible; «Скачать журнал запуска (JSON)» button visible. «Исправленный DOCX» button MUST be ABSENT.
    6. **PDF scanned rejection path:** generate a scanned-PDF fixture on the fly:
       ```bash
       python -c "
       import fitz
       src = fitz.open()
       page = src.new_page(width=595, height=842)
       td = fitz.open(); tp = td.new_page(); tp.insert_text((50, 100), 'placeholder')
       pix = td[0].get_pixmap(matrix=fitz.Matrix(1.0, 1.0), colorspace=fitz.csGRAY)
       page.insert_image(page.rect, pixmap=pix)
       td.close()
       src.save('/tmp/scanned_uat.pdf', deflate=True)
       src.close()
       print('/tmp/scanned_uat.pdf')
       "
       ```
       Upload `/tmp/scanned_uat.pdf` and click «Запустить аудит».
    7. Confirm an `st.error` red banner displays **«PDF без извлекаемого текстового слоя — OCR не поддерживается.»** verbatim. No traceback visible. No report renders. The run-log JSON download (failure-path button) appears at the bottom.
    8. Confirm the run-log JSON download contents (open in editor): the «document-read» record has `status: "error"`, `error_class: "PdfNoTextLayer"`, `error_message: "PDF без извлекаемого текстового слоя — OCR не поддерживается."` — NO `text`, `paragraph`, `traceback` keys (D-04 PII boundary).

    If any of steps 3-8 fails: report the divergence and DO NOT mark the checkpoint complete. The implementer fixes the issue and re-runs the checkpoint.
  </verify>
  <resume-signal>Type «approved» if all 8 verification points PASS. Type the failing point number(s) + observed behaviour for any FAIL — executor will fix and re-request approval.</resume-signal>
  <done>
    User approval recorded; Phase 7 SC-1..SC-4 manually confirmed; phase ready to close via `/gsd-verify-work` and `/gsd-close-phase`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Streamlit upload widget → save_uploaded_bytes | Untrusted bytes from browser; suffix is taken from `uploaded_file.name` (path traversal mitigated by `tempfile.NamedTemporaryFile`) |
| preflight_translate_error(exc) → st.error | Exception object enters translator; only fixed Russian strings exit — PII boundary |
| render_report(result) → DOM | Audit-only badge uses `unsafe_allow_html=True` with a hardcoded `<span class="badge badge-warn">` — fixed string, no interpolation of user content |
| README.md → CI test | `tests/inference/test_pdf_loader.py::test_readme_limits_keywords` reads README and asserts substrings |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-W2-01 | Cross-Site Scripting | `st.markdown(..., unsafe_allow_html=True)` with audit-only badge | mitigate | The HTML string is a hardcoded constant containing no user input: `'<span class="badge badge-warn">PDF — режим аудита, без исправлений</span>'`. No string interpolation, no f-string variable, no concatenation with `filename` or `result.summary`. Plan 03 Task 2 acceptance asserts the exact literal via grep. |
| T-07-W2-02 | Information Disclosure | preflight_translate_error returns user-facing Russian string | mitigate | Translation function never interpolates `str(exc)` into the returned string; PdfNoTextLayer branch returns a literal Russian constant; PII boundary preserved per Phase 6 D-04. Test `test_preflight_translate_pdf_no_text_layer` asserts no «Traceback» and no «ratio=0.0» leak. |
| T-07-W2-03 | Spoofing | Uploader accepting `.pdf` extension | accept | Streamlit's `st.file_uploader` enforces `type=["docx", "pdf"]` client-side; server-side `validate_document_input` re-checks via `SUPPORTED_EXTENSIONS`. Extension-based spoofing is bounded — `fitz.open` validates PDF magic bytes; a non-PDF file with `.pdf` extension raises a fitz parse error, which propagates to the generic `except Exception` branch (already in run_processing per Phase 6). |
| T-07-W2-04 | Tampering | Locked Russian strings drift across deploys | mitigate | All four locked substrings are asserted by tests landed in Plan 07-01 (`tests/test_preflight.py`, `tests/inference/test_pdf_loader.py`, `tests/test_app_upload_contract.py`). CI fails on any character drift. README substrings asserted by `test_readme_limits_keywords`. |
| T-07-W2-05 | Information Disclosure | README §Limits leaks implementation details | accept | English paragraph contains only product-level limits (text-layer requirement, OCR scope, audit-only output). No file paths, no exception names, no internal API references. Matches 07-UI-SPEC §"README §Limits update" contract. |

The fitz-side parsing threats (T-07-W1-01..07) are covered in Plan 07-02's threat model — they are stage-shared but the catching/translation site is `app.run_processing`, which is finalised in this plan. No new fitz threat is introduced by Plan 03 itself.
</threat_model>

<verification>
1. Targeted GREEN of all Phase 7 test surface:
   ```
   python -m pytest tests/inference/ tests/test_preflight.py tests/test_app_upload_contract.py tests/test_render_block_section.py -v
   ```
   Expected: 100% GREEN. Total of 9 tests in tests/inference/test_pdf_loader.py + 5 in tests/test_preflight.py (1 new + 4 existing) + 2 in tests/test_app_upload_contract.py + 9 in tests/test_render_block_section.py — all pass on the Streamlit-enabled venv. On system Python the Streamlit-importing files skip cleanly via `pytest.importorskip("streamlit")`; tests/inference/test_pdf_loader.py runs without Streamlit and is GREEN.

2. Full suite:
   ```
   python -m pytest tests/
   ```
   Expected: no regression vs the Phase 6 close state. Pass/skip count identical to pre-Phase-7 main except for the 9 NEW tests in tests/inference/ (all PASS).

3. Static grep audit for locked strings (every assertion the planner mandated):
   - `grep -c "PDF без извлекаемого текстового слоя" app.py tests/test_preflight.py tests/inference/test_pdf_loader.py` totals ≥3
   - `grep -c "PDF — режим аудита, без исправлений" app.py` returns 1
   - `grep -c "PDF: только аудит, без OCR" app.py` returns 1
   - `grep -c "Загрузите документ (DOCX или PDF)" app.py` returns 1
   - `grep -c "## Limits" README.md` returns 1

4. Dead-code orphan audit (Pitfall 3 + CLAUDE.md «Удаляй orphans»):
   - `grep "NotImplementedError" app.py src/inference/document_loader.py src/inference/application_service.py` — only Streamlit / Phase 6 unrelated matches, NO match referencing «PDF input is not supported» or «PDF аудит ещё не поддерживается»
   - `grep "Only DOCX is currently supported" src/` — no matches

5. Manual UAT (Task 4) — required for plan close.

6. CLAUDE.md «верификация готово»: pytest must run in the current session before the SUMMARY is written.
</verification>

<success_criteria>
- [ ] app.py `SUPPORTED_UPLOAD_TYPES == ["docx", "pdf"]`
- [ ] app.py imports `PdfNoTextLayer` from `src.inference.pdf_loader`
- [ ] app.py `preflight_translate_error` returns the locked Russian string for `PdfNoTextLayer`; old `NotImplementedError` branch removed
- [ ] app.py `run_processing` except tuple is `(FileNotFoundError, PdfNoTextLayer, ValueError, zipfile.BadZipFile)`
- [ ] app.py sidebar uploader label is «Загрузите документ (DOCX или PDF)»; caption «PDF: только аудит, без OCR» under uploader
- [ ] app.py `render_report` shows audit-only badge gated on `result.input_extension == ".pdf"`
- [ ] app.py `render_report` hides corrected-DOCX download card on `result.input_extension == ".pdf"`
- [ ] README.md has `## Limits` section with 4 locked substrings (audit-only, text layer, OCR is not supported, no corrected PDF)
- [ ] All 9 Plan-07-01 tests GREEN in `tests/inference/test_pdf_loader.py`
- [ ] New `test_preflight_translate_pdf_no_text_layer` GREEN
- [ ] `test_app_upload_contract_unchanged` GREEN (with the Phase 7 contract content)
- [ ] No Phase 6 test regressed
- [ ] Manual UAT (Task 4) approved by project owner
- [ ] No new CSS class introduced; `.badge.badge-warn` reused for audit-only badge
- [ ] No dead `NotImplementedError`-PDF code anywhere in src/ or app.py
</success_criteria>

<output>
After completion, create `.planning/phases/07-pdf-text-layer-audit-slice/07-03-SUMMARY.md` per the standard template. Record:
- Full `python -m pytest tests/` exit summary (paste last 5 lines)
- The exact diff stats (`git diff --stat` for the plan)
- The four locked Russian substrings verified present (grep counts)
- Task 4 UAT verdict (approved / rejected + reason)
- Phase 7 close-readiness: «all 4 SCs satisfied — ready for `/gsd-verify-work`»
</output>
